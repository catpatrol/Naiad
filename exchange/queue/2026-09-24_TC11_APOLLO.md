TIER-C11 · THE ENGINE BOUND TO EVENTS — ranges nested, warnings measured, every action given its trigger
RATIFIED: operator 2026-09-24 ("conscientious range design — never ask again" · "early warnings on the
1h crosses" · "retune the scalper: 5m 12/89 to the top of the 1h range" · "decide adds on breakout or SFP"
· "tie entry→adds→harvest→take-profit to specific events" · "as large as it needs to be"). Drafted:
APOLLO. Executor: HEPHAESTUS. Seed 20260924. Corridor: full water, latest closed 4h bar, one as-of pin.
LAWS: resume law (R0 census; verify-or-quarantine; frozen texts; one corridor; draft off-bus; PROGRESS.json
per stage) · read-only worktrees · as-of warranties · every fixture leg states its failure condition ·
every grid reported whole · "a SELECTION, not a result" on every non-registered table · registrations are
ONE family, m = 9 independently-failable hypotheses (bar 0.10/9), text frozen at STEP Q.
STEP Q: file this paste to exchange/queue/2026-09-24_TC11_APOLLO.md (sha printed).

STAGE R · RANGES, DONE ONCE AND COMPLETELY
 R1 LENSES: the twin (analytics/rangefinder_census.py, pins = TC10 sha) runs on {5m, 15m, 1h, 4h, 12h,
    1d, 1w} × the five home assets (twelve new assets: 1h/4h/1d only, Tier-E). Macro scale self-
    calibrated per asset per lens to 0.75 confirmed ranges/100 bars; every pin printed.
 R2 FEASIBILITY GATE PER LENS [Q-R3 law]: confirmed-range height ÷ round-trip toll (distribution) + the
    edge-fade leg; PASS/FAIL printed per lens; a FAIL closes that lens for range trading in this and every
    later tier unless the toll model changes (maker twin below is the only reopening path).
 R3 THE NESTING VECTOR ["nest"]: at every instant of every campaign of every card (base v6, 9/12, the
    lanes below): for each lens L in {1h,4h,12h,1d,1w} — macro state (in-range/expansion), %-of-range,
    signed distance to nearest boundary (ATR of L), boundary age, deviations per side, last flip, and
    BOUNDARY COINCIDENCE (a boundary of L within 0.25 ATR_L of a boundary of L+1). Causal: a value at bar t
    uses only confirms/redraws stamped ≤ t (F-NEST-ASOF, sabotage leg: a leaked redraw must FAIL).
 R4 THE TWO TRADES PER LENS, TIER-E, WHOLE: for L in {1h,4h,12h,1d}: (a) BREAKOUT = macro death →
    first retest that HOLDS (band taps 89/127/200 and the memory-line, both printed); (b) SWING-FAILURE =
    deviation confirmed at a CONFIRMED boundary (spring/upthrust). Each conditioned on the lens above:
    {L+1 in expansion aligned · L+1 in-range mid · L+1 in-range at its own boundary (coincidence)}.
    Outcomes H20/H100 net of L's toll, base rate, null (gaps+order), per direction. This grid is the
    operator's fractal thesis measured once, completely.
 R5 STAMP v6's ENTRIES BY RANGE STATE (the five-row table extended): base-card outcomes by 4h and 12h
    state × %-of-range decile — the "killed in chop" test, printed first in §0.

STAGE W · EARLY WARNINGS FROM THE 1H CROSSES (Tier-E → one conditional registration)
 W1 stamp every 4h campaign's timeline with 1h events: 9/12, 12/26, 12/89 crosses (with/against the
    trade), only 1h bars CLOSED before the 4h bar closes (F-WARN-ASOF).
 W2 tables: P(win) and E[net R] given {1h counter-12/89 before +1R · 1h counter-12/26 before +1R ·
    1h 9/12 re-cross with trend after entry · 1h counter-12/89 after +1R} vs the base rate; also the
    SAME events as PRE-ENTRY conditions: does a 1h 12/26 with-trend cross inside the open 4h window
    precede the 4h trigger, and by how many bars (the relay's evidence).
 P-WARN-1 [40%, CONDITIONAL]: IF W2's "1h counter-12/89 before +1R" cohort's E[net] is below the
    base by a cluster-90% interval excluding zero, score the rule "exit at the 1h counter-12/89 close
    while pre-+1R" paired vs v6; ELSE report-only, no slot spent, stated.

STAGE G · ADMISSION GATES (registrations; each removes trades → two-sample)
 P-AGE-1 [50%] tide-age gate, TRAILING-quantile definition (refuse the OLD band) vs v6; the absolute
    definition (refuse age > 206 bars) printed as shadow.
 P-WIN-1 [50%] window-age gate: refuse triggers with arm→trigger lag ≥ 16 bars vs v6; lag 7–15
    printed as shadow cut.
 Tier-E: structure-distance (r_dist > 2.2 ATR) as admission/priority; the three gates crossed.

STAGE T · TRIGGERS AND LANES
 P-BRK-4H [45%] the 4h breakout-retest lane: 4h macro death → first HOLD retest on the 89 tap (memory-
    line printed beside); stop beyond retest extreme railed 1.0 ATR(4h); tide aligned; v6 management;
    vs zero, five assets (seventeen Tier-E).
 P-RELAY-1 [45%] ["relay"]: 4h window open+armed; ENTRY on the 1h 9/12 with-trend close inside it;
    stop = 4h pivot railed, R on 4h; miss column first-class (windows the relay never activates);
    vs the 12/26-triggered base, two-sample.
 FORWARD LEDGER (standing, unscored until n ≥ 30): frozen base and frozen 9/12 re-ridden on new bars
    only at every refresh; both books printed side by side.

STAGE S · THE SCALPER RETUNED ["scalp"]: trade the 5m 12/89 cross TOWARD the far 1h boundary.
 Preconditions: R2 must PASS at 1h (the 1h range is the target; if 1h ranges are not respected net of
    toll, the mode is closed and the stage prints that verdict and stops).
 Form: a live CONFIRMED 1h macro range; price in the lower (upper) third of it; a 5m 12/89 cross
    upward (downward) → enter at that 5m close; TARGET = the 1h range's far boundary minus 0.25 ATR_1h
    (take-profit order); STOP = beyond the 5m structural pivot, railed 1.0 ATR_5m; invalidation also on a
    1h boundary breach; no trail (this is a range trade — target or stop); 4h tide NOT required (range
    trades are two-way inside balance).
 Twins on every row: taker toll (record) AND maker toll (limit fill at the 5m close, entry and target
    only) — the reopening path. Regime gate printed: 5m ATR tercile, 1h boundary age.
 P-SCALP-2 [30%] vs zero, five assets, holdout era, taker; the maker twin and the 17-asset view Tier-E.

STAGE A · ADDS, DECIDED ["add"] (adds keep the campaign set → paired vs v6; ≤ 2 adds; tranche 0.5 unit;
 each add stops with the campaign)
 P-ADD-BRK [40%]: add when a range ONE LENS BELOW the trade (1h for a 4h campaign) DIES in the trade's
    direction after +1R has been touched.
 P-ADD-SFP [40%]: add when a swing-failure CONFIRMS at the trend-side boundary of the 1h range (the
    deviation back into range in the trade's favour) after +1R.
 Both printed against each other; neither promoted by the other's failure.

STAGE H · HARVEST AND TAKE-PROFIT TIED TO RANGE EVENTS
 keep: de-risk at the opposing 89/316 band (law).
 P-TP-RNG [35%]: take-profit of the remainder at the approach (0.25 ATR) of the NEXT-HIGHER lens's range
    boundary (12h boundary for a 4h campaign) when that lens is IN-RANGE; if the lens above is in
    EXPANSION, no take-profit (let the trail run) — paired vs v6; the wall-exit lesson (tail cut) is the
    named risk and the D15 tail ratio is printed on the row.

FIXTURES: F-CTRL v6 0.000e+00 cross-process-anchored · F-NEST-ASOF · F-WARN-ASOF · F-DEF (both tide-age
 definitions computed and printed; a swapped definition must FAIL) · F-GATE (each gate removes only the
 trades its text names; sabotage: an un-gated book must FAIL) · F-RELAY (3 relays hand-verified: 1h close
 precedes the 4h trigger bar) · F-SCALP (3 scalps hand-walked: 1h range alive, third confirmed, target
 and stop placed as written) · F-ADD (3 adds hand-verified against the 1h event tape) · F-TP (3 take-
 profits hand-verified against the 12h boundary as-of) · F-FEAS (feasibility verdict per lens re-derived
 from the height and toll columns) · F-DET · F-GRID · F-KEY · import-closure · worktree attestation.

CLOSE: exchange/reports/BUILD_2026-09-24_TIERC11_EVENTS.md — §0 verdicts first (nine rows + R5's
 chop table + the feasibility verdicts per lens) · the nesting grid whole · warnings tables · gates
 crossed · the scalper's twins · adds head-to-head · findings-not-fixed · BOX-COST · LEDGER append
 (m=9; SAIL still HELD; the forward ledger opened). publish_exchange; STATE push; final line:
 "Operator: click Sync now."
VETOES BY NAME: "nest" (lens set / coincidence 0.25 ATR) · "relay" (1h, not 15m) · "scalp" (lower-third
 entry, target −0.25 ATR, no trail, no tide) · "add" (≤2 adds, 0.5 unit, post-+1R only) · "tp" (next-higher
 lens, in-range only).
