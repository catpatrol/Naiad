# NOTE — APOLLO → ATHENA · 2026-08-15 · **HANDOFF: BOX & PUBLISH-GUARD GOVERNANCE**
**Route:** operator drags this into the ATHENA lane; a copy files to `exchange/reports/` on the
next builder paste. **Effect:** everything below leaves APOLLO's desk on your acknowledgment;
conflicts between this note and your standing rules → quote both, ask the operator.

## §1 · WHAT TRANSFERS (state as of 2026-08-15, all builder-verified on the bus)
1. **The publish guard, whole.** Operator ruling ["box", VETO]: `BOX_BYTES` 6,390,000 →
   **16,000,000**, warn 0.25→**0.50**, refuse 0.40→**0.80**; boundary semantics preserved
   (exactly 80.0% warns). `exchange/**` = 2,568,320 B = **16.05% OK**. Commits: the raise +
   `905f...` sweep fix (`905b9fc`). You now own: threshold custody, future raise/lower words to
   the operator, and the guard's config location.
2. **The rotation, unchanged.** Queue 003 scheduled ~2026-08-28, `AGE_DAYS = 30` untouched — the
   operator's ruling was explicit: *the ceiling rises, the housekeeping stays.* Execution and
   cadence are yours; APOLLO's only interest is that `exchange/reports/` content survives per the
   pointer discipline.
3. **The D3 metering gap (your queue-003 report, 2026-08-11, still open):** the guard meters
   `exchange/` against the box alone and under-reports true box occupancy by ~4 points. Under the
   new 16 MB ceiling the distortion shrinks but the principle stands — yours to close or
   deliberately accept, on the record.
4. **The named-constant change protocol (new, from today's incident — recommend CONVENTIONS
   adoption):** changing any named constant requires (a) grep the NAME, the VALUE, and the
   THRESHOLD TEXT — two of today's five sites contained no `BOX_BYTES` string at all; (b) list
   every dependent with a pin-vs-import decision recorded per site; (c) *a historical report
   reproduces history* — regenerators of filed documents stay pinned, labelled historical.
   Today's exemplar: `mc1_report.py:20` (`BOX_CAPACITY`, pinned+labelled) vs
   `census2b_report.py` (now imports, one definition in one place).
5. **Already yours, restated for one list:** Drive-mirror verify-then-delete · Cowork remounts +
   discriminator test · Spotlight privacy list · the D:-move-era root-cause file (closed by the
   Mac crossing unless you rule otherwise) · box-watch cadence in your all-lanes notes.

## §2 · WHAT APOLLO RETAINS
Content of `exchange/reports/` (what gets filed and why) · build-doc leanness as craft (the new
thresholds are room, not license) · pointer discipline (data never on the bus) · ledger appends ·
the probe ledger. APOLLO will not touch guard constants again without routing through you.

## §3 · HANDSHAKE
Acknowledge in your next all-lanes note (one line suffices). From that line, box governance
questions route ATHENA-first; APOLLO's lane is cleared for SSv12 exclusively (Tier-C2 baseline ·
oracle grid → Pine v12.0.2 · the registration slate).

— APOLLO · the guard held the line twice today — once against the budget, once against me.
