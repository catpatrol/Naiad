# SETUP REPORT — 2026-07-28

Environment: local Windows Claude Code, working Naiad clone.
All commands run from the repo root unless stated. No git write operation was performed anywhere in this task: no `add`, no `commit`, no `push`, no `checkout`, no `clean`.

---

## STEP 0 — asserts

| assert | expected | actual | result |
|---|---|---|---|
| working directory | inside `...\Midas-Claude Code Resources\naiad` | `/c/Users/luisf/OneDrive/Desktop/Midas-Claude Code Resources/naiad` | **PASS** |
| `git rev-parse --short HEAD` | `268cdf9` | `268cdf9` | **PASS** |

Both asserts passed; proceeded.

---

## PART A — virtualenv rebuild outside OneDrive

### A1 — capture

| item | value |
|---|---|
| existing `.venv` Python | **3.12.10** |
| `pip freeze` written to | `C:\venvs\naiad_requirements_2026-07-28.txt` |
| package count | **18** |
| `.venv` size | **225,308,460 bytes (214.9 MB)** |
| `.venv` file count | **7,813** |

Packages: certifi 2026.6.17, charset-normalizer 3.4.9, colorama 0.4.6, idna 3.18, iniconfig 2.3.0, numpy 2.1.3, packaging 26.2, pandas 2.2.3, pluggy 1.6.0, pyarrow 18.1.0, pytest 8.3.4, python-dateutil 2.9.0.post0, pytz 2026.2, PyYAML 6.0.2, requests 2.32.3, six 1.17.0, tzdata 2026.2, urllib3 2.7.0.

### A2 — build — **PASS**

Base interpreter `C:\Users\luisf\AppData\Local\Programs\Python\Python312\python.exe` reports **3.12.10** — same version as the old venv, so the version match is exact, not approximate.

New venv at `C:\venvs\naiad` (Python 3.12.10). All 18 packages installed, exit code 0, no package failed. `pip freeze` of the new venv **diffs clean against the captured requirements** — the two environments are package-identical.

New venv size: 225,161,948 bytes (214.7 MB), 7,825 files.

### A3 — verification — **PASS on a, b, d, e; one pre-existing failure on c**

**a) engine imports — PASS.** `engine.signals`, `engine.trading`, `engine.data`, `engine.cells` all import cleanly under `C:\venvs\naiad\Scripts\python.exe` from the repo root. `engine.version` exposes `ENGINE_VERSION`.

**b) library versions — PASS.** pandas **2.2.3**, numpy **2.1.3**, pyarrow **18.1.0** — identical to the old venv.

**c) pytest — 1 failed, 72 passed, 1 skipped, 1 warning.**

The failure is `fixtures/test_s2_sim.py::test_c141t213_regression`, raising
`FileNotFoundError: research_outputs\s1\resampled\BTCUSDT_30m.parquet`.

This is **not caused by the rebuild.** Evidence:

| venv | result |
|---|---|
| **old** `.venv` | `1 failed, 72 passed, 1 skipped, 1 warning in 27.39s` |
| **new** `C:\venvs\naiad` | `1 failed, 72 passed, 1 skipped, 1 warning in 25.40s` |

Identical counts, identical failing test, identical error. The cause is data, not environment: `research_outputs/s1/resampled/` now contains only `resample_report.json` — the parquet files were moved into `research_outputs/_archive/s1_2026-07-27.zip` by the 19.61 GB reclamation that is HEAD (`268cdf9`). The test has been failing since that commit regardless of interpreter.

**d) `scripts/reviewer_manifest.py` — PASS.** All four fixture lines:

```
PASS F-M1 - self sha256 d3a2004f6ae8fc22 re-read and equal
PASS F-M2 - true copy accepted, one-byte mutant rejected (41a5f11f0997)
PASS F-M3 - two builds byte-identical over 25885 chars
PASS F-M4 - porcelain unchanged (11 entries before and after)
```
Wrote `_reviewer_box/MANIFEST.json` — 46 sources, 23 box files, 11 untracked root.

**e) `scripts/daily_brief.py` — PASS.** Completes, **exit code 0**, 337 s, **8/8 fixtures pass**, headline biases printed for all ten assets. Note `F-B6` reports *"all Tier-2 fetches failed; brief still renders"* — the optional public fetches did not succeed on this run and the brief degraded gracefully exactly as that fixture is designed to prove. Not a regression.

### A4 — delete `.venv` — **EXECUTED, after operator ruling**

The gate was tripped on a literal reading (pytest returned one failure), so deletion was **held** and put to the operator with the evidence. Ruling: *fix the three hardcoded references first, then delete.* Both steps are done.

**Step 1 — the three functional references repointed** (see A5 for why these three):

| file | before | after |
|---|---|---|
| `.claude/commands/brief.md` | `.venv/Scripts/python.exe scripts/daily_brief.py` | `C:\venvs\naiad\Scripts\python.exe scripts/daily_brief.py` |
| `.claude/commands/brief-history.md` | `.venv/Scripts/python.exe scripts/brief_lookup.py $ARGUMENTS` | `C:\venvs\naiad\Scripts\python.exe scripts/brief_lookup.py $ARGUMENTS` |
| `scripts/setup_brief_schedule.ps1` | `$python = Join-Path $repo ".venv\Scripts\python.exe"` | `$python = "C:\venvs\naiad\Scripts\python.exe"` |

Each carries a short comment noting the venv lives outside the repo because a venv cannot be relocated. Verified **before** deleting the fallback: `brief_lookup.py --date 2026-07-28` runs under the new interpreter and exits 0; the ps1's `Test-Path $python` guard passes; no `.venv` string remains in any of the three.

**Step 2 — deletion:**

| item | value |
|---|---|
| bytes freed | **225,308,460 (214.9 MB)** |
| files freed | **7,813** |
| `.venv` present afterwards | **no — confirmed absent** |
| `C:` free space | 40.2 GB → **40.4 GB** |

**Post-delete sanity — PASS.** `engine.signals`, `engine.trading`, `engine.data`, `engine.cells` all import under `C:\venvs\naiad\Scripts\python.exe`; `reviewer_manifest.py` re-run returns F-M1..F-M4 all PASS.

**Residual `.venv` references — reported, deliberately not edited.** Twelve scripts still mention `.venv` in their **module docstrings** as usage examples (`brief_lookup.py:13-15`, `daily_brief.py:38-40`, `census_build.py:28-30`, `census_analyze.py`, `census1b_*.py`, `s1_runner.py`, `s2_runner.py`, `s3_enrich.py`, `tc1_runner.py`, `tc4_runner.py`, `tc5_runner.py`), plus `README.md:22-33`, `LEDGER.md`, `tests/brief_fixture/README.md` and ~40 allowlist prefixes in `.claude/settings.local.json`.

**None of these is executable code — nothing breaks.** They are stale documentation. I left them alone for a specific reason beyond scope discipline: most of those scripts are among the manifest's **46 tracked SOURCES**, so editing even a docstring changes their sha256 against HEAD and flips `match_head` to `false`, dirtying the integrity manifest for a cosmetic gain. That should be a deliberate, batched act, not a side effect of a venv move.

The one practical consequence: the `.claude/settings.local.json` allowlist entries keyed to `.venv/Scripts/python.exe` no longer match, so expect **more permission prompts** on routine python calls until they are rewritten.

### A5 — agent-instruction and python-path references — **report only, nothing edited**

**No `CLAUDE.md` at the repo root.** Also absent: `CLAUDE.local.md`, `AGENTS.md`, `AGENT.md`, `.cursorrules`, `.windsurfrules`, `GEMINI.md`, `.github/copilot-instructions.md`.

But agent-facing instruction files **do** exist under `.claude/`, and they hardcode the in-repo venv. These break the moment `.venv` is deleted:

| file | line | reference | effect of deleting `.venv` |
|---|---|---|---|
| `.claude/commands/brief.md` | 10 | `.venv/Scripts/python.exe scripts/daily_brief.py` | **`/brief` breaks** |
| `.claude/commands/brief-history.md` | 11 | `.venv/Scripts/python.exe scripts/brief_lookup.py $ARGUMENTS` | **`/brief-history` breaks** |
| `scripts/setup_brief_schedule.ps1` | 41, 53 | `$python = Join-Path $repo ".venv\Scripts\python.exe"`; `throw "venv python not found at $python"` | **scheduler installer hard-fails** |
| `.claude/settings.local.json` | ~40 entries | allowlist prefixes on `.venv/Scripts/python.exe` | no breakage; the entries stop matching, so **more permission prompts** |
| `README.md` | 22–33 | setup instructions say `python -m venv .venv` | documentation drift only |

Three of these are functional breakage, not cosmetic. I have not edited any of them, per instruction.

---

## PART B — Google Drive sync folder

**CORRECTED 2026-08-04 — this is no longer true.** `G:` IS mounted and holds 988 MB of estate and workflow archives under `G:/My Drive/naiad-backups`, plus a separate one-off legacy snapshot and, as of 2026-08-04, a `phases/` folder. Drive for Desktop was installed on **2026-07-28, the same day as this report and shortly after it was written** — see `FIXUP_2026-07-28.md`, which recorded two `GoogleDriveFS` processes running and correctly noted that this finding "was correct when written". The blocker this line recorded on `backup_estate.py`'s offsite target is RESOLVED for `--estate` and `--workflow`; `--phase` remains unwired to Drive by a separate cause (it hardcodes an in-repo destination — see the 2026-08-04 TC5-OFFSITE build report). Superseded text: “**Finding: there is no Google Drive on this machine. Not a streaming mount, not a local folder, not installed.**”

Checked and all absent:

- **Drive letters:** only `C:` exists (`Windows-SSD`). No `G:`, no `H:`, no mount of any letter carrying a `My Drive` folder.
- **Drive for Desktop install:** `C:\Program Files\Google\Drive File Stream`, `C:\Program Files (x86)\Google\Drive File Stream`, `C:\Program Files\Google\DriveFS`, `C:\Users\luisf\AppData\Local\Google\DriveFS` — none exist.
- **Processes:** no `GoogleDriveFS`, no `googledrivesync` running.
- **Folder paths:** `C:\Users\luisf\Google Drive`, `C:\Users\luisf\My Drive`, `C:\Users\luisf\GoogleDrive` — none exist. The only cloud-sync folder in the user profile is `C:\Users\luisf\OneDrive`.

| item | value |
|---|---|
| Google Drive path | **none found** |
| real local folder or streaming mount | **neither — not present** |
| `C:` free space | **40.2 GB free of 236 GB** |

**This contradicts a FACT line in the Part D status text** — *"a local Google Drive sync folder exists so offsite archiving can be automated [verified]."* I appended your block verbatim as instructed and did not alter it, but the claim does not hold on this machine. Consequently **PENDING item 3 (`backup_estate.py`) is blocked on more than Amendment 1** — there is no Drive path for Part B to supply. Either Drive for Desktop needs installing, or the offsite target needs to be something else.

Also worth flagging while it is in view: `C:` has only **40.2 GB free**. The new venv consumed 215 MB of it, and any local staging for offsite archiving needs to be sized against that headroom, not against the 2 TB the OneDrive folder might suggest.

---

## PART C — daily routine

### C1 — `scripts/routine_jobs.json` — created, exactly as specified

Registry written verbatim: version 1, python `C:\venvs\naiad\Scripts\python.exe`, output_dir `_reviewer_box/daily`, two jobs (`manifest` required, `brief` optional) with their stage mappings.

### C2 — `scripts/daily_routine.py` — created

Implements the full contract:

- repo root resolved as `Path(__file__).resolve().parent.parent` — **never hardcoded**
- reads the registry; runs jobs **in registry order** via the configured interpreter
- **required** job failure stops the run; **optional** failure is recorded and the run continues
- creates `output_dir` if absent; stages each declared output with `{date}` substituted
- writes `<output_dir>/DAILY_<yyyy-mm-dd>.md` with the five sections in the specified order
- **performs no git operations of any kind** — repo state is read only from the staged manifest
- returns exit 1 if any required job failed, else 0

**One spec point I could not satisfy as literally written, and how I handled it.** Section 4 asks for *"commits added"*. That is not derivable: the routine is forbidden from git operations, and the manifest records only the HEAD sha, not a commit list. The report therefore prints the HEAD transition (`old -> new`) and states plainly that commit enumeration is unavailable by contract. Everything else in section 4 — sha256 changes, untracked added/removed, the `no change since <date>` case — is fully implemented. If you want real commit counts, the routine needs either a git exemption or the manifest needs to start recording `git rev-list` output.

### C3 — hand run — **PASS**

```
  manifest: exit=0 elapsed=4.5s
  brief: exit=0 elapsed=211.2s
wrote _reviewer_box/daily/DAILY_2026-07-28.md
ROUTINE_EXIT=0
```

**`git status --porcelain` identical before and after: 13 entries both times, zero-line diff.**

Change-detection could not be exercised by this run (no earlier manifest exists), so I tested it separately against a synthetic prior manifest in a temp directory, then discarded it. It correctly reported a HEAD move, a changed sha256, an untracked addition and an untracked removal; and the control case with an identical prior correctly returned `no change since 2026-07-27`.

Generated report follows in full.

---

```markdown
# DAILY -- 2026-07-28

Generated 2026-07-28T22:29:06Z by `scripts/daily_routine.py` (registry version 1).

## 1. Failures

all jobs OK

## 2. Jobs

| job | exit code | elapsed (s) | staged |
|---|---:|---:|---|
| manifest | 0 | 4.5 | `MANIFEST_2026-07-28.json` |
| brief | 0 | 211.2 | `brief_2026-07-28.html`, `brief_2026-07-28.json` |

## 3. Repo state

- HEAD: `268cdf9220bde8e21d73dc47ffaea955d8fde0c1`
- branch: `v12-v1-census`
- ahead / behind: 0 / 0
- worktree clean (tracked files): yes
- sources with `match_head: false`: none
- untracked files at repo root (11):
    - `# ORCHESTRATOR PRIMER — what a fresh orchestrator session needs to know.txt`
    - `12-25EMA Trend Scanner-Pinescript.txt`
    - `CONTRACT_ANALYTICS1_BRIEF2_FORWARD0_draft.md`
    - `HANDOFF_2026-07-27_BRIEF_to_CENSUS_1.md`
    - `HANDOFF_2026-07-27_BRIEF_to_ENGINE.md`
    - `Naiad — Orchestrator Control Center.html`
    - `Naiad_Orchestration_and_Open_Questions.md`
    - `ORCHESTRATOR CONTROL CENTER — protocol & state of record.txt`
    - `Rvwap pine code.txt`
    - `STATUS — BRIEF (daily market brief · Atlas HTML report · live laboratory).txt`
    - `STATUS — ENGINE (engine builds · repo operations · integrity & manifest).txt`

## 4. Change since last run

No earlier manifest in the archive -- this is the first recorded run.

## 5. Headline bias

| asset | daily bias | weekly bias |
|---|---|---|
| BTCUSDT | Lean-short | Lean-short |
| ETHUSDT | Lean-long | Lean-long |
| FARTCOINUSDT | Lean-short | Lean-short |
| HYPEUSDT | Lean-short | Lean-short |
| JTOUSDT | Lean-short | Lean-short |
| LITUSDT | Neutral-mixed | Neutral-mixed |
| NEARUSDT | Lean-short | Short |
| SOLUSDT | Lean-short | Lean-short |
| TAOUSDT | Lean-short | Lean-short |
| ZECUSDT | Neutral-mixed | Neutral-mixed |
```

### C4 — no scheduler registered

Confirmed: **no scheduled task, no cron, no trigger of any kind was created.** Note that `scripts/setup_brief_schedule.ps1` — the existing installer — would fail against a deleted `.venv` (see A5); it needs its `$python` path repointed at `C:\venvs\naiad\Scripts\python.exe` before it can register anything.

---

## PART D — status relay

### D1 — appended verbatim

Written to **`claude\STATUS_SYSTEM.md`** (file and its parent directory did not exist; both created). The block was written exactly as supplied, unmodified.

**Path note:** a directory named `claude` (no dot) did not exist. `.claude` does exist and holds Claude Code's own `commands/` and `settings.local.json`. I took your path literally and created `claude\`, on the reasoning that an operator status relay is not Claude Code configuration. If you intended `.claude\STATUS_SYSTEM.md`, it is a one-line move — say so and I will do it.

**Content flag, not altered:** the FACT line asserting a local Google Drive sync folder exists `[verified]` is contradicted by Part B. See that section.

### D2 — final `git status --porcelain`

```
 M .claude/commands/brief-history.md
 M .claude/commands/brief.md
 M scripts/setup_brief_schedule.ps1
?? "# ORCHESTRATOR PRIMER \342\200\224 what a fresh orchestrator session needs to know.txt"
?? "12-25EMA Trend Scanner-Pinescript.txt"
?? CONTRACT_ANALYTICS1_BRIEF2_FORWARD0_draft.md
?? HANDOFF_2026-07-27_BRIEF_to_CENSUS_1.md
?? HANDOFF_2026-07-27_BRIEF_to_ENGINE.md
?? "Naiad \342\200\224 Orchestrator Control Center.html"
?? Naiad_Orchestration_and_Open_Questions.md
?? "ORCHESTRATOR CONTROL CENTER \342\200\224 protocol & state of record.txt"
?? "Rvwap pine code.txt"
?? "STATUS \342\200\224 BRIEF (daily market brief \302\267 Atlas HTML report \302\267 live laboratory).txt"
?? "STATUS \342\200\224 ENGINE (engine builds \302\267 repo operations \302\267 integrity & manifest).txt"
?? claude/
?? scripts/daily_routine.py
?? scripts/routine_jobs.json
```

17 entries. Nothing committed, nothing pushed.

- **3 modified tracked files** — `.claude/commands/brief.md`, `.claude/commands/brief-history.md`, `scripts/setup_brief_schedule.ps1`, all from the A4 repointing the operator authorised. These will show `match_head: false` in the next manifest until committed; that is expected, not drift.
- **3 new untracked** from this task — `claude/`, `scripts/daily_routine.py`, `scripts/routine_jobs.json`.
- **11 pre-existing untracked** at the repo root, unchanged.

The manifest was re-run after deletion and reports 46 sources / 28 box files / 11 untracked root, F-M1..F-M4 all PASS.

---

## Observations worth your attention

1. **A file appeared at the repo root mid-task that I did not create:** `# ORCHESTRATOR PRIMER — what a fresh orchestrator session needs to know.txt`. The root untracked count went 10 → 11 between the start of Part A and the manifest run. Most likely OneDrive sync landing a file from another machine or session. Flagged because a repo root that mutates underneath a run is exactly what the manifest's untracked inventory exists to catch — and because it means a scheduled routine will occasionally see root changes that no local action caused.
2. **`_reviewer_box/` is gitignored** (`.gitignore:80`), so this report, the reply handoff, and every staged daily artifact stay out of git by design. `research_outputs/brief/` is likewise ignored (`.gitignore:83`). The routine therefore cannot dirty the tracked worktree, which is what C3 confirmed empirically.
3. **The brief ran twice today** (once for A3e, once inside the routine), so `research_outputs/brief/brief_2026-07-28.*` was overwritten by the second run and the staged copies are from the routine.
