# TC11 SCOUT · SUBSYSTEM G — DOCTRINE GLOSSARY

Scout map for HEPHAESTUS (TIER-C11). Read-only scouting, 2026-09-24, branch `v12-v1-census`, HEAD `b9ed953`.
Contract of record: `exchange/queue/2026-09-24_TC11_APOLLO.md` (103 lines, cited below as `TC11:<line>`).
Paths are repo-relative unless absolute. "RC" = `scripts/tierc7_rules.py` as `tierc9.py` imports it (it re-exports
the tierc6→tierc5→tierc4→tierc3→tierc2 register objects by identity). "C" = `scripts/tierc10_census.py`,
"TP" = `scripts/tierc10_panel.py`, "BRK" = `scripts/tierc10_brk.py`, "LR" = `scripts/tierc7_lab_regime.py`,
"T9" = `scripts/tierc9.py`, "RF" = `engine/rangefinder.py` (the machine), "RFC" = `analytics/rangefinder_census.py` (the port).

Counts quoted from the filed TC10 control journal (`research_outputs/tierc10/panel/control_journal.parquet`,
sha256 `fcbf5db0…`, 200 campaigns, CLASSIC5, as-of 2026-09-21T16:00Z) are COHORT SIZES ONLY. The scout read no
outcome for any TC11 gate cohort into this file on purpose.

---

## 1 · THE GLOSSARY

| term | operative definition (and value) | source file:line | code that implements it |
|---|---|---|---|
| **tide** | 4h, three-state. LONG iff `e89 > e316 AND close > e316`; SHORT on the mirror; else STAND DOWN (0). Gate evaluated **only at the arming bar** (the 12/89 cross), never re-checked at the trigger. | `scripts/tierc5_rules.py:244` (RULE_CARD_V5 "TIDE"); `exchange/reports/SSV12_LOGIC_2026-08-16.md:15-18`; periods `scripts/tierc2_rules.py:66-67` (TIDE_FAST 89, TIDE_SLOW 316) | `scripts/tierc2_rules.py:317-321` (in `armings`); de-aliased `T9.armings9` `scripts/tierc9.py:168`; array form `BRK.tide_state(e_fast,e_slow,c)` (−1/0/+1); per-exec-bar as-of `BRK.tide_4h_for_exec(exec_open_ms, f4, roles)` (4h bar visible iff it CLOSED at or before the exec bar's OPEN) |
| **tide age** | **TWO INCOMPATIBLE DEFINITIONS EXIST.** (A) TC7-lab / TC10 P-AGE-1 of record: run length of `sign(e89−e316)` (TWO-state, price ignored) on 4h, flip bar = 1, counting starts at `TIDE_WARM_BARS`=316 (left-censored before), read **at the ENTRY bar** (`run[t.entry_i]`). (B) TC9: consecutive bars ending **at the ARM bar** whose THREE-state tide (incl. `close vs e316`) equals the arm bar's; flip bar = 1; `censored=True` if count reached `roles.floor_bars`. Scales differ a lot: (B) median on non-top10 campaigns 100.5 bars vs top-10 8.0 (`research_outputs/tierc9/a2_separation.csv`, row `tide_streak_age_bars`); (A) bar-pooled whole-corridor quartiles `[88, 206, 396]`. | (A) `scripts/tierc7_lab_regime.py:306-352` `tide_streak(sym)->(state,run,start)`; TC10 reading `research_outputs/tierc10/close/P_AGE_1_TIDE_YOUTH.md:9`. (B) `scripts/tierc9.py:537-562` `tide_streak_age(t, roles)->(age,censored)`, column `tide_streak_age_bars` in `T9.features9` | (A) `LR.tide_streak`; bands `LR._streak_band(run, edges)`; `LR.STREAK_BAND_QUANTILES=(0.25,0.5,0.75)` `:181`; `LR.STREAK_LABELS` `:446` = (`B1 YOUNG`,`B2 EARLY`,`B3 MATURE`,`B4 OLD tide`). TRAILING edges: `T9._trailing_pool(lo,hi)` `:1290` + `T9._trailing_edges(pool, at_ms)["streak_bar"]` `:1318` (prefix `open_ms <= at_ms`, pooled over the 5 panel assets, None until ≥30 bars). Entry-banding loop `T9.tc7g_tables` `:1331` (at_ms = `f.open_ms[t.entry_i]`). |
| **window (open)** | Opens at the close of a 4h **12/89 cross** in the permitted direction ("arming"); in the CARD it closes ONLY at the **counter 12/89 cross** (`window_end_i`, exclusive). Displacement at the cross `|close − e89|/ATR ≥ 0.75` (`D_DISPLACEMENT`, strip {0.5,1.0}). Note: **posture_engine's DEAD word also closes a window on the 89/316 turning against** — the card does not; they diverge. | `scripts/tierc5_rules.py:245-246`; `scripts/tierc2_rules.py:68-69,72` ; card window `scripts/tierc2_rules.py:296-333`; posture `scripts/posture_engine.py:243-270` | `RC.armings(sym,f,lo_i,hi_i)` / `T9.armings9(sym, roles, lo_i, hi_i)`; `Arming` dataclass `scripts/tierc2_rules.py:232` (fields `arm_i, arm_ms, window_end_i, tide_ok, disp, d_ok, trigger_i, reject`) |
| **armed / arm / arming** | "Arming" = the 12/89 cross itself (the unit the funnel counts). A window is **ARMED** (posture word) when it is open AND admitted: `tide_ok AND d_ok`, no counter 12/89 (and, in posture_engine only, no 89/316 against), no in-window trigger yet. Stamps keep `arming` and `window_open` as separate instants, equal by construction on v6 (LEAN S-d). | `scripts/posture_engine.py:243-252` (ARMED gate, RULED); `scripts/tierc10_stamps.py:299-303` (S-d) | `posture_engine.windows_for` `:430-470` (word logic: not admitted→STALKING; closed→DEAD; trigger→TRIGGERED; else ARMED) |
| **trigger** | First same-direction 4h **12/26 cross** inside the open window → **enter at that bar's close**. Search starts AT the arming bar inclusive (`trig[a.arm_i:end]`), so lag 0 is legal. Gate order: tide → d → trigger. One position per asset: a trigger at `ti <= open_until` is rejected `position_open`. | `scripts/tierc5_rules.py:247`; `scripts/tierc2_rules.py:70-71` (12/26) | `T9.card_candidates9` `scripts/tierc9.py:207-233` (search `:225`); `T9.replay9` `:334` (position gate `:391`); entry px `f.c[ti]` |
| **arm→trigger lag** | `lag = entry_i − arm_i` in **4h bars** (entry bar = trigger bar under v6). Heavily tied at 0. TC10 control (counts only): lag 0 → 52; 1–6 → 81; **7–15 → 4**; **≥16 → 63** (of 200). | `scripts/tc6v_riders.py:127`; `T9.features9` column `lag_arm_to_entry_bars` | journal has `arm_ms`,`entry_ms`: `lag = (entry_ms − arm_ms) // 14_400_000` |
| **12/26 cross** | The v6 trigger pair (EMA12 × EMA26 of close, engine `ind.ema`, seeded at series start). History: canon said 12/25; operator A2-3 (2026-08-16) ruled 26. The 12/25 pair survives as the BRK "ribbon" bell/lifecycle (`BRK.RIBBON_FAST, RIBBON_SLOW = 12, 25` `scripts/tierc10_brk.py:200`). | `scripts/posture_engine.py:255-262`; `scripts/tierc2_rules.py:70-71` | `ind.crossover/crossunder` `engine/indicators.py:66-78` (a>b now AND a<=b prior; NaN→False) |
| **9/12** | Trigger 9/12 replacing 12/26, all else v6 = **P-TRG-2**'s book: `tierc9.Roles(trg_f=9, trg_s=12)` (tide 89/316, window 12/89 unchanged). The frozen "9/12" of TC11's forward ledger. In TC11 Stage W/T also a **1h** 9/12 cross (relay entry, W1 event). | `research_outputs/tierc10/registrations/P-TRG-2.json` `book_spec.arms[0].ride.roles_fields`; TC9 cell `scripts/tierc9.py:122-130` | `T9.Roles(name=..., trg_f=9, trg_s=12)`; `T9.role_arrays(sym, roles)` `:136` |
| **12/89** | The WINDOW pair (4h). As an exit, the **counter 12/89 cross = bell** (`bell_12_89`). In TC11 also a 1h 12/89 (W-stage warning) and a **5m 12/89** (scalper entry). | `scripts/tierc2_rules.py:68-69`; bell `scripts/tierc5_rules.py:256` | 4h: `T9.role_arrays` `w_up/w_dn`; bell in `T9._ride_leg9` `:303-306` |
| **89/316 band + the de-risk law** | "HARVEST": **50% off at the FIRST armed touch of the OPPOSING 89/316 band, filled at that bar's CLOSE, once per campaign**; "from below"/outside is a precondition (`h_armed` only after a prior close outside the near edge). Edge = `max(e89,e316)` for a long, `min` for a short; touch is INTRABAR (`low<=edge` long / `high>=edge` short). `harvest_min_unit_r = −inf` (fires at a loss). Evidence: 37 fills, 32 helped, +5.74R; in-profit variant fires 0×; "its only job is variance destruction". 89/316 crossing against = the second bell (`bell_89_316`). | `scripts/tierc5_rules.py:252-254`; register `scripts/tierc4_rules.py:163-185` (HARVEST_FRACTION 0.5, HARVEST_BAND (89,316), MAX 1, FILL bar_close); `SSV12_LOGIC_2026-08-16.md:47-51,91-94` | `RC.harvest_edge` `scripts/tierc4_rules.py:415`, `harvest_outside` `:427`, `harvest_touched` `:445`, `harvest_fill_px`; ride usage `scripts/tierc9.py:282-293` |
| **harvest** | In TC-series code = the de-risk above (H1/H3 ruled 50%). Older doctrine (SS synthesis C7) used "harvest" for profit-taking into the opposing wall — TC11 keeps de-risk as "harvest" and names the new profit exit "take-profit" (`TC11:81-86`). | `exchange/reports/DEFINITIONS_D_H_2026-08-16.md:74,86`; `exchange/reports/SS_SYSTEM_SYNTHESIS_2026-08-06.md:40` | as above |
| **+1R** | R = entry stop distance (`r_dist`, fixed for life). "+1R touched" = first bar `j ∈ (entry_i, exit_i]` whose **intrabar favourable extreme** reaches `entry ± 1R` (high for long). HIGH-WATER LATCH: never un-set. Updated at the TOP of each bar, **before** the stop test (the exit bar counts). 103 of 200 v6 campaigns reached it. | `scripts/tierc6_rules.py:36-44`; `scripts/tierc10_stamps.py:289-294` (LEAN S-b) | `scripts/tierc9.py:274-279`; `tierc10_stamps._plus_1r_bar(f,t)` `:406`; journal `reached_1r`, `mae_to_1r_r` |
| **trail** | LPS-ratchet on confirmed **(2,2) fractals**, stop = pivot ∓ **0.5 ATR**, railed ≥ **1.0 ATR** from the confirming close, monotone, **min advance 0.05 ATR** (refused advance not remembered), governs the NEXT bar; **arms only after +1R** (v6's headline diff, H5). | `scripts/tierc6_rules.py:121-178` (REGISTER v6: TRAIL_ARM_AFTER_R 1.0 `:122`, TRAIL_MIN_ADVANCE_ATR 0.05 `:129`); `scripts/tierc4_rules.py:116-160` | `V6.trail_step` `scripts/tierc6_rules.py:241`; ride uses `T8.matched_step(... min_advance_atr=card.trail_min_advance_atr)` `scripts/tierc9.py:314-322` |
| **'R on 4h'** | R is the distance from entry to the **4h structural stop** (anchor/rail on 4h pivots and ATR(4h)); every result is in that unit ("ACCOUNTING: … R = ENTRY stop distance"). For the relay (`TC11:56`) it means: entry at a 1h close, stop and R still from the 4h pivot rail. | `scripts/tierc5_rules.py:257`; `SSV12_LOGIC:32-35` | `RC.struct_stop_4h(...).r_dist` |
| **pivot rail / railed stop** | Nearest confirmed 4h **(5,5)** swing pivot strictly beyond entry whose CONFIRMATION bar is within **200** 4h bars (the "800h pin"), stop = pivot ∓ **0.5×ATR(entry bar)**, then RAILED: `stop = farther of (pivot_stop, entry ∓ 1.0×ATR)`; no eligible pivot → trade NOT taken (`no_struct_anchor`). "Beyond an extreme, railed" variant (spring/BRK): `extreme ∓ 0.5 ATR`, railed 1.0 ATR. Rail binds on 49/200 v6 campaigns (r/ATR ≤ 1.0). | `scripts/tierc5_rules.py:248-250`; register `scripts/tierc3_rules.py:88` (PIVOT_LOOKBACK_4H 200), `:98` (MIN_STOP_ATR 1.0); `scripts/tierc2_rules.py:93,103` (PIVOT_L/R 5, STOP_BUF_ATR 0.5) | `RC.struct_stop_4h(pv, cur_i, entry_px, direction, atr_sig, lookback=200, min_stop_atr=1.0, forbidden=None) -> Stop|None` `scripts/tierc3_rules.py:237`; `RC.spring_stop` `scripts/tierc5_rules.py:481`; `BRK.brk_stop(direction, retest_extreme, touch_i, entry_px, atr_sig, rail_atr=1.0)` |
| **the wall-exit lesson** | **P-WALL-1** (TC6): exit the remainder on a CLOSE within **0.25 ATR** of the profit-side **12h champion wall** → paired Δ **−0.0331** [−0.0859, +0.0278], p 0.8318, NOT SUPPORTED; "**loses BY CUTTING THE TAIL** (tail_exit_ratio 0.9329). 16 wall exits." The mechanism the card depends on is the tail; a take-profit near a structural level cuts it. TC11 names it as P-TP-RNG's risk. | `exchange/status/LEDGER_APOLLO.md:1840-1845`; `exchange/reports/BUILD_2026-08-16_TIERC6_REVB.md:17,111,122,126`; WALL_EXIT_ATR `scripts/tierc6_rules.py:148` | `V6.wall_touch(close_px, wall_px, atr, …)` `scripts/tierc6_rules.py:354`; `V6.wall_series_12h` `:278`; card knob `wall_exit_atr` |
| **D15 tail ratio** | `tail_exit_ratio` = **MEAN of the top decile** (k = floor(0.1n), ≥1) of the cell's `net_r` ÷ the same for the base. Unbounded, no pass bar, printed not judged ("D15 caveats not hard gates"). Siblings: `paired_delta_expectancy_r` (mean Δ over pairs keyed (symbol, entry_ms)), `max_single_trade_delta_share`, `n_paired`, `unpaired_*`. | `scripts/tierc5.py:579-624`; `exchange/reports/DEFINITIONS_D_H_2026-08-16.md:53-64` | `T5.d15(cell, base) -> dict` (`T9.d15`); used as `**d15(book, base)` in rows |
| **memory-line** | A **dead MACRO range's boundary** (its corpse line). v1: frozen at the first touch after death (`h >= top` / `l <= bottom`). v2 LEASH: lines expire untouched after `MEM_TTL_BARS`=400 bars or when >6 LIVE per side (oldest out); an expired line neither freezes nor flips. FLIP = the FIRST retest from the side opposite its birth side that survives `FLIP_HOLD_BARS`=6 bars with no body close through by `FLIP_HOLD_MARGIN`=1.0×ATR (dead top→support ▲, dead bottom→resistance ▼); a failed first retest CONSUMES the candidacy; knowable only at `touch_i + 6`. | `engine/rangefinder.py:469-478` (memory-touch), `:548-571` (FLIP, LEASH), `:584-589` (PINS_V2) | `RFC.flips_and_leash` `analytics/rangefinder_census.py:702`; `RFC.leash_known_at`; census classes `memory-touch-v1`, `memory-touch-2s`, `flip-hold` (`C.CLASSES` `scripts/tierc10_census.py:149`); lane anchor `BRK.memory_flip_candidates(h,l,dies,flips,n,hold_bars=6,ttl_bars=400)` |
| **band taps 89/127/200** | Operator R1 (2026-09-21) bands for the retest-hold: ribbons `[min,max](EMA89,EMA127)`, `[min,max](EMA127,EMA200)` and single-line taps EMA89, EMA127, EMA200, EMAs of close on the lens's own bars, NaN until the slower EMA has seen its period. RETEST-HOLD LAW: after a macro DIE, the FIRST bar whose [low,high] meets the band having CLOSED beyond it on the die side the bar before gets ONE evaluation; HOLD = no close through the band's far edge by `margin_atr×ATR[k]` over `hold_bars+1` bars; truncated windows never confirm; candidacy lapses `ttl_bars` after the DIE or at the next DIE; `known_at = touch + hold_bars`. Pins of record for ALL lenses = the **5m** R1 tuning: band ribbon127_200, **margin 1.0, hold 3, ttl 400** (tuning era only). 1d tuned separately (ribbon89_127, margin 0.25, hold 6) for Tier-E only. | `research_outputs/tierc10/OPERATOR_RULINGS.md:21-22`; `scripts/tierc10_census.py:141-153,222-241,261-290`; `research_outputs/tierc10/census/TUNING_RESULT.json`, `TUNING_RESULT_1d.json` | `C.retest_holds(tape, dies, lo, hi, margin_atr, hold_bars, ttl_bars)` `:983`; `BRK.r1_band(name)` (`"tap-89"` or `"tap89"`), `BRK.band_hold_candidates(h,l,c,atr,dies,lo,hi,margin_atr,hold_bars,ttl_bars)`, `BRK.candidacy_window(dies,n,ttl)`, `BRK.tuned_pins(lens="5m")` |
| **r_dist > 2.2 ATR** | `r_over_atr = r_dist / atr_at_entry` (initial risk in 4h ATR). v6 book median 1.6203; 53 of 200 campaigns > 2.2. **No on-disk provenance for 2.2** (grep: zero hits outside TC11). | TC11:49; column `r_over_atr` in `T9.features9` and `control_journal.parquet` | `t.r_dist / t.atr_at_entry` |
| **age > 206 bars** | 206 = the **whole-corridor bar-pooled MEDIAN (q50)** of definition (A) — the B2/B3 edge, NOT the OLD-band edge (q75 = 396). Under whole-corridor edges B3+B4 hold 62+58 = **120 of 200** v6 campaigns; trailing B4 OLD holds 59. So the "absolute shadow" refuses about twice what the registered OLD-band gate refuses. | `research_outputs/tierc10/close/P_AGE_1_TIDE_YOUTH.md:55-57,63,71-77` | `LR.cuts(lo,hi)["streak_bar"]` = `[88.0, 206.0, 396.0]` on the TC10 corridor (look-ahead edges; TC10's F-AGE break-leg uses exactly this substitution as its RED plant, `research_outputs/tierc10/close/FIXTURES_CLOSE_p_age_1.txt:68`) |
| **lag ≥ 16 and 7–15** | 4h-bar thresholds on arm→trigger lag. **No on-disk provenance** for 16 or 7 (grep: zero hits outside TC11). Cohorts on the TC10 control: ≥16 → 63/200 (31.5%); 7–15 → **4/200** (the shadow cut is structurally tiny, provisional under n<30). Prior art: TC6V L-LAG / P-LAG-1 cut by value at the 10th/90th percentiles (≤0 bars n=49 vs ≥71 bars n=20). | TC11:47-48; `exchange/reports/BUILD_2026-08-17_TC6V_TIERC7.md:103,111-113` | `scripts/tc6v_riders.py:273` `l_lag`, `:313` `l_lag_gate_evidence` |
| **the five-row table** | TC10 Stage-A table: v6 CLASSIC5 book by **rf4h macro state at ENTRY** — rows BEAR_EXP (n 40), BULL_EXP (66), NEUTRAL (94), NONE (0), `__ALL__` (200). Columns: `macro_state_at_entry, n, n_assets, net_r_sum, expectancy_r, median_net_r, win_rate_pct, median_mfe_r, median_pct_of_range, median_dist_boundary_atr, n_in_range, provisional, …, as_of_*`. Lens 4h, SCALE 3.0 (frozen). NEUTRAL ≡ a confirmed 4h range alive (in_range) on 200/200. | `research_outputs/tierc10/stamps/control_entry_by_state.parquet` (sha `e8a5df1c…`); `research_outputs/tierc10/close/REGIME_PRIOR.md:181-205` | `scripts/tierc10_stamps.py` (writer); extended entry×exit grid `research_outputs/tierc10/close/REGIME_PRIOR_V6_BY_STATE.parquet` |
| **'killed in chop'** | **No on-disk definition.** Operator hypothesis that v6 entries taken inside a live range die. TC11 R5 operationalises it as base outcomes by 4h AND 12h state × %-of-range decile. Estate "chop" objects are unrelated: P-CHOP-1 (WITHDRAWN) / P-CHOP-2 (filed, unscored) chop composite of CENSUS-2A. | TC11:30-31; `exchange/reports/CENSUS2A_CLOSEOUT_2026-08-12.md:45` | — (12h stamps do not exist; see GAPS) |
| **SAIL (HELD)** | SAIL = the move to live paper (Prometheus route) on a frozen `card_spec`; "SAIL-READY = card_spec freeze at v6 PLAIN". **Q6 (operator 2026-09-21): SAIL spec HELD until TC10 is autopsied**; TC10's contract forbids any SAIL act; TC11 LEDGER append must say "SAIL still HELD". `card_spec_v6.json` does not exist. | `exchange/queue/2026-09-22_TC10_RESUME_APOLLO.md:2-3,143`; `exchange/status/LEDGER_APOLLO.md:2348,2422`; `exchange/queue/008_sail-skeleton.md:14-24`; TC11:99 | — |
| **Q-R3 law** | "detection and gating first; a range-trading contract is drafted ONLY IF the **height-vs-toll feasibility gate AND the edge-fade outcome leg both pass**." TC10 operationalisation: ratio = `(top0−bottom0)/atr[confirm_i]` ÷ `(toll_bps/1e4)·close[confirm_i]/atr[confirm_i]` (ATR cancels). **HEIGHT PASS iff n_ranges ≥ 30 AND median(ratio) ≥ 3.0 AND share(ratio<1) ≤ 0.10**. Verdict = HEIGHT ∧ EDGE-FADE, judged **within one era** (ALL / tuning / holdout rows). TC10 result: 5m FAIL everywhere (0/17 single-asset on holdout and ALL); 4h pooled FAIL except CLASSIC5·holdout (+0.0062 ATR); 1d CLASSIC5 PASS on ALL/tuning, FAIL holdout (−0.4360). "THE HEIGHT FLOOR DOES NO WORK" (REV_MIN pins the height); the toll bites through the edge leg. | `LEDGER.md:834`; `scripts/tierc10_census.py:2153-2170` (thresholds), `:2186-2254` (HEIGHT_GATE_LAW; `gate_height` `:2199`); `research_outputs/tierc10/BUILD_DRAFT.md:673-744` | TC10 artifacts `research_outputs/tierc10/census/height_toll.parquet`, `height_toll_verdict.parquet` (360 rows, key `asset,lens,scale_kind,era`) |
| **edge-fade leg** | Trade TOWARD the far boundary from the **near 20%** of the range (`EDGE_NEAR_FRAC = 0.2`); PASS iff `median(term H20) − toll_atr > 0` within the row's own era AND `edge_n_ranges ≥ 30` AND `edge_n ≥ 30` in-range entries. Margins filed by entry decile × range-age bucket `AGE_BUCKETS ((0,5),(5,10),(10,20),(20,40),(40,80),(80,∞))`. | `scripts/tierc10_census.py:2164-2169` (floors, `EDGE_NEAR_FRAC` `:2168`), `:2231` (`gate_edge_fade`) | census Stage 0c (`R34_STAGE`) |
| **Q-R4** | "the census measures four candidate acceptance operationalisations head-to-head (**2-close / 3-close / 6-outside-close stale-run / time-beyond**); the engine default is chosen FROM DATA." TC10 printed them beside the RangeFinder DIE rule (8-close OR 1.5 ATR); **nothing promoted** (`BREAK_CONFIRM_N=8 · BREAK_MARGIN=1.5 — UNCHANGED`); time-beyond T = DEV_RETURN_BARS = 7. The DIE fires on its 1.5-ATR margin 82–87% of the time. | `LEDGER.md:835`; `scripts/tierc10_census.py:2110-2150`; `research_outputs/tierc10/BUILD_DRAFT.md:746-780` | `C.ACCEPTANCE_LAW`, `research_outputs/tierc10/census/acceptance_head_to_head.parquet` |
| **macro death (DIE)** | A CONFIRMED range's pending breach dies when **`closes ≥ BREAK_CONFIRM_N` (8) consecutive beyond-closes OR `|close − boundary| ≥ BREAK_MARGIN (1.5)×ATR` beyond**; state → DEAD, `die_i = i`, global latch → `BULL_EXP` (top) / `BEAR_EXP` (bottom). Event `breakout-die`; census class `DIE`, anchor = close of the die bar. Only the MACRO machine writes deaths/state. | `engine/rangefinder.py:455-468`; `scripts/tierc10_census.py:199-221` | `RFC.run_machine`; runner-side numbers `BRK.macro_signals(sym, lens, scale_mult=3.0) -> {'dies':[(die_i,rid,side)],'flips':[...]}` |
| **macro scale** | `SCALE_MULT` multiplies ONLY `LEG_MIN` (0.5) and `REV_MIN` (1.75) for the MACRO run (`macro_pins`). Frozen of record **3.0** (every registered lane and every Stage-A stamp, LEAN L2). CENSUS-R also printed a **self-calibrated** scale: grid `1.5..4.0 step 0.25`, pick = density nearest **0.75 confirmed ranges / 100 bars**, tie → toward 3.0 then lower; **fitted on the WHOLE tape = in-sample, "no registered lane and no Stage-A stamp may read it"**. | `analytics/rangefinder_census.py:154-160,215-220`; `scripts/tierc10_census.py:126-131, 297-301 (L2), 648-697` | `C.run_scale(tape, scale)`, `C.calibrate(tape) -> (pick, grid_df)`, `C.pick_scale`, `C.density_per_100` |
| **confirmed range** | POTENTIAL is SEEDED when the first counter-pivot P1 confirms after an expansion-terminal pivot P0 ({top,bottom} = body extremes in BODY mode); a body close beyond either boundary before confirmation INVALIDATES it; **CONFIRMED on the first traverse touching the boundary OPPOSITE the seed's final pivot within `TOUCH_EPS` (0.60)×ATR**; ONE ACTIVE RANGE (other candidates SUPERSEDED, seeding pauses until it dies). As-of: `in_range` iff `confirm_i ≤ t < die_i`, never from `seed_i`/`backdated_from`. Pins: LEG_MIN 0.5 · REV_MIN 1.75 · TOUCH_EPS 0.60 · DEV_RETURN_BARS 7 · BREAK_CONFIRM_N 8 · BREAK_MARGIN 1.5 · BOUNDARY_MODE body · REDRAW_BASIS wick · ATR_LEN 14. | `engine/rangefinder.py:73-127` (PINNED READINGS), `:136-169` (PINS), `:353-377` (seed/confirm) | `RFC.PINS` `analytics/rangefinder_census.py:136`; `C.asof_view(tape, macro, leash)` `scripts/tierc10_census.py:745` |
| **deviation** | A breach (body close beyond a CONFIRMED boundary, `breach-open`) that closes back inside within **`DEV_RETURN_BARS` = 7** bars → `harden` (deviation-confirm) + `redraw` of that boundary to the episode's WICK extreme; `n_deviations += 1`. Back inside AFTER 7 bars → `breach-lapse` (no redraw). Inception deviation = wick beyond the seed body (spring-birth zone). As-of `dev_top/dev_bot` = hardens so far per side. | `engine/rangefinder.py:395-455`; `:85-106` | census classes `breach`, `harden`; `tierc10_lanes.harden_episodes(events)` `scripts/tierc10_lanes.py:1155` |
| **spring / upthrust / SFP** | Estate objects: (i) **P-SPR-2 / TC11 "swing-failure" = deviation-confirm (harden) at a CONFIRMED macro boundary**: bottom-side harden → LONG spring; top-side harden → SHORT upthrust; `swept_level` = the as-of boundary AT THE BREACH BAR; `sweep_extreme` = raw extreme over [breach, harden]; known at the harden bar. (ii) Legacy TC5 spring lane (P-SPR-1): sweep of the prior 96-bar extreme, reclaim close within 3 bars, tide agrees. "SFP" has no estate detector under that name (`scripts/mc2_program.py:1525`: "the SFP / deviation object … has NO detector"); TC10/TC11 SFP ≡ harden. | `scripts/tierc10_lanes.py:1195-1228` (springs_from_episodes), `:1231` (spring_signals_from_census); `scripts/tierc5_rules.py:401-480` | `tierc10_lanes.spring_signals_from_census(sym, lens="4h", scale_mult=3.0)` → `SpringSignal(direction, sweep_i, sweep_extreme, swept_level, reclaim_i, known_at, rid, side)` then `signals_on_frame(f, springs)` (re-index by ms, never position) |
| **'full water'** | TC5's term: corridor = each asset's **first bar (plus the 316-bar warm-up floor) → the latest CLOSED 4h bar**, one unbroken window, **seal/lockbox OPEN** (no mask). In TC11 header: "full water, latest closed 4h bar, one as-of pin". | `exchange/reports/BUILD_2026-08-16_TIERC5_FULLWATER.md:1-30,78`; `scripts/tierc5_rules.py:239-242` | `TP.corridor_n(panel, pin_close_iso=None, floor_bars=316) -> (lo_ms, hi_ms, meta)` `scripts/tierc10_panel.py:650` |
| **eras tuning / holdout** | **Tuning ≤ 2024-06-30T23:59:59Z** (= `1719791999000` ms, the old "exploration-classic" era); **holdout ≥ 2024-07-01T00:00:00Z**. Panel/lanes: era by **ENTRY instant** (`TP.era_window`: tuning `(None, 1719791999000]`, holdout `[1719792000000, None)`). Census: era by the **ANCHOR bar's close** (`C.era_of`). ALL is filed beside, never the sum. Holdout is out of sample in TIME only (same assets tuned the pins). | `research_outputs/tierc10/OPERATOR_RULINGS.md:43-48`; `scripts/tierc10_panel.py:746-781`; `scripts/tierc10_census.py:159-179` | `TP.era_window(era)`, `TP.in_era(ms, era)`, `TP.corridor_era(panel, era)`; `C.era_of(known_close_ms)`, `C.era_cut(tape)` |
| **Tier-E** | "EXPLORE — anything, everything … stamped `EXPLORATION — ungated; promotion requires registration`. No probe result may be cited as a finding; every probe is logged (question · tables · selection surface m)." In TC10 usage: "report-only: the row gates nothing and holds no slot in m". Counterpart TIER-P = registration + prior + unseen data + FDR family. | `exchange/reports/CENSUS2A_CLOSEOUT_2026-08-12.md:135-148`; `research_outputs/tierc10/BUILD_DRAFT.md:26-27` | collar constants e.g. `C.TIER`, `C.GATES`, `C.COLLAR_COLUMNS` `scripts/tierc10_census.py:351-362` |
| **'a SELECTION, not a result'** | Mandatory label on every non-registered table (every cut of an in-sample book, every grid cell, every census row). Origin: TC6V `query_filter` v1; TC9 separation table. | `scripts/tc6v_riders.py:438`; `scripts/tierc9.py:948-951` (SELECTION_NOTE); TC11:8 | reuse the column set `tier, selection_not_a_result, m_selections_this_table, …` (e.g. `scripts/tierc10_close_p_age_1_tide_youth.py:149-157`) |
| **cluster-90%** | Asset-cluster bootstrap: resample ASSETS with replacement (same draw into both books for two-sample), statistic = MEAN, `n_boot = 4000`; interval = **5th–95th percentile** of draws; `p_one_sided = (#draws ≤ 0 + 1)/(B + 1)` (floor 1/4001 = 0.00025). TC10 ruler seed 20260921, sensitivity seed 20260816. With 5 clusters intervals are coarse. | `scripts/tierc5.py:1178-1236`; `scripts/tierc10_panel.py:91-94` | `T5.cluster_boot(values, clusters, seed, n_boot)`, `T5.cluster_boot_diff(va,ca,vb,cb,seed,n_boot)`, `T5._ci_from(draws, point) -> {point, lo, hi, p_one_sided}`; TC10 scorer `TP.score(...)` `:2173`, `TP.loao_n` `:1234`, `TP.above_half_bar(n)` `:1193` |
| **the prior scalper (P-SCALP-1?)** | **There is no P-SCALP-1 anywhere in the estate.** The predecessor is **P-BRK-S1 "THE SCALPER FORM, 5m"** (TC10, prior 30%): 5m macro DIE (SCALE 3.0) → first HOLD retest of the tuned band (ribbon127_200, margin 1.0, hold 3, ttl 400) in the DIE direction → enter at the hold close; stop beyond retest extreme railed 1.0 ATR(5m); **4h tide aligned** (last closed 4h bar); v6 management lens-scaled; CLASSIC5, **holdout era only**. Result: n 1836, **−0.211069 R [−0.298366, −0.144105]**, LOAO 0/5 above (5/5 below), p 1.0 — NOT SUPPORTED, CI wholly below zero; its own 5m Q-R3 gate: height PASS, edge-fade FAIL (−0.5885 ATR H20). | `research_outputs/tierc10/REGISTRATION_TEXTS.json["P-BRK-S1"]`; `research_outputs/tierc10/BUILD_DRAFT.md:69,215-238` | `BRK.run_lane_s1(panel, reg_id, text, arm, signals)` `scripts/tierc10_brk.py:2201` |
| **the forward ledger** | Charter: forward paper journals are "**Virgin, renewable. Honest exactly once per frozen ruleset; each window is a one-shot exam.**" TC10's precursor = the FORWARD STRIP: v6 book from TC9's as-of 2026-08-22T00:00Z to 2026-09-21T16:00Z, REPORT-ONLY, never scored: 4 campaigns (3 closed, 1 open marked to pin). TC11 wants a STANDING ledger: frozen base + frozen 9/12 re-ridden on NEW bars only at every refresh, unscored until n ≥ 30, side by side. | `Naiad_Phase0_Charter.md:127`; `research_outputs/tierc10/close/FORWARD_STRIP.md`; `scripts/tierc10_close_forward_strip.py`; TC11:58-59 | forward-strip builder (counts & sums only; membership ENTERED-IN-STRIP / CONTINUATION / OPEN-AT-CORRIDOR-END) |
| **the five home assets** | **CLASSIC5 = BTCUSDT, ETHUSDT, SOLUSDT, NEARUSDT, ZECUSDT** (the card's UNIVERSE since TC2). The "twelve new assets" (Tier-E): ENA, PUMP (PUMPFUN→PUMPUSDT, lean), HYPE, MNT (Bybit, lean), SUI, LTC, XMR, BNB, UNI, 1000PEPE, DOGE, 1000BONK — only PUMP, MNT, SUI never-touched; nine display-only. | `scripts/tierc2_rules.py:56-60`; `scripts/tierc10_panel.py:197`; `research_outputs/tierc10/data/DATA_SPEND_AUDIT.json` | `TP.CLASSIC5`; `TP.panel17()`; `TP.resolve_unseen12()` |
| **'the 12/26-triggered base'** | Card v6 on CLASSIC5 = `TP.CONTROL_CARD` (`tierc8.Card(name="v6-control")`) with `T9.V6_ROLES` (tide 89/316, window 12/89, trigger 12/26). The only book `run_cell_n` rides without a registration. TC10 filed journal: 200 campaigns, net +41.7433 R, exp +0.2087, win 34.5%, exits stop 195 / bell_12_89 4 / corridor_end 1; v6 vs zero NOT SUPPORTED ([−0.012774, +0.463319], p 0.0545). | `scripts/tierc10_panel.py:816,882,1083`; `research_outputs/tierc10/panel/control_journal.parquet`; `research_outputs/tierc10/BUILD_DRAFT.md:293-295` | `TP.run_cell_n(TP.CONTROL_CARD, T9.V6_ROLES, TP.CLASSIC5, lo, hi)` |
| **'v6 management'** | Everything after the entry, card v6: struct stop (4h (5,5) pivot ≤200 bars, 0.5 ATR beyond, rail 1.0 ATR) · hands-off to +1R · trail arms after +1R ((2,2), 0.5 ATR, rail 1.0, min adv 0.05, next bar) · harvest 50% at first armed touch of opposing 89/316 band, close fill, once · bells: counter 12/89 or 89/316 against → exit at close · within-bar order **STOP → BELL → HARVEST → TRAIL**, adverse-first, stop fills AT the stop · fee 5 bps/side (10 RT) + journaled funding, funding COST capped at 1R (D12) · one unit, one position per asset. Full field list: `P-TRG-2.json book_spec.arms[0].ride.card_fields` (e.g. `trail_arm_after_r 1.0, trail_min_advance_atr 0.05, harvest_frac 0.5, entry_rail_atr 1.0, funding_ceiling_r 1.0, adds_max 0, wall_exit_atr None`). "Lens-scaled" = the same ride transcribed onto another lens's own arrays. | `scripts/tierc6_rules.py:1-60,121-221`; `scripts/tierc5_rules.py:238-259`; `scripts/tierc9.py:236-331` | 4h: `T9._ride_leg9(sym, card, roles, d, ti, entry_px, stop0, r_dist, hi_i)`; any lens: `BRK.ride_leg_l(lf, card, roles, d, ti, entry_px, stop0, r_dist, hi_i, ribbon=None)` (with `ribbon=None` on 4h it equals v6 exactly — F-BRK-RIDE-4H) |
| **bell** | Exit at close on the counter 12/89 cross (`bell_12_89`) or the 89/316 crossing against (`bell_89_316`); backstop only (4 of 200 v6 exits). | `scripts/tierc6_rules.py:156-170`; `SSV12_LOGIC:61-65` | `scripts/tierc9.py:303-311` |
| **funnel stamps (%-of-range, distance, ages, flips)** | As-of arrays, element t = what is knowable at the CLOSE of bar t: `pct = 100·(c−bot)/(top−bot)` UNCLAMPED; `dist_atr = min(|top−c|,|c−bot|)/atr` (**unsigned**), `near_side` (tie→top); `bnd_age` = bars since the nearest boundary VALUE last changed (confirm or same-side harden redraw), 0 on change bar (LEAN C-a); `range_age = t − confirm_i`; `dev_top/dev_bot`; `flip_pol/flip_age` (flip known at touch+6). No live range → geometry NaN/−1 (never the last corpse's box, LEAN C-b). Macro state four-valued in stamps: NONE / NEUTRAL (range alive) / BULL_EXP / BEAR_EXP (LEAN S-a). | `scripts/tierc10_census.py:745-790, 894-935, 310-317`; `scripts/tierc10_stamps.py:278-288` | `C.asof_view`, `C.asof_index(tape, instant_ms)` (last bar whose CLOSE ≤ instant), `C.stamps_at(tape, view, idx)` |
| **R0 / resume law, one corridor, draft off-bus, PROGRESS.json** | TC10 LAWS 1–6: a stage is COMPLETE only if artifacts exist, manifest shas verify and fixtures RE-PASS now; partials quarantined to `_partial_<ts>/`; registration texts frozen; ONE corridor (the Stage-D as-of governs); build doc drafts under `research_outputs/tierN/` and moves to `exchange/reports/` only at CLOSE; after every stage commit + PROGRESS.json `{stage,status,artifact shas,as_of,fixtures}`. R7 ruling: LAW 4 governs the corridor pin only. | `exchange/queue/2026-09-22_TC10_RESUME_APOLLO.md:11-24`; `research_outputs/tierc10/OPERATOR_RULINGS.md:87-99` | `scripts/tierc10_resume_fixtures.py`; `research_outputs/tierc10/PROGRESS.json` |
| **null (gaps+order)** | Random-boundary box sets at matched coverage; `gaps+order` is the null of record (R10: own-window overlap 25.62%→7.97%); `gaps-only` printed beside; K = 20 draws → percentile is a DESCRIPTION, never a p-value. | `research_outputs/tierc10/OPERATOR_RULINGS.md:125-137` | `scripts/tierc10_null.py`; outputs `research_outputs/tierc10/null/gaps_order/` |
| **toll** | Round-trip toll bps per asset read from Stage D `research_outputs/tierc10/data/fee_schedule.json` `round_trip_bps_used` (10.0 = flat 5 bps/side TAKER "ASSUMPTION", not a venue schedule), else `FEE_BPS_ROUND_TRIP`; in ATR: `(bps/1e4)·close/atr`. Haircut twin (charter tiers A 2 / B 5 / C 10 bps slippage per side) is an ADDED column. **No maker rate exists anywhere.** | `scripts/tierc10_census.py:510-526`; `scripts/tierc2_rules.py:87-91,123` | `C.toll_bps_for(stem)`, `C.get_toll(asset, lens, cls, scale_kind, horizon, era)` (reads the filed grid only) |

---

## 2 · HISTORY OF TC11'S NINE REGISTRATION NAMES (and predecessors)

| TC11 name | predecessors on disk | prior results |
|---|---|---|
| **P-WARN-1** [40%, conditional] | None by name. Nearest: **P-AE-1 / P-AE-2** (early invalidation pre-+1R: exit when MAE reaches 0.60R, `AE_ABORT_R` `scripts/tierc7_rules.py:204`); **P-BE-1** (breakeven floor at first tape +1R, SEQUENCED via 5m children); bell backstop; TC7 weave. Conditional-registration precedent: **P-LAG-1** (TC6V) — registered only IF a cluster-90% interval excluded zero; it did not → report-only, m unchanged. | P-AE-1 paired +0.0044 [−0.0979, +0.1017] p 0.4379 NOT SUPPORTED (`LEDGER_APOLLO.md:2052`); P-AE-2 byte-identical book, same numbers (`:2140-2151`). P-BE-1 **HALT, no verdict** (key set did not move: 200 paired, 0/0 book/base-only) (`BUILD_DRAFT.md:149-166`). |
| **P-AGE-1** [50%] | **Same id used in TC10 as a Tier-E TABLE, never a registration** (`TC10_RESUME:113`; not in `research_outputs/tierc10/registrations/REGISTRY.jsonl`). Lineage: TC7 L-REGIME lab (`tierc7_lab_regime.py`), TC7-g trailing re-emission in TC9 (`tierc9.py:1290-1400`), TC9 separation row. | TC10 P-AGE-1 (trailing edges, v6 book): B1 YOUNG n 25 exp +0.4708; B2 EARLY n 60 +0.5773; B3 MATURE n 56 +0.3213; **B4 OLD n 59 −0.3840** (Tier-E, in-sample; `P_AGE_1_TIDE_YOUTH.md:23-27`). TC9 separation: top-10 winners' tide age median 8 vs 100.5 (def. B, SELECTION). |
| **P-WIN-1** [50%] | **P-LAG-1** (TC6V, report-only) and L-LAG (`scripts/tc6v_riders.py:273-360`); TC9 `a1_win_rate` lag rows. | L-LAG: lag-0 (n 49) exp +0.4149, 56-bar decile −0.5796; P-LAG-1 condition: bottom (≤0 bars) +0.4149 vs top (≥71 bars, n 20) +0.3534, Δ +0.0616 [−0.7819, +0.9566] → NOT MET (`BUILD_2026-08-17_TC6V_TIERC7.md:103,111-113`). |
| **P-BRK-4H** [45%] | **P-BRK-S1** (5m) and **P-BRK-I1** (1d investor: 1d DIE → first memory-line flip-hold; permission weekly 12>25 AND daily 12/25 lifecycle) in TC10; census 4h retest-hold rows. | P-BRK-I1: n 7, +0.718270 [−0.602912, +1.452261], LOAO 1/5, NOT SUPPORTED (one ETH trade > whole net). P-BRK-S1: −0.211069 (above). Census (Tier-E, SELECTION over 7,920 cells): 4h `retest-hold-tap89` CLASSIC5 NET H20 **+0.681** (ALL, n 236) / **+0.706** (holdout, n 88), UNSEEN12 +0.064 / +0.272; under null of record "percentile 100" per R10 (`OPERATOR_RULINGS.md:135-136`), pct 85/75 in the build tables (`BUILD_DRAFT.md:824,856`). 4h `flip-hold` (memory-line) CLASSIC5 +0.159 ALL / +0.904 holdout. |
| **P-RELAY-1** [45%] | **P-REL-1** (CENSUS-2A, WITHDRAWN — control arm had no anchor) → **P-REL-1b** "in-window 12/25 presence" **SUPPORTED-PROVISIONAL** (+0.19 ATR H100; panel return correlation 0.6458, "five witnesses ≈ one") — the origin of the 12/26 trigger. TC-5 intraday re-spec (1H/5m): mechanical fractal confirmed, economic claim falsified, entries floored at 5m. | `exchange/reports/CENSUS2A_CLOSEOUT_2026-08-12.md:39,44,93`; `LEDGER.md:25`; `SSV12_LOGIC:26-30,78-80`. |
| **P-SCALP-2** [30%] | No P-SCALP-1 exists. Predecessor = **P-BRK-S1 "THE SCALPER FORM"** (TC10). SS synthesis "S1 · Scalper (5m-1H stack)". | P-BRK-S1 significantly negative on holdout (above). 5m Q-R3: FAIL on every pool and every era (edge-fade NET −0.25 to −0.59 ATR); **1h has never been measured**. |
| **P-ADD-BRK / P-ADD-SFP** [40% each] | **P-CASC-1** (TC5 adds: ≤2 adds, retrace into MH-or-heavier band then close back beyond FAST ribbon; `ADDS_MAX=2` `scripts/tierc5_rules.py:182`); **P-CIRC** (97.59% of +1R winning moments had no pullback-reclaim PRIME available); charter §3.4 adds at 0.5R each. SSv12: "adds … back to the lab". | P-CASC-1 card+adds PAIRED n 195 Δ +0.1461 [−0.0100, +0.2532] NOT SUPPORTED, LOAO 1/5 (ZEC) (`LEDGER_APOLLO.md:1627`). |
| **P-TP-RNG** [35%] | **P-WALL-1** (TC6, remainder exit ≤0.25 ATR of profit-side 12h champion wall); TC5 shadow S-HARV fixed +2R; H1 (in-profit harvest fires 0× in six years). | P-WALL-1 paired −0.0331 [−0.0859, +0.0278] p 0.8318, tail_exit_ratio 0.9329 — "loses BY CUTTING THE TAIL". |

Registry facts: TC10 family m = 6 (bar 0.016667), `REGISTRY.jsonl` hash-chained (`TP.register`, `TP.require_registered`, `TP.require_arm`, `TP.external_book`, `TP.score`, `TP.finish_family`). TC11: m = 9 → bar **0.10/9 = 0.011111**; TC10 precedent: "m stays DECLARED: fewer tests never loosen the bar" (`BUILD_DRAFT.md:314-320`).

---

## 3 · HOW TO CALL THIS FROM A NEW tierc11 SCRIPT

```bash
export NAIAD_CACHE_DIR=/Users/luis/.cache/naiad/snapshots/tc10_20260921 PYTHONDONTWRITEBYTECODE=1
~/venvs/naiad/bin/python scripts/tierc11_<stage>.py      # never write ~/.cache/naiad/data_cache
```

```python
import sys
from pathlib import Path
import numpy as np
ROOT = Path(__file__).resolve().parents[1]
sys.path[:0] = [str(ROOT), str(ROOT / "scripts")]

import tierc10_panel as TP          # corridor, eras, gated books, scorer, registry
import tierc9 as T9                 # Roles, role arrays, v6 ride, tide_streak_age, trailing edges
import tierc7_lab_regime as LR      # tide_streak (definition A), bands
import tierc7_rules as RC           # the card's register objects by identity
import tierc5 as T5                 # d15, cluster_boot(_diff), _ci_from
# range machinery: import tierc10_census / tierc10_brk INSIDE runner functions only
# (F-BR-14 / F-LANES-CLOSURE: no decision closure may reach a range module)

lo, hi, meta = TP.corridor_n(TP.CLASSIC5)                       # Stage-D pin (TC10: 2026-09-21T16:00Z)
base = TP.run_cell_n(TP.CONTROL_CARD, T9.V6_ROLES, TP.CLASSIC5, lo, hi)   # v6 = the 12/26 base
trg912 = T9.Roles(name="trigger-9/12", trg_f=9, trg_s=12)       # needs a filed registration to ride

pool = T9._trailing_pool(lo, hi)
for t in base:                                                  # t: tierc5_rules.Trade
    f = T9.frame(t.symbol)["f"]                                 # 4h Frame (e12,e26,e89,e316,atr,…)
    lag = t.entry_i - t.arm_i                                   # arm→trigger lag, 4h bars
    r_over_atr = t.r_dist / t.atr_at_entry                      # structure distance
    _, run, _ = LR.tide_streak(t.symbol)                        # tide age, definition A (TC10 P-AGE-1)
    age_A = int(run[t.entry_i])
    edges = T9._trailing_edges(pool, int(f.open_ms[t.entry_i]))["streak_bar"]   # None while < 30 bars
    band = LR._streak_band(age_A, edges) if edges else None     # 3 == "B4 OLD tide"
    old_trailing = band == 3
    old_absolute = age_A > 206                                  # shadow (see ambiguity A1)
    age_B, cens = T9.tide_streak_age(t, T9.V6_ROLES)            # definition B (TC9, at ARM bar)

d15 = T5.d15(cell_book, base)                                   # tail_exit_ratio etc.
draws = T5.cluster_boot_diff([x.net_r for x in cohort], [x.symbol for x in cohort],
                             [x.net_r for x in rest], [x.symbol for x in rest], seed=TP.SEED, n_boot=4000)
ci = T5._ci_from(draws, point)                                  # {point, lo, hi, p_one_sided}

def range_view(sym, lens="4h"):                                 # runner-side only
    import tierc10_census as C
    tape = C.load_tape(sym, lens)                               # lens ∈ ("5m","4h","1d") today
    m = C.run_scale(tape, C.FROZEN_SCALE)                       # 3.0; calibrated scale = in-sample
    view = C.asof_view(tape, m["macro"], m["leash"])
    return tape, m, view                                        # C.stamps_at(tape, view, C.asof_index(tape, ms))
```

---

## 4 · GAPS — what the existing code CANNOT do today

1. **Lenses.** `C.LENSES = ("5m","4h","1d")` (`scripts/tierc10_census.py:117`) and `load_tape` HALTs on anything else; TC11 R1 needs {5m, **15m, 1h**, 4h, **12h**, 1d, **1w**}. `BRK.frame_l` covers 5m/1h/4h/12h/1d/1w via Stage D `D.STEP_MS` (`scripts/tierc10_data.py:202-206`) but **no 15m** (not a Stage-D lens; 15m parquet exists in the snapshot for the five home assets only, unmanifested). 1d/1w are derived from native 4h (LEAN L1).
2. **Stamps exist only for rf4h** (`control_v6_stamped.parquet` columns `rf4h_*`). R3's nesting vector (1h/4h/12h/1d/1w), R5's 12h state, and "signed distance" all need new builds. Existing `dist_atr` is UNSIGNED.
3. **BOUNDARY COINCIDENCE** (L boundary within 0.25 ATR_L of an L+1 boundary) — no code.
4. **Mixed-lens entries.** The v6 ride starts on the bar after a 4h entry (`_ride_leg9` loops `j in (ti+1 …)`). The relay (entry at a 1h close inside a forming 4h bar) and the scalper (5m entry, 1h target) need a ride that handles the partial 4h bar or a 5m/1h path. `BRK.ride_leg_l` is single-lens.
5. **Sub-bar ordering "before/after +1R"** vs 1h events: the +1R latch is intrabar on 4h. Only 5m-children sequencing exists (`tierc10_lanes.be_sequence`, `children_5m` `scripts/tierc10_lanes.py:262-296`).
6. **Adds**: no add machinery on the v6 path (`adds_max 0`); TC5's P-CASC-1 add (`scripts/tierc5_rules.py:517-565`, `V5.Add`, `add_context`) is a different trigger. Add P&L accounting in campaign-R exists only in TC5 (`scripts/tierc5.py:407`).
7. **Take-profit orders / range-target exits**: none. The nearest is P-WALL-1's close-based wall exit (`V6.wall_touch`).
8. **Maker toll**: no maker rate anywhere; `fee_schedule.json` is a flat 5 bps/side taker assumption.
9. **Standing forward ledger**: only a one-shot report-only strip exists; there is no refresh mechanism, and the frozen substrate ends 2026-09-21T16:00Z. "Latest closed 4h bar" implies a NEW frozen snapshot (the live cache is another lane's).
10. **Per-lens feasibility at 1h/12h/1w and at 15m** was never computed (TC10 gate rows: 5m/4h/1d only).
11. **Calibrated scale is whole-tape**; a causal (trailing) scale calibration does not exist, which matters for F-NEST-ASOF if nest reads the calibrated scale.
12. **F-GATE "removes only the trades its text names"** conflicts with the one-position-per-asset rule (`scripts/tierc9.py:391`): refusing a trigger can FREE a later trigger that v6 rejected as `position_open`, so a gated book can hold campaigns v6 never had.

## 5 · AMBIGUITIES — contract phrases that do not bind to one reading

- **A1 · "refuse age > 206 bars"** (TC11:46). 206 is the whole-corridor MEDIAN edge (B2/B3) of definition A, not the OLD-band edge (396). As a shadow for "refuse the OLD band" it refuses ~120/200 vs 59/200. It is also look-ahead (whole-corridor quantile); TC10's own F-AGE fixture uses exactly this substitution as its RED plant.
- **A2 · "tide-age"** (TC11:45). Definition A (two-state sign(e89−e316), at ENTRY, TC10 of record) vs definition B (three-state with price, at ARM, TC9). F-DEF's "both definitions" means trailing vs absolute; the A/B split is a third axis the contract does not name.
- **A3 · "window open+armed"** (TC11:55). The card closes a window only on the counter 12/89; posture_engine also closes it on 89/316 against. Whether 1h bars inside the ARM 4h bar count (the window is only known at that bar's close), and whether the relay may still enter after the 4h 12/26 trigger fires, are not stated. "ENTRY on the 1h 9/12 … close" does not say FIRST cross only.
- **A4 · relay evidence vs relay rule.** W2's pre-entry evidence measures a **1h 12/26** with-trend cross (TC11:38-39). P-RELAY-1 trades a **1h 9/12** (TC11:55).
- **A5 · "stop = 4h pivot railed, R on 4h"** for a 1h entry. The pivot eligibility index and the ATR bar are not stated. The natural reading is the last CLOSED 4h bar (`visible_htf_idx` law).
- **A6 · P-BRK-4H hold pins.** "first HOLD retest on the 89 tap" (TC11:52) does not name margin/hold/ttl. Of record for 4h census rows are the **5m-tuned** 1.0/3/400. Alternatives are the flip-hold 1.0/6 and the 1d-tuned 0.25/6. The 89 tap was picked after TC10's 4h census (a SELECTION over 7,920 cells, in sample on CLASSIC5), which is the same hazard as P-TRG-2.
- **A7 · SCALE for registered lanes.** R1 says the macro scale is "self-calibrated per asset per lens". TC10 LEAN L2 forbids any registered lane or stamp from reading the calibrated scale because it is in-sample. The contract does not say which scale P-BRK-4H, P-SCALP-2, P-ADD-* and P-TP-RNG ride.
- **A8 · P-WARN-1 "below the base"** (TC11:41). Base could be the whole v6 book (which contains the cohort) or the complement. The L-LAG precedent compared disjoint groups. m stays 9 if report-only (the TC10 precedent), but the contract says "no slot spent".
- **A9 · P-ADD-SFP "trend-side boundary … (the deviation back into range in the trade's favour)"** (TC11:77-78). For a long, the trend side is the TOP, but a favourable deviation back into range is a BOTTOM spring. The two halves point at opposite boundaries.
- **A10 · P-ADD-BRK "a range ONE LENS BELOW … DIES in the trade's direction"**. Which 1h ranges count (any alive after +1R, or one confirmed before it) is not said, and neither is the add's fill (the DIE bar's close?) or its R denominator (campaign R?).
- **A11 · P-TP-RNG "approach (0.25 ATR)"** (TC11:83). The ATR could be the 4h or the 12h. The fill could be a close-based exit (as P-WALL-1) or an intrabar limit. "the remainder" could mean after the harvest, or the whole unit if there was no harvest. "When that lens is IN-RANGE" does not say whether that is judged at entry or at the approach bar.
- **A12 · Scalper details.** "invalidation also on a 1h boundary breach": a breach-open close, or the DIE? "the 5m structural pivot": (5,5) with a 200-bar lookback as in the 4h stop, i.e. 16.7h on 5m? "lower third": `pct ≤ 33.33` of the as-of (redrawn) range. R2's edge-fade leg uses the near 20% (`EDGE_NEAR_FRAC`), not a third.
- **A13 · R4 "L+1 in-range mid"** is undefined: no %-of-range band is given.
- **A14 · R5 "%-of-range decile"**. `pct` is unclamped: inside a live range during a pending breach it runs below 0 or above 100, so the decile edges must say how those rows are handled.
- **A15 · "Corridor: … latest closed 4h bar, one as-of pin"** vs F-CTRL "cross-process-anchored". TC11's corridor extends past TC10's pin. Precedent (TC10 vs TC9): campaigns entered after the referee's last bar are reported as continuations or live-only, not as diffs.
- **A16 · Name reuse.** P-AGE-1 (a TC10 table id) becomes a registration. P-SCALP-2 implies a P-SCALP-1 that never existed.
