{
  "version": 5,
  "_correction_2026-08-15": "QUEUE 005 M4. FIX (live instruction, not a dated report). This key was the bare name 'python'. PROVENANCE, checked with git log -L rather than assumed: the bare name is NOT a Windows-era leftover -- it was introduced THREE COMMITS AGO by f403546 'M3 platform sweep', which replaced the working Windows value C:\\venvs\\naiad\\Scripts\\python.exe with 'python'. So this is a regression from the macOS port itself, not something the port failed to reach. On this machine bare 'python' does not resolve AT ALL: not under launchd's minimal PATH (/usr/bin:/bin:/usr/sbin:/sbin) and not in the operator's interactive shell either -- only python3 exists. daily_routine.py builds argv = [python, script] at line 232 and has no --python override, so every child job would have returned exit=126 'could not launch: [Errno 2] No such file or directory'. The M3 sweep left the daily routine armed-looking and totally inert. Now an ABSOLUTE path to the venv interpreter, which is what the three launchd agents also use: launchd supplies no shell rc and performs no venv activation, so nothing else can put the right python on the path.",
  "python": "/Users/luis/venvs/naiad/bin/python",
  "slots": ["london", "ny_am", "post_ny"],
  "output_dir": "exchange/status/daily",
  "publish": true,
  "daily_archive_dir": "research_outputs/_daily_archive",
  "keep_daily": 7,
  "backup_dest": "/Volumes/LaCie/naiad-backups",
  "reminders": {
    "estate_max_days": 8,
    "workflow_max_days": 8,
    "operator_export_max_days": 35,
    "keep_estate": 4,
    "keep_phase_sets": 1,
    "keep_workflow": 4
  },
  "jobs": [
    { "id": "manifest", "script": "scripts/reviewer_manifest.py", "required": true,
      "stage": [{ "from": "exchange/status/MANIFEST.json", "to": "MANIFEST_{date}.json" }] },
    { "id": "brief", "script": "scripts/daily_brief.py", "required": false,
      "retired": "2026-08-05",
      "retired_because": "ruling D-3: v1.1 retires once a BRIEF-2 render exists. It does. daily_brief.py is NOT deleted -- it is still imported by brief_capture.py for the Part I layers, and its published numbers are the bridge to every pre-2026-08 archive. It is no longer SCHEDULED.",
      "superseded_by": ["brief2_capture", "brief2_panel"],
      "scheduled": false,
      "stage": [],
      "reference": ["research_outputs/brief/brief_{date}.html",
                    "research_outputs/brief/brief_{date}.json"] },
    { "id": "brief2_capture", "script": "scripts/brief_capture.py",
      "required": false, "slot_aware": true,
      "stage": [],
      "reference": ["briefs/brief_{date}_{slot}.json"] },
    { "id": "brief2_panel", "script": "scripts/brief_panel.py",
      "required": false,
      "stage": [],
      "reference": ["briefs/panel/snapshots/{date}.parquet",
                    "briefs/panel/levels/{date}.parquet",
                    "briefs/panel/areas/{date}.parquet"] }
  ]
}
