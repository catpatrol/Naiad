# Pointer — CAPTURE_2026-08-03_post_ny.json

This machine artifact was removed from `exchange/` on 2026-08-04 under the ratified content
guard (CONVENTIONS.md §4.2: text only, 1 MB per file; larger artifacts are referenced by
path + sha256 pointer, never copied in). At 1,879,133 B it was 29.4% of the ~6.39 MB box on
its own — more than double the combined weight of every prose document in `exchange/`.

**It was not deleted.** A byte-identical copy is TRACKED and PUSHED at the path below, and the
removed copy also remains in git history.

| field | value |
|---|---|
| original path | `exchange/reports/CAPTURE_2026-08-03_post_ny.json` |
| lives at | `briefs/brief_2026-08-03_post_ny.json` |
| that copy is | **tracked, in HEAD, and present on `origin/v12-v1-census`** (blob `d7c24f67`) |
| sha256 | `c60eac9a330ef9d51f2b41621a9f99caf4fe62f6f3cdbd9490cbfb15b0619bc2` |
| bytes | 1,879,133 |
| box cost when synced | 29.4% |

To read it, open `briefs/brief_2026-08-03_post_ny.json` in the local repo or on GitHub. It is not on the sync bus by
design.
