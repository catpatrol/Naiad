# FIXUP REPORT — 2026-07-28

Environment: local Windows Claude Code, working Naiad clone.
This report supersedes the earlier same-day version and covers the full fixup: test-data restore, stale-reference repointing, Google Drive location, and the commit.

**One commit was created, as instructed. Nothing was pushed.**

---

## STEP 0 — asserts

| assert | expected | actual | result |
|---|---|---|---|
| working directory | naiad repo root | `/c/Users/luisf/OneDrive/Desktop/Midas-Claude Code Resources/naiad` | **PASS** |
| `git rev-parse --short HEAD` | `268cdf9` | `268cdf9` | **PASS** |

Asserted before any work. HEAD advanced later only via the Part D commit.

---

## PART A — restore test data

### A1 — every `research_outputs\` path the test suite reads

| test module | line | path read | state before restore | phase archive |
|---|---:|---|---|---|
| `fixtures/test_f6_parity.py` | 18 | `research_outputs/parity` | present (27 files) | not archived — never moved |
| `fixtures/test_i_input_parity.py` | 29 | `research_outputs/parity/journal/BTCUSDT_swing` | present (10 files) | not archived — never moved |
| `fixtures/test_s2_sim.py` | 69 | `research_outputs/s1/resampled` | **broken** — only `resample_report.json` | **`s1_2026-07-27.zip`** |
| `fixtures/test_f8_journal.py` | 21 | `research_outputs/dryrun/journal` | absent | **no archive exists** |

`fixtures/conftest.py:5` mentions `research_outputs/` in a docstring only; it reads nothing.

**Only one path was actually broken by the archiving.** `test_s2_sim.py::test_c141t213_regression` is the sole test reading the resampled directory, via `run_replay(..., "BTCUSDT_intraday", ...)`. The loaders at `engine/s1.py:164-166` and `engine/s2.py:140-142` read exactly `{cell.symbol}_{tf}.parquet` for `tf in ("30m","1d")` — so the files any test reads are **two**, both BTCUSDT. The archive holds fourteen resampled parquets (seven assets × 30m/1d); the other twelve are read by no test and were deliberately left archived.

**`research_outputs/dryrun/journal` cannot be restored and does not need to be.** No `dryrun_2026-07-27.zip` exists — the six archives are `s1`, `s2`, `s3`, `tc1`, `tc4`, `v3_anchor`. Both tests touching it are guarded by `pytest.mark.skipif(not DRYRUN.exists(), ...)` at `fixtures/test_f8_journal.py:106` and `:110`. That guard is the source of the `1 skipped` in every run, before and after. Nothing is broken.

### A2 — extraction and verification — **PASS**

Extracted from `research_outputs/_archive/s1_2026-07-27.zip` (1,564 entries; archive paths are relative to `research_outputs/s1/`). Each file's sha256 was checked against the pin in the archive's embedded root-level `MANIFEST.json` (`phase: "s1"`, `file_count: 1563`, `created_utc: 2026-07-27T07:00:36+00:00`) **before** being written, then re-hashed from disk. Re-verified again at the start of this run:

| file | size on disk | manifest pin | sha256 | matches pin |
|---|---:|---:|---|---|
| `research_outputs/s1/resampled/BTCUSDT_30m.parquet` | 5,116,414 | 5,116,414 | `1e2af0f03ea13a90…` | **yes** |
| `research_outputs/s1/resampled/BTCUSDT_1d.parquet` | 109,994 | 109,994 | `39898ce8e77fd271…` | **yes** |

**Total bytes restored: 5,226,408 (4.98 MB).**

### A3 — full test suite under `C:\venvs\naiad\Scripts\python.exe` — **PASS**

```
73 passed, 1 skipped, 1 warning in 23.05s
PYTEST_EXIT=0
```

Targeted confirmation of the required test:

```
fixtures/test_s2_sim.py::test_c141t213_regression  ->  1 passed, 1 warning in 8.51s
```

| run | result |
|---|---|
| before restore (old and new venv, identical) | `1 failed, 72 passed, 1 skipped` |
| **after restore** | **`73 passed, 1 skipped`** |

`test_c141t213_regression` passes, as required. The remaining skip is the dryrun-guarded test in A1. The one warning is pre-existing and cosmetic — `PytestUnknownMarkWarning: Unknown pytest.mark.slow` at `fixtures/test_s2_sim.py:60`; the marker is used but never registered in `pytest.ini`. Not touched.

---

## PART B — repoint stale references

### B1 — `scripts/setup_brief_schedule.ps1` — **correct, verified**

Line 43 reads `$python = "C:\venvs\naiad\Scripts\python.exe"`. This was set during the earlier setup task under the operator ruling to repoint the three functional references before deleting `.venv`; it needed no further edit. Verified in place.

Guard check — both preconditions at `:53-54` resolve:

```
Test-Path $python  : PASS -> C:\venvs\naiad\Scripts\python.exe
Test-Path $script  : PASS -> ...\naiad\scripts\daily_brief.py
```

**No schedule registered.** Confirmed by query: no `NaiadDailyBrief` task exists. The script itself was never executed — only its guard conditions were evaluated independently.

### B2 — `.claude/settings.local.json` allowlist — **33 entries rewritten**

| item | value |
|---|---:|
| allow-list entries, before and after | **110** (unchanged) |
| entries containing `.venv` before | **33** |
| **entries rewritten** | **33** |
| entries containing `.venv` after | **0** |
| non-`.venv` entries left byte-identical | **77** |

The 33 split across two escaping conventions, and **each was rewritten in its own convention** — an allowlist entry only suppresses a prompt if it matches the command string exactly:

| form | count | before | after |
|---|---:|---|---|
| Bash entries, forward slash | **32** | `.venv/Scripts/python.exe` | `C:/venvs/naiad/Scripts/python.exe` |
| PowerShell entry (index 106), escaped backslash | **1** | `.\\.venv\\Scripts\\python.exe` | `C:\\venvs\\naiad\\Scripts\\python.exe` |

**Stated deviation.** The instruction specified the backslash form `C:\venvs\naiad\Scripts\python.exe` for all entries. The 32 Bash entries are invoked from a POSIX shell where a backslash is an escape character; writing that form into them would produce 32 permanently non-matching rules and *increase* prompt friction rather than remove it. Each entry therefore keeps the slash convention of the context that invokes it. One line to change if you want it literal regardless.

The file re-parses as valid JSON and its entry count is preserved exactly.

---

## PART C — Google Drive

Drive for Desktop is now installed and running (two `GoogleDriveFS` processes; `C:\Program Files\Google\Drive File Stream` and `C:\Users\luisf\AppData\Local\Google\DriveFS` both present). The earlier report's finding of "no Google Drive on this machine" is superseded — it was correct when written.

### C1 / C2 — the mount

| item | value |
|---|---|
| **exact path** | **`G:\My Drive`** |
| mount type | **streaming mount**, not a mirrored local folder |
| volume label / filesystem | `Google Drive` / FAT32 (the DriveFS virtual volume) |
| **free space** | **13.14 GB free of 15 GB** (1.86 GB used) |
| signed-in account | **not readable** — see below |

**Why streaming rather than mirrored:** the content is reachable only through the virtual `G:` volume, whose sole top-level entry is `My Drive`. Both mirrored-mode locations were checked and are absent — `C:\Users\luisf\My Drive` and `C:\Users\luisf\Google Drive` do not exist. A mirrored setup would place a real folder under the user profile and consume local disk; this one does not.

**Account:** the DriveFS profile directory is `%LOCALAPPDATA%\Google\DriveFS\103318730532797253152`, so the numeric Google account ID is **103318730532797253152**. The account *email* was not recoverable from the two most recent DriveFS log files. Reporting the ID as fact and the email as unknown rather than inferring it from the machine's other signed-in identities.

**Capacity note worth carrying into the backup design:** 13.14 GB free on Drive, against a kline estate previously measured at ~633 MB and phase archives totalling ~1.0 GB in `research_outputs/_archive/`. One full estate snapshot fits comfortably; a rolling history of them does not. Retention needs deciding before `backup_estate.py` is built.

### C3 — target folder — **created**

```
CREATED:  G:\My Drive\naiad-backups
verified present
```

It did not previously exist. **Nothing was copied into it**, as instructed — it is empty.

---

## PART D — commit

### D1 — commit created

```
[v12-v1-census 97f0eee] ops: daily routine + venv repoint to C:\venvs\naiad + restore test fixture data
 6 files changed, 386 insertions(+), 4 deletions(-)
```

| file | change |
|---|---|
| `scripts/daily_routine.py` | new, 349 lines |
| `scripts/routine_jobs.json` | new, 12 lines |
| `claude/STATUS_SYSTEM.md` | new, 18 lines |
| `scripts/setup_brief_schedule.ps1` | modified, 4 lines |
| `.claude/commands/brief.md` | modified, 5 lines |
| `.claude/commands/brief-history.md` | modified, 2 lines |

### D1 — two requested items were NOT committed, both blocked by existing policy

Neither is an oversight; both are ignored deliberately, and force-adding either would be materially harder to undo than to redo.

**1. The Part A restored parquets — blocked by charter §10.**

`git check-ignore` resolves them to `.gitignore:27` (`research_outputs/s1/**`), sitting under the explicit header at `.gitignore:23`:

> `# --- charter §10 hygiene: journals & derived data live on orphan 'data' branch only ---`

and the charter itself, `Naiad_Phase0_Charter.md:163`:

> *"Raw candles are **never committed** — Binance is the durable archive; the agent re-fetches on demand."*

restated as build-prompt invariant 9 (`Naiad_Phase1_Build_Prompt.md:38`): *"Raw candles never committed."*

Committing 5.2 MB of resampled candle parquet to `v12-v1-census` would require `git add -f`, violate a ratified invariant, and permanently enlarge history — and this project's own record notes that rewriting history is not available as a remedy, because the ledger cites commit hashes. The files are restored **on disk**, which is what makes the suite pass; the commit message's "restore test fixture data" describes work that is real and complete, just not carried in git by design.

**2. `.claude/settings.local.json` — blocked by a global ignore rule.**

`git check-ignore` resolves it to `C:\Users\luisf/.config/git/ignore:1` — `**/.claude/settings.local.json` — the user's *global* gitignore, i.e. a machine-wide convention that this file is local-only. It also contains absolute scratchpad paths carrying prior session UUIDs. Committing it would push machine-local permission state into a shared repo.

Both are one `git add -f` away if you want them; say so and I will amend.

### D2 — pre-existing untracked files left alone

The 11 pre-existing untracked files at the repo root were **not staged**, as instructed. Files were staged by explicit path, never by directory, so nothing was swept in incidentally — in particular `scripts/orchestrator_state.py` (which is not on your list) was left untracked.

### D3 — final state

**HEAD:** `97f0eee0f0f7f71fe90e65db65594291303f24e7` (`97f0eee`)

```
$ git status -sb
## v12-v1-census...origin/v12-v1-census [ahead 1]
?? "# ORCHESTRATOR PRIMER \342\200\224 what a fresh orchestrator session needs to know.txt"
?? "12-25EMA Trend Scanner-Pinescript.txt"
?? CONTRACT_ANALYTICS1_BRIEF2_FORWARD0_draft.md
?? HANDOFF_2026-07-27_BRIEF_to_CENSUS_1.md
?? HANDOFF_2026-07-27_BRIEF_to_ENGINE.md
?? "Naiad \342\200\224 Orchestrator Control Center.html"
?? Naiad_Orchestration_and_Open_Questions.md
?? "ORCHESTRATOR CONTROL CENTER \342\200\224 protocol & state of record.txt"
?? "Rvwap pine code.txt"
?? "STATUS \342\200\224 BRIEF (daily market brief \302\267 Atlas HTML report \302\267 live laboratory).txt"
?? "STATUS \342\200\224 ENGINE (engine builds \302\267 repo operations \302\267 integrity & manifest).txt"
?? scripts/orchestrator_state.py
```

`git status --porcelain` is identical to the above minus the branch line. **Ahead 1, not pushed.** No tracked file is modified or staged; every remaining entry is untracked.

```
$ git log --oneline -2
97f0eee ops: daily routine + venv repoint to C:\venvs\naiad + restore test fixture data
268cdf9 docs: estate protection, OneDrive quota root cause, 19.61 GB reclaimed, rulings R-A/R-B/R-C
```

---

## Incidents and observations

1. **A stale `.git/index.lock` blocked the first staging attempt.** Zero bytes, last written 19:09:36, **117.8 minutes old**, with **no git process running** on the machine. Verified stale on all three counts before removal; then removed, and git responded normally. Flagging it because a lock that old with no owner means some earlier git invocation died mid-operation rather than exiting — worth knowing if the daily routine is ever given git privileges it currently does not have.
2. **A third file appeared that I did not create:** `scripts/orchestrator_state.py`, absent from the git status captured at the end of the previous task. This follows `# ORCHESTRATOR PRIMER — …txt` and the same pattern earlier today. Most likely OneDrive sync landing files from another machine or a parallel session. Combined with the orphaned lock above, something else is touching this working tree. The daily routine will report these as "untracked added" with no local cause.
3. **`pytest.mark.slow` is unregistered** (`pytest.ini` has no `markers` section), costing one warning per run and preventing `-m "not slow"` from skipping the ~8-second replay test. A three-line addition fixes both. Out of scope, not done.
4. **The earlier Google Drive finding is now superseded**, and `PENDING item 3` (`backup_estate.py`) is unblocked on the path question: the target is `G:\My Drive\naiad-backups`. It still needs Amendment 1 filed, and a retention decision against the 13.14 GB ceiling.
