# REVIEWER HANDOFF — 2026-07-13 (V3 anchor complete → forensics phase → V4 design)
**From:** reviewer session (V2 closure → V3 anchor run → verification → forensics report 1 → deep-dive scoping)
**To:** next reviewer session, same project. **Authority:** repo `LEDGER.md` on `origin/v12-v1-census`. This doc summarizes; on any conflict, the repo wins.
**Roles:** Claude (Project) = reviewer, Fable-mode. Claude Code = builder (local, Windows/PowerShell). Ludwig = operator, owns all merges/gates, no programming background — every operational instruction step-by-step, plain language, "what this does / what you should see."

## 0. Session-start actions for the new instance (in order)
1. **State the LEDGER** from §2, then verify against repo tail (builder paste: `Print the last 60 lines of LEDGER.md and git log --oneline -12`).
2. **Verify two instructed-but-unconfirmed commits landed** (issued at end of verification, no screenshots received): (a) ledger evidence-spend entry — commit message `v3 anchor: evidence spend recorded + reviewer verification sign-off`, which REQUIRED two corrections first: replace the false draft line "Engine 1.0.3, zero code changes since 38b3f66" with the 1.0.7 (2f260e1) lineage statement, AND fix the same stale template at `scripts/v3_scorer.py:375`; (b) deliverables hygiene commit `v3 anchor: deliverables` (research_outputs/v3_anchor/ JSON+MD tracked; `journal_pass1/` [1.3 GB] and packet zip stay untracked). If absent or uncorrected → issue them before anything else; the false engine line must never reach the ledger (cd91ded precedent).
3. **Receive the builder's ENGINE_LOGIC_EXTRACT.md** (paste already issued, §7; re-issue if not run).
4. **Receive the operator's interview answers** (six questions, verbatim in §8).
5. **Receive the Prometheus agent's report** and process per the intake protocol (§9). Items 3–5 arrive in any order; report 2 waits for 3+4; Prometheus intake is independent.
6. Optional: operator may also want forensics report 1 committed (`V3_STOP_AND_EXIT_FORENSICS.md`, delivered as download) — offer a hygiene add.

## 1. Mission context
Two workstreams. **v12 Study**: pre-registered backtesting program; gates V1(census)✓ → V2(parity)✓ → V3(anchor run)✓ **complete and verified** → now forensics → V4 design concept. **Project Naiad Phase 1.5** (forward collector): parked cleanly mid-Step-5; resumes after CD-2 ruling. Philosophy: observe→hypothesize→test→falsify→iterate; falsification is a deliverable; gate integrity non-negotiable; evidence-spend accounting strict; exploration-classic is chartered "free to mine" (VR-1); lockbox sealed until V8.

## 2. State of record
**Branch `v12-v1-census` commit chain (this session's arc):** 38b3f66 (engine 1.0.3, parity-signed baseline) → cd91ded (parity sign-off; defective V2 claim) → d10b9e6 (ledger correction per VR-4) → 7f1b4a9 (hygiene: manual v2.0, playbook+D6 archived, pine, artifacts, handoffs) → fb4dddd (collector W-2: mkdir journal/state) → 195f284 (egress-probe workflow) → 03cf247 (**V2 CLOSED**: ETH+FARTCOIN 20/20 spot check PASS, incl. regime-tint verification) → b77e16c (V3 contract ratified) → 350b82b (Amendment A1: Q-1 config v12_anchor, Q-2 F4-a annex lockbox warm-up exception, Q-3 sizing language, Q-4 month-aligned starts) → 1d8a596 (configs/v12_anchor.yaml; signal block byte-identical to v11_faithful, trading/shadows = naiad_v0, v_births_provisional:false; sha a3917ea5…) → 22b6771 (**1.0.4** G-3 stop-guarantee one-bar exemption) → 3828980 (**1.0.5** G-4 wake-time campaign-death net, exit_reason `campaign_died`; V-entry-filter variant seed logged) → cd42c5c (**1.0.6** G-5/G-5b same-wake sibling semantics: breakeven assert `fill_i < i`, gate risk projection counts queued siblings; dedupe variant seed logged) → 2f260e1 (**1.0.7** G-6 gate tranche-cap sibling projection; family-closure enumeration in CHANGELOG) → [expected: evidence-spend ledger commit + deliverables commit, §0.2].
**Engine:** 1.0.7 (2f260e1). Every bump passed the pinned-stamp parity gate: Run A (new code, stamp pinned "1.0.3") reproduced signed SHA `4c7343178af89dc14cd25933a3d5ab76c6b21211d090aad578d81e7413ce92de` byte-identical, 5,446 rows, run_id 1869ff707385dc4e — four consecutive times; Run B field-diff = {engine_version, run_id} only. **Queued:** 1.0.8 = G-1 (code-level lockbox guard in replay path — currently only window arithmetic + manifest assertions protect it) + J-1 (REJECT-stage dataclass default). G-2: LIT cache holds 31,680 pre-Lighter Litentry 1m bars (2025-12-01→12-22) — estate remediation is operator/reviewer action (no-deletion doctrine); LIT excluded from anchor.
**Failure-family record:** death-transition family (G-3 false-alarm assert, G-4 silent-re-arm missing exit) closed at 1.0.5; sibling-projection family (G-5 breakeven, G-5b risk, G-6 cap) closed at 1.0.7 by enumeration. Fill-time floors behaved correctly throughout. G-6 relaunch-checkpoint mismatch (SOL_position): one relocated null-valued REJECT row (gap_through_stop@fill-bar → max_tranches@signal-bar), zero trading divergence, four aggregate pairs identical — ruled ACCEPT; baseline hash updated to 13b84c6b….
**Collector (parked):** geo-block diagnosed to certainty — HTTP 451 "restricted location", runner 48.214.53.84 AS8075 Boydton VA; data.binance.vision reachable (200); pure geographic → any non-US egress works. W-2 committed (all-failure ticks now commit green as error-state records; N-1: one such tick a1f27a2 on `data` — harmless, flag at go-live so it's not read as a paper epoch; **no paper epoch stamped anywhere**). Default branch = v12-v1-census (CD-1 done). `data` = orphan branch, single empty root + a1f27a2. **CD-2 pending operator ruling:** VPS ~$4–6/mo (reviewer recommendation; run one-call probe from the specific VPS first) vs Tailscale-via-PC ($0, requires 24/7 PC); operator leaned VPS; CD-3 (cache persistence) dissolves under self-hosted. W-1: Node 20 deprecation — bump actions at next workflow touch. Collector go-live ferry/Phase-2 protocol: never delivered; cron never enabled.

## 3. V3 anchor — verified results (reviewer recomputed everything from raw journals; all 15 checks PASS)
**Run identity:** engine 2f260e1, config a3917ea5…, 47 runs ×2 passes, determinism 47/47 byte-identical; grid evidence hash `261bd6a1c988dc66aeef85a4920e8f805f4adf8e8dda8f578af7509c572deb6d` (= sha256 of per-cell journal SHAs sorted by cell key, concatenated). Lockbox proof: max scored entry open 2024-06-30T22:02Z; 27 annex cells carry F4-a warm-up traversal disclosures; zero scored rows from sealed windows. Packet: `v12_v3_anchor_packet_20260713.zip` (45.5 MB, repo root, untracked).
**Scored grid (exploration-classic, 20 cells, 3,456 campaigns):** net R 0× **+1,345.31** / 1× **−5,756.91** / 2× −12,859.13 — cost stack flips the sign (Σcosts 7,102 ≈ 2.05R/campaign). Expectancy 1×: **−1.6658R** [95% CI −2.0407, −1.3597, campaign bootstrap]. Win rate 11.28% (390 winners). All 20 cells negative. Mandates: position −0.85 ≥ swing −1.04 ≥ intraday −1.92 (intraday: 2,500 campaigns, −4,801 of the loss). Assets all negative (BTC −1,154 … ZEC −975). Grades: A+ −1.03 (39) > A −1.50 (1,153) > B −1.61 (1,954), C=0 structurally (never trades); 310 ungraded = 45 V-initiated + 265 no-filled-R1; 21 dual-entry campaigns; scorer rule = grade of earliest entry fill. Taxonomy (per-tranche, 8,387): PROTECTED 3,381 · STILLBORN 2,076 · NEVER_GREEN 1,830 · FADED 1,100. Best campaign: BTCUSDT_swing c197, 2023-08-16 (Aug-23 crash), A-grade + 2 adds, **+114.45R**; strip-best −5,871.36. Annex (characterization only): BTC-spent −3.53R/campaign (115), contaminated −1.68 (978).
**Hypotheses at registered thresholds:** H-A1 INDETERMINATE (registered in a positive-total frame — reviewer registration miss, owned) · H-A2 PASS-partial (A≥B holds; C void by design — registration flaw, owned) · H-A3 FAIL (0/4 assets positive in ANY mandate; diagonal ambiguity moot) · H-A4 PASS · H-A5 FAIL (PROTECTED-heavy, not STILLBORN). **Charter framing (critical):** evidence about the mechanical ruleset only — NOT the discretionary edge (spent-annex row exists for that comparison) and NOT the signal layer alone (0× is positive: signals carry weak gross edge; execution structure spends it twice).
**Evidence & disclosure ledger:** exploration-classic FIRST LOOK consumed. Partial-look records: run-15 halt (7 cells' one-line summaries incl. end equity, builder-only) and run-21 halt (10 cells, builder-only); 10/10 stripped-hash inertness proven empirically. Reviewer+operator disclosure: SOL_position terminal equity 9005.57 and realized-R −20.14 (seen during G-6 mismatch verification; recorded). Post-verification, reviewer has mined the full packet — chartered (VR-1). Lockbox untouched; spent window characterization-only forever.

## 4. Forensics report 1 — verified findings (file: V3_STOP_AND_EXIT_FORENSICS.md; commit status per §0.6)
- **99.7% of exits are stop exits** (8,365/8,387; failure_x 12, opposite_cross 5, campaign_died 5, stop_gap 84 within the stop count). Exit logic IS stop logic.
- **Stop census, two lenses that disagree:** field lens (exit-row stop vs entry stop): 72% "unmoved" / 8% ratcheted<BE / 20% ratcheted≥BE (stable across mandates). Economic lens (authoritative): only **1%** paid full −1R gross; 74% partial losses; 25% profitable stop exits; "unmoved" class median gross −0.83. Reconciliation: most fills occurred at levels tighter than entry stop ⇒ protection DID move ⇒ the exit-row `stop` field does not record the filled level (extraction item). 
- **H-S1 (open hypothesis):** re-arms clear stops (G-4 bar-trace: stop→NaN on cross bar) and re-place them — protection may be non-monotone; candidate mechanism for +1R-to-loss round-trips. S-1 counts de-ratchet events.
- **Shaken-out:** 35% of stop-outs (2,899) saw ≥+1R favorable continuation within 20 exec bars (initial-class: 36% ≥1R, 23% ≥2R; median +0.23); forfeited pool ≈ **+4,429 cell-R** (capped 10R/unit) ≈ 77% of the grid loss.
- **Shadow stop families:** wholesale substitution FALSIFIED (actual gross +1,345 vs anchor +408 vs volbuf +246; better on only 16%/11%) — but both IMPROVE swing (+100/+192 on a +284 swing book) while destroying intraday/position. Enrichment-per-mandate, not replacement.
- **Harvest (centerpiece):** 40% of tranches reached ≥1R MFE; **median capture 6% of peak**; 62% kept <25%; **1,581 (47% of PROTECTED) still exited at gross loss** (Σ −1,224); PROTECTED cohort NET NEGATIVE (−649). Oracle exit +9,225 gross vs actual −5,757 net (~15k gap); oracle entry (MAE trough, hindsight) +7,525.
- **Costs:** fees 60–66% / slippage ~34–40% / funding ~0–1%; per-unit per-tranche median swing 0.47 / intraday 0.82 / position 0.27 (means 1.18/2.07/0.72). Stop-distance medians: 1.22 / 1.36 / 1.14 ATR_exec — **operator's 0.25-ATR premise corrected**; the problem is the left tail: sd/ATR<0.5 = 739 tranches (9%) carrying **−3,388 cell-R (59% of grid loss)**, of which 47% still reached +1R MFE; the 0.5–1.0 band is least-bad (Σ −283).
- **Entries:** alt-entry shadow WORSE than actual (better 37%, median −0.93 R-units) — fill prices aren't the leak; retracement depth monotone (−0.69 shallow → −0.18 deep); Z2≥Z1; provisional≥full (E-1 entries not the problem). Adds carry the gross edge (+0.47/unit vs R1 +0.11) but pay ~2.4R/unit costs; **orphan campaigns** (adds into never-filled R1): 265, Σ−817, −3.08/campaign — worst cohort. All top-10 campaigns are full 3-tranche ladders; top-20 winners = 38% of Σwins (tail broad-ish).
- **Candidate register C1–C7 (bounds = in-sample ceilings, non-additive, mixed gross/net):** C1 min-stop-distance gate ≥0.5 ATR (**+3,388 net**) · C2 half-harvest @+2R (+587 gross, 2,082 tranches) · C3 BE-floor @+1R MFE (+509 gross) · C4 orphan kill — no adds unless R1 filled (+817 net) · C5 swing-only volbuf trail (+192 gross swing) · C6 resumption re-entry (pool +4,429; trigger undesigned → S-1 + interview Q3) · C7 deep-retr preference (monotone table). All require S-1 measurement → pre-registered variant slot → lockbox/forward validation.

## 5. Doctrine discoveries (last turn; reshape report 2 and V4)
- **The v11 ratchet is EVENT-driven, not profit-driven** (Playbook §6): stop steps only when a NEW signal prints (each R2+ PRIME → new signal bar's extreme ∓ buffer). It never watches open profit. ⇒ **PROTECTED paradox resolved:** PROTECTED = entitlement label (MFE ≥1R/unit), not outcome; no new PRIME during the excursion ⇒ no protection despite +1R+. This is SSv10's X0 failure in new costume (gate = next PRIME instead of +1R arming line). Report 2 ships the MFE distribution so V4's engagement condition is FITTED (Prometheus principle: distribution first, mechanism second).
- **V3 has NO take-profit logic — harvest is absent, not deficient.** Playbook §7 (operator's own doctrine: bank 25–50% at TPW; partial into ~2-ATR extension; exec-9 trail for the core; flat on X) was never mechanized; partial exits don't exist (all-or-nothing by charter). BUT the engine journaled the flags unused: `engagement_flags.tpw_before_exit`, `ext_before_exit` (+ others, extraction item 5) ⇒ report 2 can measure the operator's own exit doctrine against the ride, from the diary.
- **Prometheus doctrine already absorbed** (project file `Prometheus_Stop_Loss_Logic_Learnings.md`): X0 R1-armed ratchet decorative for ⅔ of trades (51/77 pre-engagement deaths; capture −0.05..−0.43); winner = engagement-guarded e200 structure trail + ATR buffer (X3 0.25× / X4 0.5×), two-phase lifecycle (structural survival stop anchored to trigger-bar extreme → structure trail); simplest beat hybrids; never-loosen invariant; buffer regime-sensitivity (tight wins chop/crash, wide wins trends — keep both arms running); FADED ≈ fixed 17% tax; median trade −1.000 under gating (initial stop dominates trailing in low-hit systems); tail-capture line outranks give-back; shadow-stop journaling = highest-leverage schema decision; size the exit's share of loss before tuning it. `stop_alt_anchor`/`stop_alt_volbuf` are almost certainly descendants of P1-anchor and X3/X4 — extraction confirms formulas.

## 6. Open semantics questions (block precision in report 2 until extraction answers)
Exit-row `stop` field meaning at journaling time · `postexit_cont_{1,5,20}` exact definition (empirically: signed per-unit R, trade-direction-positive, ~46% negative on stop exits, unbounded) · `give_back_r` identity (neither mfe−net_u nor mfe−net matched; report 1 used self-defined capture = gross_u/mfe) · `exit_XA..XD` shadow meaning/units · `retr` exact definition · full `engagement_flags` set · initial-stop placement formula per entry kind + any floor · every stop-move/clear/re-place code path + monotonicity scope.

## 7. Builder extraction paste (issued last turn; output pending — re-issue verbatim if needed)
```
V3 forensics support — read-only code extraction, no changes, no runs. From engine at 2f260e1 + naiad_v0/v12_anchor configs, report verbatim logic with file:line refs, in plain terms:
1. INITIAL STOP: exact placement rule per entry kind (R1 PRIME, ADD, V) — anchor bar, extreme, buffer formula, any mandate/TF dependence. Is there any minimum-distance floor?
2. RATCHET: every code path that MOVES a stop — triggers (new PRIME? CONFIRM? anything price/MFE-based?), step target formula, buffer. Every code path that CLEARS or RE-PLACES a stop (re-arms, V, flips). Is monotonicity (never-loosen) enforced per tranche, per campaign, or not at all across re-arms?
3. EXIT-ROW `stop` FIELD: at journaling time, which value is written — the level that filled, the tranche's original stop, or post-bar-update state?
4. SHADOW DEFINITIONS, verbatim: stop_alt_anchor and stop_alt_volbuf (formulas, parameters, update cadence); entry_alt (what rule it tracks); exit_XA/XB/XC/XD (what each variant is, and its units in the journal).
5. FLAGS: exact definitions of engagement_flags (tpw_before_exit, ext_before_exit, all others present).
6. HARVEST: confirm or refute — does any partial-exit / take-profit path exist anywhere in trading.py?
Deliverable: one markdown doc, research_outputs/v3_anchor/ENGINE_LOGIC_EXTRACT.md, commit "v3 forensics: engine logic extraction (read-only)", push.
```

## 8. Interview — six questions awaiting operator answers (receive, then write report 2)
1. **Initial stop, in his own words:** where does his discretionary stop actually go (structure + cushion? ATR? invalidation)? Does he skip trades when the honest stop is too close? → calibrates S-1 placement families + C1 floor.
2. **Harvest, in his own words:** is Playbook §7 (TPW 25–50% bank; extension partial; exec-9 trail; flat on X) faithfully his practice? Omissions? Most-trusted element? → harvest family #1, mechanized as stated.
3. **Re-entry trigger** after a stop-out with idea intact, concretely. → designs C6.
4. **Ratify shaken-out definition:** stop-out counts as shaken out if post-exit continuation ≥ +1R (trade direction) within 20 exec bars (journal horizon; S-1 extends to 100+ with adverse-first screening). Accept/amend.
5. **Pre-register his gut:** rank C1–C7 by expected value BEFORE S-1 measures them (his intuition vs instruments, on the record).
6. **V4 scope:** same signal brain + rebuilt risk/harvest shell (charter-light), or signal-layer surgery on the table? Frames report-2 recommendations.

## 9. Prometheus report — intake protocol (operator will deliver a report authored by the Prometheus agent)
1. **Baseline:** the stop-loss learnings doc is ALREADY in project knowledge and absorbed (§5). On receipt, **diff for deltas** — do not re-import what's already integrated.
2. **Provenance discipline:** that lineage marks claims [record]/[verified]/[analysis]. Demand or reconstruct the same tiering; treat unmarked numbers as [analysis] until sourced. Prometheus's numbers cannot be recomputed here (its journals aren't in this repo) — so claims are inputs to hypothesis design, never evidence for Naiad gates.
3. **Applicability caveats, standing:** Prometheus = SSv10, single-position, 5m, one asset-class posture; Naiad = v11 cascade, ≤3-tranche campaigns, 3 mandates, 10 assets. Exits must be re-fitted per entry set — the anchor/volbuf grid-loss-but-swing-win result is the in-house proof that v10 winners don't transfer wholesale.
4. **Mapping rule:** every Prometheus claim lands in exactly one bucket — (a) confirms/refutes/extends a C1–C7 candidate or H-S1; (b) proposes a NEW S-1 shadow family (name it, parameterize it a priori); (c) V4 design principle (doctrine tier); (d) inapplicable (say why). Nothing enters engine logic without S-1 measurement + pre-registered variant slot; discretionary-tier insights may go straight to the operator's manual.
5. **Watch for:** the X3-vs-X4 buffer regime question (keep-both-arms pattern), the FADED-by-engagement partition method, tail-capture metric spec, aging/time-stop status ([record gap] there), and anything on re-entry or partial harvests — those map to C6/C2 directly.

## 10. Next-step queue
**Now:** §0 checks → receive extraction + interview (+ Prometheus intake in parallel) → **Report 2** ("V3 Forensics — Deep Dive"): plain-language for a non-developer; three-source structure (doctrine[playbook] vs implementation[extraction] vs behavior[journals], every divergence = finding); the Q1a breakdowns (MFE-before-death buckets × stop class × mandate × grade × continuation); exhaustive shadow cross-comparison with conditional "why" decomposition; TPW/ext flag doctrine test (mechanized §7 vs actual); MFE/MAE distributions for ratchet fitting; Prometheus diagnostic suite (capture ratio, pre-engagement death share, FADED-by-engagement, tail-capture line, asymmetric-evaluation rule); C1–C7 refresh + Prometheus deltas. **Then:** S-1 contract (measure-only instrumented replay on exploration-classic; families: initial-stop ATR sweep incl. floors 0.5/0.75/1.0 [NOT journal-recomputable — path dependence], BE-floor 0.75/1.0/1.5R triggers, giveback trail 30/40/50% from peak ≥2R, swing structural trail (volbuf-tuned), resumption re-entry, §7-harvest family from interview Q2; semantics pins from §6; pre-registered P-S1 ~70% de-ratchet events exist & explain ≥⅓ of +1R-to-loss round-trips, P-S2 ~65% path-aware BE-floor/giveback beat static bounds, P-S3 ~60% C1 realized benefit within ±40% of +3,388). **Then:** V4 design concept (operator experience × forensics × Prometheus; variant-slot prioritization — seeds now exceed the 5 chartered slots: zoneMemory-5, provisional-Z1-only, zone-gated CONFIRM adds, V-entry-filter, one-add-per-bar dedupe, plus C1–C7). **Parked:** CD-2 ruling → collector build → first green tick with journal rows → paper epochs stamp → go-live ledger entry (consolidating first-dispatch failure, W-1, W-2, CD-1, probe verdict, CD-2, N-1) + Phase-2 protocol + ferry · engine 1.0.8 (G-1+J-1) · G-2 LIT remediation · Pine backlog B-1/B-2 (B-3 shipped as v11.3).

## 11. Registers & tallies
**Open:** G-1 (lockbox guard, →1.0.8) · G-2 (LIT estate) · J-1 (→1.0.8) · W-1 (Node bump) · N-1 (error-only tick on data) · CD-2 (VPS lean) · CD-3 (downstream) · H-S1 (open hypothesis) · B-1, B-2 (Pine backlog) · E-1 (erratum on record; revert = variant seed). **Closed this session:** V2 gate · V3 gate+run · G-3..G-6 · CD-1 · W-2 · B-3(v11.3, prior). **Prediction tally: 18 confirmed / 6 falsified** (P-1..P-24 scored; the six falsified: P-2 file-map, P-3 pure-rename, P-5 first collector run, P-18 & P-20 & P-16-completion legs — each converted to findings). Outstanding: P-S1/S2/S3 (§10). Anchor-run hypotheses scored separately: 2 pass / 2 fail / 1 indeterminate.
**Discipline recap:** ledger appends builder-typed byte-for-byte, append-only, correction entries never rewrites · every headline recomputed from raw artifacts before "real" · pre-register before investigating; score at registered thresholds; own registration flaws (H-A1/H-A2 precedent) · one-word vetoes on flagged decisions · strip-best line by default; no cost-free numbers; CI on every headline · plain language always · builder proposes, reviewer verifies, operator merges.
