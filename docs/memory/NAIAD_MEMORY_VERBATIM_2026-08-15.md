# NAIAD PROJECT MEMORY — VERBATIM CAPTURE · 2026-08-15 (post box-governance session)

**What this is.** Both memory layers exactly as they stand at capture time, for the redesign's
before-record. Part A = the auto-synthesized injection (what actually enters every message's
context). Part B = the 17 operator-controllable edit entries (the steering input the synthesis
composes from). Provenance: Part A transcribed from the live injection this session; Part B read
via the memory tool's view command this session. Both `[verified]`.

**Known defects at capture, named so the redesign is graded against them:**
- Entry 6 still asserts Windows-era facts (C:\Users\luisf paths, G:\ destinations, D:/Naiad archive
  home, 6.39 MB box with warn 25/refuse 40, Task Scheduler triggers) — contradicting entries 13/15.
- Entry 17 carries the superseded warn 50%/refuse 80% thresholds (recalibrated to 40/70 the same day).
- Entries 4/16 carry study numbers duplicated from LEDGER.md; entries 5/6/9/11 carry procedure
  duplicated from CONVENTIONS §§2/8/4/3.

---
---

# PART A · THE INJECTED SYNTHESIS (auto-generated; steerable, not directly writable)

## Purpose & context

**Prime directive:** Develop consistently profitable trading systems. Secret Sauce is one candidate
system, not the project itself.

**Operator:** Ludwig — discretionary crypto trader, Buenos Aires, no programming background. All
operational instructions: plain-language, step-by-step, expected outputs stated. Major deliverables:
zero-context explainers with per-claim provenance (verified/measured/expect/open), ending in an
options menu with reasoning.

**Guiding attitude:** "The alpha is buried under a pile of debris." Stay with underperforming
mandates — turn the problem until the mechanism is apparent, then fix it. Priority order: better
trade-birth filters → effective stop ratchet → take-profit that keeps profit. No restructuring
around fees/execution before demonstrable edge.

**Fractal lens (standing):** Price structure may replicate across timeframes with scale-adapted
variables. Working hypothesis: GROSS behavior is fractal-compatible, NET is not (costs are
scale-invariant in bps; excursions scale with TF). All MTF tables print absolute AND
governor-relative coordinates.

## Current state

**Machine:** MacBook Pro 16" M5, 48 GB, macOS. Geography: `~/Naiad` = working repo (~4.6 GB,
outside all sync trees) · `/Volumes/LaCie` = backup only · GitHub `catpatrol/Naiad` via SSH =
anchor. venv `~/venvs/naiad` (Py 3.12). Three launchd agents armed: daily 07:00, estate Sun 08:00,
workflow Sun 08:30.

**Data residency v2 (2026-08-15):** Live working data born and read locally under `~/Naiad`. New
archives born local (--phase), mirrored to LaCie in the same act. Older/completed zips → LaCie
only; repo keeps tracked `.sha256` sidecars + `POINTER.md`. LaCie reads never accessed during
normal work — backup writes only. Estate cache: `~/.cache/naiad/data_cache`.

**M-phases:** M0–M4 + zip-residency all ACCEPTED. M2 restore: 425 files/7.45 GB/0 mismatches
(including census2a, census2b, seq8_run2 previously declared unrecoverable). M3: 43 Windows
drive-literal defects patched, CRLF sidecars fixed, census gates un-deadened — census work
UNBLOCKED. M4: launchd agents live.

**Remaining M5 items:** Spotlight privacy list · Cowork remount at `~/Naiad` + discriminator test ·
Drive mirror 53/53 verification then PC-era cloud cleanup · 11 CRLF source files · slot-anchored
brief triggers (launchd has no timezone field) · `backup_estate --no-publish` · box at ~37% WARN.
Old PC alive, decommission = later phase.

**Suite baseline:** 287/0/1.

**Census-2A:** COMPLETE (contract SPENT). One supported result of nine scored.

**Active orchestration:** Exchange bus live; CONVENTIONS.md is the authoritative instruction
surface for all six actors. Box at ~37%.

## On the horizon

**Next-cycle slate (APOLLO):** P-ARM-2 [55] · P-FAN-1 [60] · P-NEST-2 [50] · P-RAT-3 [40] ·
P-VBT-2 [45] · P-CHOP-2 (ribbon operand = OR{30m,1h}) · P-REL-1b replication. Needs-word first:
leap/stair window recut, ATR buckets, H-VBT band rule.

**Path forward:** EngineV2/Tier-C design interview and/or the Rotation Program funnel (now a
statistical mandate — panel votes 65% together). Pickup document:
`exchange/reports/CENSUS2A_CLOSEOUT_2026-08-12.md`.

**Parked:** momentum-vs-mean-reversion regime discrimination (pre-M4); three additional items noted
in the Aug-15 session log.

**Infrastructure:** TC-2 (re-entry quality bar, inherits intraday mandate) · SSv12 Pine indicator
updates · ARGUS brief redesign (Board/Trap Cards/Watch architecture) · box cleanup post-WARN.

## Key learnings & principles

**Study spine (findings through Census-2A; full numbers in LEDGER.md; SS definition + THE PATH +
MC program in `exchange/reports/SS_SYSTEM_SYNTHESIS_2026-08-06.md`):**

- **Architecture of record:** Structural stop (anchor beyond a real 1H swing pivot) dominates —
  grid +565.58, strip-best +301.99. Swing positive-and-robust for the first time.
- **FALSIFIED, never re-assert:** Two-line exit architecture (S-2: native ratchet fires first,
  trail clips engaged winners).
- **Harvest problem (priority 3, unsolved):** 73.67% of +1R-touchers round-trip to gross loss.
  Winning book rides to regime break (opposite_cross 67% / failure_x 24%) — never manages
  individual winners.
- **Add gate (P-CIRC):** 97.59% of +1R winning moments had no pullback-reclaim PRIME available
  (circular topology).
- **Entry admission:** Biggest gate vindicated (P-F1); 8 conditions negative on S-1 flip positive
  on struct book, slow TFs strongest.
- **Fractal program:** FH-3 CONFIRMED RELATIVE — all cross-TF rules in governor-relative steps.
  TC-5: mechanical fractal confirmed, economic claim falsified — entries floored at 5m PERMANENTLY.
- **Research inversion (ratified "forensics"):** Condition on outcome, discriminate at birth. Loser
  book = TWO species with opposite cures — born-dead births → entry gates; died-of-neglect +1R
  round-trips → harvest fixes.
- **Census-2A structural findings:** Ride-only book = 12.3% winners × mean +9.4R (every exit study
  is tail preservation). EMA-arming edge over random ≈ toll-sized (+0.052 ATR). Registry
  co-location rare at armings (0.7%) — confluence-as-count premise unsupported. Incumbent loser
  decile was a position-size selector (97.5% size-0.5).
- **P-REL-1b:** SUPPORTED-PROVISIONAL (in-window 12/25 presence, +0.19 ATR H100; qualified by panel
  return correlation 0.6458 / sign-agreement 0.60 — five witnesses ≈ one).

**Discriminant frame + tail-retention gauge:** Mandatory census deliverables —
decision-curtain-clean, five-asset sign-replicated, held-in-time stable, BH-FDR q=0.10; TRG printed
beside strip-best.

**Prior conclusions are points of control, never mandatory design constraints.** Sizing deferred
post-TC-1; count-based tiers, never fitted weights.

**MTF_SET = {5m, 1h, 4h, 12h}** — engine has NO 1D and NO 30m anywhere.

**EMA family amendment (2026-08-12):** The observed EMA family gains the 500, with ±n×ATR cushion
grammar, across all future instruments and contracts. Warmup (SEQ8 rule, ~1,731 bars): trivial ≤4h;
12h warms ~2022; 1d evidence-side marginal; 1W/1M never. Pairs discipline: primary = price↔500
interactions + 450/500 ribbon spread; full cross-pair set is annex-only.

## Approach & patterns

**Decision-funnel interview (every gating decision):** (1) What it is — plain language, zero
context assumed; (2) Why it matters and why it needs Ludwig's attention; (3) All actionable options
including reviewer's path; (4) Implications for and against each. Recommendations only as far as
evidence supports; state uncertainty explicitly. Questions self-contained, big first, HTF→LTF;
one-word answer affordances welcome. Keep everything moving that can be moving — independent
workstreams run in parallel always.

**Reviewer error taxonomy (three classes; catalogue in `docs/memory snapshot 2026-08-06` entry 11
and CONVENTIONS §6.2):**
- **Class A — machinery-content:** Asserting what code/data/memory contains without reading it.
  Cure: context-gap audit during contract drafting; any §Basis claim cites a file actually read
  this session; to claim absence, enumerate the set.
- **Class B — post-action-state:** Writing a verification gate from pre-paste state. Cure: simulate
  steps in order, write every gate from post-action state; gates match distinctive fragments, never
  full sentences authored in the same act.
- **Class C — provenance:** Promoting a report to [verified]. Cure: [verified] = computed or read
  this session; operator-reported = [operator]; builder-reported = [handoff].

Counter-pattern: the builder refusing reviewer instructions on ratified invariants is the system
working correctly.

**Paste construction rules (full text CONVENTIONS §2–3):** Every paste = plain-language purpose
statement + routing line ABOVE the code block + rollback line + hard environment assertion halting
before any action (read-only pastes included). Identity probe: `git rev-parse --short HEAD; pwd`.
Two-sided identity gate on every paste: HALT if path contains OneDrive/iCloud/Drive staging
markers; HALT unless pwd == `$HOME/Naiad`.

Shell hazards (each having cost a failed run): backslashes die in transit — use forward slashes;
`publish_exchange.py` is a library with no `__main__` — call `publish()` per CONVENTIONS §3.4;
`publish()` is path-scoped to `exchange/**`; a status read through a pipe ending in a formatter
always exits 0 — capture grep's status directly.

Every build document ends with the disposition table: PATH · EXISTS · TRACKED · COMMITTED · PUSHED
· PROTECTED BY · BOX COST. Committed ≠ pushed ≠ backed up. EXISTENCE IS NOT PROTECTION.

**Single build document rule:** Every builder session emits exactly one document — forensic record
AND review artifact — gate values as printed, full fixture transcript, full result tables, findings
reported-not-fixed, decisions with authority, open items with owners, options with implications,
disposition table with BOX COST.

**Backup verification standard (binding):** Bidirectional per member — bytes re-read out of archive
hash equal to fresh read of source; membership cross-checked archive/manifest/disk; re-read from
destination after write; no-clobber. MANIFEST.json/manifest.json case-collision carries to APFS —
restore case-sensitively.

**Session-start:** Read `exchange/status/CONVENTIONS.md` FIRST — authoritative for paste
construction, reporting, file handling, lane etiquette. If CONVENTIONS and a memory entry conflict:
say so, quote both, ask Ludwig.

**Status blocks:** Every material session ends with a STATUS block (<25 lines: NOW / LAST EVENT /
FACTS tagged verified|ledger|ratified|handoff|open / PENDING / NEXT / the two Q-8 integers).

## Tools & resources

**Lanes + orchestration:**
- APOLLO — SSv12, census, Tier-C, Engine V2, SSv12 Pine
- ARGUS — analytics, daily brief, volume filter
- ATHENA — resilience, backups, integrity, workflow
- DIONYSUS — independent critique (Cowork)
- HERMES — coordination/verification (Cowork): DIGEST 2×/day, box budget, staleness stamps, inbox
  routing, queue validation; never re-authors, never self-verifies (HELIOS tripwire)
- HEPHAESTUS — the builder (local Claude Code)

Chats cannot read each other. Exchange bus + project box + operator-relayed STATUS blocks are the
ONLY channels. Queue drafting rights: APOLLO/ATHENA/ARGUS only; every item carries operator's
RATIFIED stamp. Lane state never returns to shared memory — lives in per-lane ledgers, read at
session start.

**Box rules:** ≈6.39 MB, indexed for retrieval not mounted. Standing tick set: LEDGER.md +
exchange/ ONLY; everything else requested per lane, per need. DOCUMENTS ARE CHEAP, DATA IS NOT —
captures, renders, results JSONs, and substrates NEVER enter exchange/; path + sha256 pointer only
(CONVENTIONS §4.2, text only, 1 MB/file). Box visibility test: a project-knowledge search returning
a FOLDER PATH — a bare filename may be a hand-upload and proves nothing.

**Backup tiers:**
- Tier 1 (data): `~/.cache/naiad/data_cache` (macOS) — NOT in repo, never synced; LaCie vault
  `/Volumes/LaCie/naiad-backups`, Drive-mirrored to cloud
- Tier 2 (repo): GitHub `catpatrol/Naiad`; phase archives permanent evidence, never prunable —
  tracked `.sha256` sidecars + POINTER.md in repo
- Tier 3 (AI state): `docs/memory/` snapshots (import is lossy; global Reset deletes irreversibly)

**Publish guard:** Warns at 25% / refuses at 40% of 6.39 MB box budget; refreshes MANIFEST.json
every publish. SYNC IS NOT BACKUP.

**Repo:** `catpatrol/Naiad`, branch `v12-v1-census`. Engine v1.0.12. Authoritative instruction
surface: `exchange/status/CONVENTIONS.md`.

*[recent_updates block, carried with the injection:]*
- SURFACES + THE BOX (custody: ATHENA, per APOLLO handoff 2026-08-15) … BOX CEILING = 16,000,000 B
  [operator ruling 2026-08-15, unmeasured — the old 6.39 MB was measured; treat the ceiling as
  provisional]; thresholds under ATHENA custody (warn 0.40 / refuse 0.70 of the tick set =
  exchange/** + LEDGER.md — D3 metering gap closed); publish prints tick-set %, exchange-only %,
  and absolute MB … Named-constant change protocol (grep NAME + VALUE + THRESHOLD-TEXT, dependents
  table with pin-vs-import per site, historical regenerators stay pinned).
- 2026-08-15 box + lane state: BOX_BYTES raised 6.39→16 MB … governance HANDED TO ATHENA … APOLLO
  lane cleared for SSv12 only: ① TIER-C2 BASELINE paste standing/unrun ② VIZ-3 Trade Cathedral
  contract NEVER ARRIVED on builder machine (V-10) ③ ORACLE grid + W-E+ cards → Pine v12.0.2.
  Rulings: ["stations"] station canon v1; ["fuzz"] = named-unruled [VETO] set, register-carried
  UNRULED. Cathedral payloads emitted F-V3 PASS (DESIGN_HANDOFF_VIZ3, 7 files, local).
- [Removal echo of the superseded 2026-08-03/06 SURFACES+BOX entry text.]

*(Note the synthesis's internal contradictions at capture: "Box rules ≈6.39 MB" and "warns at
25%/refuses at 40%" in the body vs the 16 MB / 0.40 / 0.70 facts in recent_updates — the stale
entry-6/17 inputs visible in the output. The redesign's success criterion is that this class of
contradiction becomes structurally impossible: one fact, one home, one pointer.)*

---
---

# PART B · THE 17 EDIT ENTRIES, VERBATIM (read via the tool this session)

**1.** THE FIRST RULE (operator, 2026-08-06): be elitist, clever, concise, efficient, and elegant —
highest standard of craft, signal-dense, no ceremony; the shortest version that loses nothing.
PRIME DIRECTIVE (operator, 2026-08-01, verbatim): "Project Naiad's main purpose is to develop
consistently profitable trading systems." Secret Sauce is ONE of Naiad's systems, not the project
itself. OPERATOR: Ludwig, discretionary trader, no programming background — every operational
instruction plain-language, step-by-step, with expected outputs; major deliverables are zero-context
explainers with provenance per claim (verified/measured/expect/open) ending in an options menu with
reasoning (ratified 2026-07-15).

**2.** GUIDING ATTITUDE (operator, ratified 2026-07-15): "the alpha is buried under a pile of
debris." (1) Stay with underperforming mandates — turn the problem until the mechanism is apparent,
then fix it; never retire early. (2) No restructuring around fees/execution before the strategy
shows demonstrable edge. (3) Priority: better trade-birth filters → effective stop ratchet →
take-profit that keeps profit. History and verbatim quotes: docs/memory snapshot 2026-08-06,
entry 2.

**3.** FRACTAL LENS (operator, 2026-07-18, standing analytic frame): price structure may replicate
across timeframes with scale-adapted variables — never treat cross-TF signals/outcomes as different
animals by default; study the correlations explicitly and falsifiably. Working hypothesis: GROSS
behavior is fractal-compatible, NET is not — costs are scale-invariant in bps while excursions
scale with TF. All MTF tables print absolute AND governor-relative coordinates.

**4.** STUDY SPINE (findings through 2026-07-20 + rulings 2026-08-03; full numbers in LEDGER.md; SS
definition, THE PATH and the MC program live in exchange/reports/SS_SYSTEM_SYNTHESIS_2026-08-06.md).
ARCHITECTURE OF RECORD: the STRUCTURAL STOP (anchor beyond a real 1h swing pivot) dominates — grid
+565.58, strip-best +301.99; SWING positive-and-robust for the first time; struct-30m dropped as a
lottery. FALSIFIED, never re-assert: the two-line exit architecture (S-2: the native ratchet fires
first and the trail clips engaged winners). HARVEST PROBLEM (priority 3, unsolved): 73.67% of
+1R-touchers round-trip to gross loss; the winning book rides to regime break (opposite_cross 67% /
failure_x 24%), never manages individual winners. ADD GATE: P-CIRC — 97.59% of +1R winning moments
had NO pullback-reclaim PRIME available (circular topology). ENTRY ADMISSION: the biggest gate
vindicated (P-F1); 8 conditions negative on S-1 flip POSITIVE on the struct book, slow TFs
strongest. FRACTAL PROGRAM: FH-3 CONFIRMED RELATIVE — all cross-TF rules in governor-relative
steps; TC-5: mechanical fractal confirmed, economic claim falsified — entries floored at 5m
PERMANENTLY; TC-2 (re-entry quality bar) inherits the intraday mandate. RESEARCH INVERSION
(ratified "forensics"): condition on outcome, discriminate at birth; the loser book is TWO species
with opposite cures — born-dead births → entry gates; died-of-neglect +1R round-trips → harvest
fixes. DISCRIMINANT FRAME + TAIL-RETENTION GAUGE are mandatory census deliverables:
decision-curtain-clean, five-asset sign-replicated, held-in-time stable, BH-FDR q=0.10; TRG printed
beside strip-best. PRIOR CONCLUSIONS ARE POINTS OF CONTROL, never mandatory design constraints.
SIZING deferred post-TC-1; count-based tiers, never fitted weights. MACHINERY: MTF_SET =
{5m,1h,4h,12h} — the engine has NO 1D and NO 30m anywhere; completion commits reproduce the run
from a clean checkout.

**5.** PASTE RULES (full text CONVENTIONS §2–3; history in docs/memory snapshot 2026-08-06,
entry 5): every paste = plain-language purpose statement (what it's for, what each step does, what
you'll see on screen, what changes on disk) + routing line ABOVE the code block ("→ WHERE TO PASTE:
your LOCAL Claude Code — the PowerShell window on your PC", or explicit cloud target) + ROLLBACK
line + hard environment assertion halting before ANY action, read-only pastes included — a
misrouted read silently returns plausible nonsense. Identity probe: `git rev-parse --short HEAD;
pwd`; "local" = the session whose probe returns the working-clone path. Cloud sessions:
catpatrol/Naiad only, must push in the SAME session or the work does not count (R-A'). SHELL
HAZARDS, each having cost a failed run: backslashes die in transit — forward slashes and chr(92);
publish_exchange.py is a LIBRARY with no __main__ — call publish() exactly per CONVENTIONS §3.4;
publish() is path-scoped to exchange/** so a git add outside it strands the file; a status read
through a pipe ending in a formatter always exits 0 — capture grep's status directly. Every build
document ENDS with the disposition table: PATH · EXISTS · TRACKED · COMMITTED · PUSHED · PROTECTED
BY · BOX COST. Committed ≠ pushed ≠ backed up; EXISTENCE IS NOT PROTECTION — a twin justifies
removal only if TRACKED and on the REMOTE, verified.

**6.** BACKUP + INTEGRITY (state verified 2026-08-04/05, guards live 2026-08-11; standard in
CONVENTIONS §8; retraction narratives in docs/memory snapshot 2026-08-06, entries 12+17). SYNC IS
NOT BACKUP — sync propagates deletions faithfully. TIER 1 estate:
C:\Users\luisf\AppData\Local\naiad\data_cache via engine/data.py cache_dir() — NOT in the repo,
never under OneDrive; dated hashed generations on G:\My Drive\naiad-backups plus the browser-only
second Drive account (date hand-recorded in exchange/status/SECOND_ACCOUNT.md). TIER 2 repo: GitHub
for everything tracked; phase archives are PERMANENT EVIDENCE, never prunable — all 9 live at
D:/Naiad/research_outputs/_archive (hash-verified) with byte-identical twins on
G:/My Drive/naiad-backups/phases/; the repo keeps only the git-TRACKED .sha256 sidecars +
POINTER.md (.gitignore covers *.zip, not *.sha256 — archive out of git, fingerprint in). QUEUE 002
SHIPPED 2026-08-11 (ACCEPT 10/10, commit c32ffa5): --phase defaults to
D:/Naiad/research_outputs/_archive with sidecar in the repo, HALTS if D: absent; --phase --dest is
an honest error; publish enforces an exchange/ size budget (warn 25% / refuse 40% of the 6.39 MB
box — metering exchange/ only, ~4 points under true tick-set occupancy) and refreshes MANIFEST.json
every publish. TIER 3 AI OS: docs/memory/ snapshots are the restore path (import is lossy); a
global "Reset memory" deletes project memories irreversibly; export inclusion disputed → T-6.
VERIFICATION STANDARD, binding: bidirectional per member — bytes re-read OUT of the archive hash
equal to a fresh read of the source; membership cross-checked archive/manifest/disk; re-read FROM
destination after write; no-clobber. RESTORE HAZARD: MANIFEST.json vs manifest.json silently
clobber on case-insensitive Windows — restore case-sensitively. TRIGGERS (by enumeration): daily
07:00 · estate Sun 08:00 · workflow Sun 08:30, all ARMED.

**7.** LANES + ORCHESTRATION (rulings 2026-08-01/02; history in docs/memory snapshot 2026-08-06,
entries 7+9+10). APOLLO — SSv12, census, Tier-C, Engine V2, SSv12 Pine · ARGUS — analytics, daily
brief, volume filter (charter CONVENTIONS §5.1) · ATHENA — resilience, backups, integrity, workflow
· DIONYSUS — independent critique (Cowork) · HERMES — coordination/verification (Cowork): DIGEST
2×/day, box budget, staleness stamps, inbox routing, queue validation; never re-authors, never
self-verifies (HELIOS tripwire) · HEPHAESTUS — the builder (local Claude Code). Chats cannot read
each other: the exchange/ bus + the project box + operator-relayed STATUS blocks are the ONLY
channels. Queue drafting rights: APOLLO/ATHENA/ARGUS only; every item is a full contract carrying
the operator's RATIFIED stamp. Status layer of record: exchange/status/LEDGER_<GOD>.md, append-only
— a correction is a NEW entry. Every material session ends with a STATUS block (<25 lines: NOW /
LAST EVENT / FACTS tagged verified|ledger|ratified|handoff|open / PENDING / NEXT / the two Q-8
integers); ARGUS facts also carry their firewall side (display-only vs evidence); lane attribution
is by CONTENT, not chat title. APOLLO'S LIVE STATE lives in
exchange/reports/SS_SYSTEM_SYNTHESIS_2026-08-06.md + exchange/status/LEDGER_APOLLO.md +
exchange/queue/2026-08-06_MC1_may26_program_APOLLO.md — read at session start in that lane; lane
state never returns to shared memory.

**8.** REVIEWER ERROR TAXONOMY (three classes; instance catalogue in docs/memory snapshot
2026-08-06 entry 11 and CONVENTIONS §6.2 — the reviewer's mistakes are almost never analytical).
CLASS A · machinery-content: asserting what code, data, or memory contains without reading it.
Cure: the context-gap audit runs during CONTRACT DRAFTING; any §Basis claim cites a file actually
read this session — the memory panel counts as a file, call the view; to claim a thing does not
exist, ENUMERATE the set — a negative from a single lookup measures that lookup, not absence.
CLASS B · post-action-state: writing a verification gate from the state before the paste's own
steps run. Cure: simulate the steps in order, write every gate from the POST-action state; gates
match distinctive fragments, case-insensitively, never full sentences authored in the same act.
CLASS C · provenance: promoting a report to [verified]. Cure: [verified] = computed or read this
session; operator-reported = [operator]; builder-reported = [handoff]. COUNTER-PATTERN, the system
working: the builder has refused reviewer instructions on ratified invariants nine-plus times — a
stack whose executor never pushes back is a stack whose reviewer errors all land.

**9.** SURFACES + THE BOX (custody: ATHENA, per APOLLO handoff 2026-08-15). WEB CHATS
(APOLLO/ARGUS/ATHENA) share the box + memory, cannot read each other, cannot touch the disk — the
box is their ONLY repo access, INDEXED FOR RETRIEVAL, not mounted: test visibility with a
project-knowledge search returning a FOLDER PATH. COWORK (HERMES/DIONYSUS): read/write the mounted
repo, no memory, standing NO-DELETE POLICY. HEPHAESTUS: local Claude Code at ~/Naiad. A file
dragged into a chat lives in that one conversation at zero box cost. BOX CEILING = 16,000,000 B
[operator ruling 2026-08-15, unmeasured — the old 6.39 MB was measured; treat the ceiling as
provisional]; thresholds under ATHENA custody (warn 0.40 / refuse 0.70 of the tick set =
exchange/** + LEDGER.md — D3 metering gap closed); publish prints tick-set %, exchange-only %, and
absolute MB. Standing tick set: LEDGER.md + exchange/ ONLY. THE LAW: DOCUMENTS ARE CHEAP, DATA IS
NOT — captures, renders, results JSONs and substrates NEVER enter exchange/ — path + sha256 pointer
per CONVENTIONS §4.2 (text only, 1 MB/file). BOX COST column prices every box-bound file; DIGEST.md
is the catalogue. Named-constant change protocol (CONVENTIONS, from the BOX_CAPACITY incident):
grep NAME + VALUE + THRESHOLD-TEXT, dependents table with pin-vs-import per site, historical
regenerators stay pinned.

**10.** WORKING WITH THE OPERATOR (rulings 2026-08-02/03, reconfirmed verbatim; full text
CONVENTIONS §1). Every gating decision = a decision-funnel interview: (1) WHAT IS IT — plain
language, zero context assumed; (2) WHY IT MATTERS — and why it needs HIS attention rather than the
reviewer's; (3) OPTIONS — every actionable one, including the reviewer's path; (4) IMPLICATIONS —
for and against each. Recommendations only as far as evidence in hand supports; state uncertainty
and request guidance rather than manufacture a confident default. Questions self-contained (never
reference scrollback), big first, HTF→LTF; one-word answer affordances welcome; context richness
never traded for brevity. KEEP EVERYTHING MOVING THAT CAN BE MOVING — independent workstreams run
in parallel always; builder idle time during reviewer↔operator sessions is a cost to design
against. The builder ends every work cycle displaying the exact filenames the operator must carry,
with full repo paths, in BRIGHT COLOURS.

**11.** SESSION-START POINTER: read exchange/status/CONVENTIONS.md FIRST — AUTHORITATIVE for paste
construction, reporting, file handling, lane etiquette, the manual-task rule; it opens with THE
FIRST RULE and a FIND IT FAST index. It is the ONLY instruction surface reaching all six actors:
memory does NOT reach Cowork — HERMES, DIONYSUS and HEPHAESTUS have never seen a memory entry.
SINGLE BUILD DOCUMENT (operator, 2026-08-04): every builder session emits exactly ONE document —
forensic record AND review artifact — gate values as printed, full fixture transcript, full result
tables, findings reported-not-fixed, decisions with authority, open items with owners, options with
implications, the disposition table with BOX COST; a file never contains its own sha256.
REACHABILITY: a repo file is visible to a web lane only when a project-knowledge search returns it
WITH a folder path — a bare filename may be a hand-upload and proves nothing. Verbatim memory
snapshots: docs/memory/ (newest 2026-08-06, the 20 pre-redraft entries). If CONVENTIONS and a
memory entry conflict: say so, quote both, ask the operator.

**12.** MEMORY SYNTHESIS GUIDANCE (operator, 2026-08-11): keep project memory to identity, standing
rules, and pointers. Exclude lane-specific working state and study-result detail from memory — that
content lives in LEDGER.md, exchange/reports/SS_SYSTEM_SYNTHESIS_2026-08-06.md, and the per-lane
ledgers, read at session start per the lanes entry. Prefer the shortest phrasing that loses nothing.

**13.** DATA RESIDENCY v2 (operator, 2026-08-15, refined same day): live working data lives locally
under ~/Naiad on the Mac's SSD — substrates, census outputs, new acquisitions — born local, read
local. ZIP AGING RULE: new archives are BORN LOCAL (--phase) and mirrored to the LaCie in the same
act; OLDER/completed zips live on the LaCie ONLY — local research_outputs/_archive holds
current-cycle zips, tracked .sha256 sidecars + POINTER.md stay in the repo as the fingerprint set.
The LaCie (/Volumes/LaCie) is BACKUP ONLY: reads never touch it; drive_wait gates backup WRITES
only (PRESENT answers 0.00s; the 18s budget is the asleep case). Backup vault:
/Volumes/LaCie/naiad-backups, Drive-mirrored to cloud. Named cost: newest archives are
LaCie-sole-copy until the Drive mirror syncs. Estate cache on macOS: ~/.cache/naiad/data_cache.
MANIFEST.json/manifest.json case-collision carries to APFS — restore case-sensitively.

**14.** EMA FAMILY AMENDMENT (operator, 2026-08-12, verbatim instruction "PLEASE REMEMBER THROUGH
THIS LANE"): the observed EMA family gains the 500, with ±n×ATR cushion grammar, across all future
Naiad instruments and contracts — motivation: if the 450 clips as a ratchet reference, the 500 may
be the efficient one. Warmup (SEQ8 rule, ~1,731 bars): trivial ≤4h; 12h warms ~2022; 1d
EVIDENCE-side warms only weeks before the 2024-07-01 ceiling (marginal); 1d ops-side fine; 1W/1M
never. Operator already plots EMA500 on his charts (4H close 72,222 on 2026-08-12 screenshots).
Pairs discipline: primary = price↔500 interactions + 450/500 ribbon spread; full cross-pair set is
annex-only to contain combinatorics.

**15.** MACHINE MIGRATION COMPLETE (Queue-005 M0–M4 + zip-residency, 2026-08-14/15): the project
runs on a MacBook Pro 16" M5 (48 GB), macOS. GEOGRAPHY: ~/Naiad (= /Users/luis/Naiad) = the working
repo (~4.6 GB after dedupe), outside every sync tree · /Volumes/LaCie = backup drive (Naiad bulk
mirror + naiad-backups vault, Drive-mirrored to naiad1310@gmail.com cloud) · GitHub catpatrol/Naiad
via SSH = the anchor. Identity gate, TWO-SIDED, every paste: HALT if path contains OneDrive,
com~apple~CloudDocs, Mobile Documents, or Drive staging markers; HALT unless pwd == $HOME/Naiad.
Terminal/zsh native bash. venv ~/venvs/naiad (Py 3.12). M2 restore ACCEPTED (425 files/7.45 GB/0
mismatches); M3 sweep ACCEPTED (census2a+mc2 gates un-deadened — census work UNBLOCKED); M4
ACCEPTED (three launchd agents armed+kickstarted: daily 07:00, estate Sun 08:00, workflow Sun
08:30; named-set rotation of 8 spent CENSUS2A run docs); ZIP-RESIDENCY pass ACCEPTED (5.98 GB
verified-then-deleted, 0 mismatches; F-M3-6 fully closed; retention scan three-state honest; ruling
4a live). Suite baseline 287/0/1. OLD PC ALIVE, old trees intact (decommission + Phase B = later
phases; one --estate refresh owed there). REMAINING: M5 GUI items (Spotlight privacy list · Cowork
remount at ~/Naiad + discriminator test · Drive mirror 53/53 verification then PC-era cloud
cleanup) · 11 CRLF source files · slot-anchored brief triggers (launchd has no timezone field) ·
backup_estate --no-publish · box at ~37% WARN.

**16.** CENSUS-2A COMPLETE (2026-08-12, 8 runs, contract SPENT — queue item sha 5cd8ab57…, body
b0da051b…): all ten modules run, hash-recorded on D:/Naiad/research_outputs/census2a/ (29
artifacts, 53.3 MB, manifest-pinned). YIELD: ONE supported result of nine scored — P-REL-1b
SUPPORTED-PROVISIONAL (in-window 12/25 presence, +0.19 ATR H100, label permanent, qualified by
panel return correlation 0.6458 / sign-agreement 0.60 — five witnesses ≈ one). NOT SUPPORTED:
P-ARM-1 (exposure-time confound; WALL-at-arming = low displacement, return limb INVERTED), P-iii-b
(short-side straddle by 0.0006; pooled grind effect real ~7pts under the size-free ruler), P-RAT-2
(0/36; exit-alone bounded again; mean-carried 4h/long/wide corners → P-RAT-3), P-NEST-1
(straddles). WITHDRAWN: P-REL-1, P-CHOP-1. NOT SCORED (undefined terms, correctly not invented):
P-VBT-1, P-i′, P-iv′. STRUCTURAL FINDINGS: incumbent loser decile was a position-size selector
(97.5% size-0.5 → A3-DECILE size-free per-asset ruler, validated by R-unit reconstruction);
ride-only book = 12.3% winners × mean +9.4R (every exit study is tail preservation); CEN-9 clock
control: EMA-arming edge over random = +0.052 ATR ≈ TOLL-SIZED (0.026-0.059), and the prior-extreme
sweep-from-below EDGES OUT the armings (+0.0845 vs +0.0748, descriptive); registry co-location RARE
at armings (0.7% of instants ≥2 levels within 0.15×ATR) — the confluence premise unsupported by
this tape's geometry; RESPECTED range verdicts = failed births (1H reclaims preceded by 3× i-b
refusal density); 12H least trap-prone on fixed windows. NEXT-CYCLE SLATE: P-ARM-2 [55] · P-FAN-1
[60] · P-NEST-2 [50] · P-RAT-3 [40] · P-VBT-2 [45] · P-CHOP-2 (ribbon operand = OR{30m,1h}) ·
P-REL-1b replication; needs-word first: leap/stair window recut, ATR buckets, H-VBT band rule.
CLOSE-OUT + handoff + analysis architecture: exchange/reports/CENSUS2A_CLOSEOUT_2026-08-12.md (the
future-Apollo pickup document). Path forward = operator's word: EngineV2/Tier-C design interview
and/or the Rotation Program funnel (now a statistical mandate — the panel votes 65% together).

**17.** 2026-08-15 box + lane state: BOX_BYTES raised 6.39→16 MB (warn 50%/refuse 80%) by operator
ruling ["box"]; exchange/ at 16.05% OK; rotation (queue 003, ~08-28, AGE_DAYS 30) unchanged.
Box/publish-guard governance HANDED TO ATHENA (NOTE_APOLLO_2026-08-15_TO_ATHENA_box-governance-handoff.md)
incl. the D3 metering gap and the new named-constant change protocol (grep NAME+VALUE+THRESHOLD-TEXT;
pin-vs-import per dependent; historical regenerators stay pinned — the BOX_CAPACITY lesson, commit
905b9fc). APOLLO lane cleared for SSv12 only: ① TIER-C2 BASELINE paste standing/unrun (pure-4h four
stones, clean corridor 2024-07→2026-01, yardstick + Q6 rule) ② VIZ-3 Trade Cathedral contract NEVER
ARRIVED on builder machine (V-10) — re-attach to any builder message, files in one pass ③ read
ORACLE grid + W-E+ cards → Pine v12.0.2 per-lens mark defaults; check 837-vs-814 winner-count
scope. Rulings given: ["stations"] six-station lifecycle w/ ORACLE-class mapping = station canon
v1; ["fuzz"] = the named-unruled [VETO] set {d floor, LPS-holds, tranche cap, whisper params},
register-carried UNRULED. Cathedral payloads emitted F-V3 PASS (DESIGN_HANDOFF_VIZ3, 7 files,
local).

*— end of capture. 17 entries, ~19 KB of steering input; the synthesis above is what they currently
produce. Redesign target: 13 entries, ~6 KB, zero internal contradictions, every entry ending in a
pointer with a section token.*
