# Naiad project memory — VERBATIM PRINT, 2026-08-06

## Provenance

Read directly from the project memory panel by ATHENA on 2026-08-06 through the memory
editing interface. `[verified — read this session]`

**Entry count at capture: 20.** Panel cap: 30 entries, up to 100,000 characters per entry.
Account memory experience: LEGACY (Settings → Capabilities → Memory).

**This is a verbatim print.** Entry text is reproduced exactly as the panel returns it,
including its own internal corrections, retractions and warning marks. Nothing has been
summarised, tidied or reordered. Numbering is the panel's own ordering.

## What this file is for

Project memory lives only in Anthropic's cloud. A global **"Reset memory"** permanently
deletes project memories too, and cannot be undone. Whether memory appears in an account
data export is **disputed** — current Anthropic support documentation says all memory data
is included; our own entry #12 previously said it was not, and the resolution is empirical
(inspect the first real export, ticket T-6).

Either way, a verbatim snapshot is the copy under **our** verification standard: versioned
in git, hash-pinned, covered by `backup_estate.py --workflow`. It is also the restore path,
because Anthropic's memory-import flow is extraction-based and lossy — a summary cannot be
restored, only re-read.

## Snapshot lineage

| file | holds |
|---|---|
| `docs/memory/claude_project_memory_2026-08-03.md` | OPENING — all 30 entries as they stood before the 2026-08-03 restructure |
| `docs/memory/claude_project_memory_2026-08-03_CLOSING.md` | DELTA — what changed, plus the auto-generated summary layer |
| `claude_project_memory_2026-08-04_CURRENT.md` | the 23 entries live after the restructure |
| **this file** | the **20** entries live on 2026-08-06 |

**Note on the count.** The 2026-08-04 audit took the panel to 17 entries. It now stands at
20: entries 18, 19 and 20 were added by the APOLLO lane between 2026-08-05 and 2026-08-06
(re-priming, the SEQ8/W-F1 state, THE PATH rulings, and the MC programme). Memory is shared
across all three web lanes, so it grows from whichever lane is working.

## Restoring from this file

Paste an entry's text back through the memory panel, or use
Settings → Capabilities → Memory → Start import. The import flow is experimental and
extraction-based, which is why this capture is verbatim rather than summarised.

---

## The 20 entries, verbatim as read 2026-08-06

### Entry 1

Reporting methodology ratified 2026-07-15: Ludwig wants major deliverables written like "V3 Forensics Report 2 — The Deep Dive" (zero-context plain-language explainer: explains the whole project and every mechanism from scratch, defines all jargon, states provenance per claim as verified/measured/expect/open, ends with an options menu + reasoning). Reason he gave: it lets him verify the work conceptually despite no programming background and while running other work in parallel, without holding full project context in mind. Use this format for future reports.

### Entry 2

Guiding attitude ratified 2026-07-15 (Ludwig's step-3 reply, his words): "the alpha is buried under a pile of debris. We explore, examine and implement a continuous improvement mindset to achieve our goal." Concretely: (1) Do NOT retire underperforming mandates — stay with the problem, turn it around, look at it from different angles until the mechanism becomes apparent, then fix it. Rejected the reviewer's "retire intraday and position, go swing-only" proposal. (2) Do NOT reorder work around fees/execution before the strategy has demonstrable edge — "if we rework the system around fees we are left with a system whose complexity isn't translating into profit, making the whole project pointless." Limit-chase/fee work is agreed but comes AFTER. (3) Priority order: better trade-birth filters → effective stop ratchet → working take-profit that keeps profit. Apply this attitude when proposing next steps.

### Entry 3

Fractal lens adopted as standing analytic frame (operator, 2026-07-18): price structure may replicate across timeframes with scale-adapted variables; never treat cross-TF signals/outcomes as "different animals" by default — study the correlations explicitly and falsifiably. Working hypothesis from existing evidence: GROSS behavior is fractal-compatible; NET behavior is provably not, because costs are scale-invariant in bps (toll 14/20/30) while excursions scale with TF (median MFE 10/28/57 bps intraday/swing/position) — "same animal, different tax bracket." TC-5 (1H/5m re-spec) = fractal repair. Falsifiable program FH-1..FH-5 (signal-shape invariance, R-space anatomy invariance, relative-vs-absolute MTF gradient discriminator, cross-mandate nesting census, rule transfer) rides RC-7 mostly free. FH-3 discriminator gates C13b's form (absolute 30m vs "two steps below governor"). Cross-scale replication added as an S-2 detector robustness bar. Reporting convention: all MTF tables print absolute AND governor-relative coordinates.

### Entry 4

STUDY FINDINGS THROUGH 2026-07-20 — conclusions and standing rulings (merge M-2, operator-approved 2026-08-02; absorbs the RC-7, RC-7r, S-2, S-2b, TC-5 and S-3 entries, plus former #4 TC-4 and the SS definition from former #12. FULL NUMERIC DETAIL LIVES IN LEDGER.md — memory keeps only what changes design). WORKING SS DEFINITION (operator, 2026-07-29; pending refinement): scrutinize the multiTF clustering of signals from the 9/89/200 EMA trio — a "signal" = a meaningful EMA/EMA interaction (cross OR test-and-rejection) or EMA/price interaction (test-and-bounce / SFP-like, or an S/R level break-and-flip) — to read range-vs-breakout, locate the meat of the move, and derive ideal entry/exit points; {9,89,200} and {12,25} are measured jointly AND separately, comparing sequential cross-cascade "fingerprints"; range detection is fractal across TFs; v12 entries are gated by "areas where I want to do business" (range extremes; RESPECTED vs DISRESPECTED range verdicts) to kill the stillbirth population. PRIOR CONCLUSIONS ARE POINTS OF CONTROL, NEVER MANDATORY DESIGN CONSTRAINTS — including the "entry combinatorics don't tilt raw price" macro-ruling; v12 maintains critical awareness of inherited v10/v11 entry and add assumptions. ARCHITECTURE OF RECORD: the STRUCTURAL STOP (anchor beyond a real 1h swing pivot, not a fixed distance) is the dominant architecture — grid +565.58, strip-best +301.99; 52% of 4,167 shakeouts convert to ≥1R; SWING flips positive-and-robust for the first time (+384.82/+250.62); struct-30m dropped as a lottery (strip-best sign-flips). FALSIFIED, and memory must not re-assert it: the TWO-LINE exit architecture was ratified 2026-07-19 and killed by S-2 (grid −121.71 vs band [500,650]) — the live native ratchet almost always fires first and the trail clips engaged winners; ranking F4-structural ≫ corrected fold-in ≫ baseline ≫ two-line. D-2 re-ruled to the TC-1 2x2 factorial {stop: bar-extreme vs structural-1h-static} × {exit: none vs gov-e200 trail}, implemented in the TRADING layer so signals.py stays byte-untouched. THE HARVEST PROBLEM (priority #3, still unsolved): PROTECTED paradox inverted in cause — 100% zero-stop-advance under struct because the stop protects by WIDTH not advance; 73.67% of trades reaching +1R still reverse to gross loss; median peak-capture −43.65% WORSE than baseline. The winning book wins by RIDING TO REGIME BREAK (opposite_cross 719@67%, failure_x 1352@24%), never by managing individual winners. ADD GATE: P-CIRC confirmed emphatically — 97.59% of 3,114 +1R winning moments had NO pullback-reclaim PRIME available; the gate is frozen exactly when adding is wanted (circular topology). ENTRY ADMISSION: P-F1 vindicated the biggest gate (229,963 no_zone rejects would run −1.205/unit vs −0.577 admitted); D3 confirmed — 8 conditions negative on S-1 flip POSITIVE on the struct book, slow TFs strongest (1d s2_aligned +0.229); P-LAT falsified both halves; C14 nesting, F8, and the detectors as entry signals all retired. FRACTAL PROGRAM: FH-1/FH-2 falsified; FH-3 CONFIRMED RELATIVE — the MTF gradient is governor-relative, not absolute-TF, so all cross-TF rules are expressed in governor-relative steps. TC-5 (1H/5m): mechanical fractal CONFIRMED (mfe/toll 0.65→1.79) but economic claim FALSIFIED (net −1,180.32) — intraday's disease is SIGNAL as well as scale; entries floored at 5m PERMANENTLY; survival-matched recount showed PRIME density 4.2× sparser and the apparent birth-count rise was purely a survival artifact; TC-2 (re-entry quality bar) inherits the mandate. TC-4 ORIGINS (2026-07-17, from former #4): equity floor = 25% of initial equity per cell, permanent halt on breach; the 1H/5m intraday re-spec was pre-registered here and became TC-5; the 3-strike rule was RESCOPED to a quality bar on re-entry fills (a re-entry = a fill with nothing open), which is TC-2 — re-entries carry ~83% of clean-grid loss via cost-in-R = cost_bps / stop_bps, and add-signal drought, not the BE gate, is the pyramid's binding constraint. SIZING: every lever is negative first-order on the current book — deferred post-TC-1 and, when it returns, uses pre-registered count-based tiers, never fitted weights. MACHINERY: MTF_SET = {5m,1h,4h,12h} — the engine has NO 1D and NO 30m anywhere; tf_align = one MTF_SET rung below governor, which makes the 1H/5m mandate a COMPRESSED confluence ladder. STANDING RULE from F-ENG: completion commits must contain every file required to reproduce the run from a clean checkout.

### Entry 5

PASTE CONSTRUCTION — routing, purpose, environment assertion, file disposition (2026-07-26/27, extended 2026-08-02/04; absorbs former #7). PURPOSE STATEMENT (operator, 2026-08-04, binds EVERY lane): every paste presented to the operator is preceded by a plain-language explanation of WHAT IT IS FOR and WHAT IT DOES — why it exists, what each numbered step does and why, what he should see on screen, and what changes on disk when it finishes. He is not a programmer and must never be asked to run something whose purpose he has to infer from the code. Full text in CONVENTIONS.md §2.0 so it reaches HERMES, DIONYSUS and HEPHAESTUS, who cannot see memory. ROUTING: every paste-go carries an operator-facing routing line ABOVE the code block ("→ WHERE TO PASTE: your LOCAL Claude Code — the PowerShell window on your PC", or an explicit cloud target), an in-paste ENVIRONMENT header, a ROLLBACK line stating exactly how to undo it or that it cannot be undone, and a HARD environment assertion that halts before ANY write — READ-ONLY pastes included, because a misrouted read paste silently returns plausible nonsense. Identity probe when unsure: `git rev-parse --short HEAD; pwd` — "local" = any session whose probe returns the C:\ (or /c/) Users...OneDrive working-clone path. History: FOUR routing failures on 2026-07-26; the three carrying the ENVIRONMENT label were all halted by it; zero damage. TWO BUILDER ENVIRONMENTS (merged from former #7): PRIMARY = the local Windows Claude Code session holding the working catpatrol/Naiad clone on branch v12-v1-census, which contains the full data estate — ALL study and ops contracts execute here. Ephemeral cloud Claude Code sessions get fresh scoped clones with NO data estate and unpushed commits evaporate at session end, hence R-A': a cloud session must push in the SAME session or the work does not count. Rulings: (R-A) cloud sessions are scoped to catpatrol/Naiad ONLY — Prometheus is deprecated and receives no new work; (R-B) cold archives go to a second cloud via backup_estate.py, dated + hashed, generations accumulate; (R-C) ledger corrections fold into the NEXT entry rather than getting their own. SHELL HAZARDS, each having cost a failed run: a literal backslash cannot be trusted to survive transit — use forward slashes for Windows paths and chr(92) where one is genuinely needed; publish_exchange.py is a LIBRARY MODULE with no __main__ and running it as a script publishes nothing and exits quietly; publish() is path-scoped to exchange/** so a `git add` of anything outside it in the same paste goes nowhere and leaves that file stranded; and a status read through a pipe ending in a formatter (grep ... | sed) always exits 0, so the failure branch can never fire — capture grep's status directly. FILE DISPOSITION TABLE: every build document ENDS with a table covering every file the paste created, modified or moved — full repo-relative PATH · EXISTS on disk · TRACKED/untracked/ignored (and which .gitignore line) · COMMITTED in which short SHA · PUSHED yes/no plus remote ref · PROTECTED BY which backup (estate zip / phase archive / --workflow archive / GitHub only / NOT PROTECTED). Committed ≠ pushed; pushed ≠ backed up; EXISTENCE IS NOT PROTECTION — before removing any file because a copy exists elsewhere, verify that copy is TRACKED and present on the REMOTE, because several directories are git-ignored on purpose and a duplicate living only there is a second copy on the same disk. And "not visible on GitHub" is usually the browser showing the DEFAULT branch (main) rather than v12-v1-census, which carries all project work.

### Entry 6

Estate location + rulings (2026-07-27, all verified): THE DATA ESTATE IS NOT IN THE REPO AND NEVER WAS UNDER ONEDRIVE — it resolves via engine/data.py:39-48 cache_dir() to C:\Users\luisf\AppData\Local\naiad\data_cache (633.5 MB, 71 files; NAIAD_CACHE_DIR unset). census.json records RELATIVE paths only (klines/BTCUSDT_1m.parquet) so it does not pin a location by itself. The pre-2026-07-27 ledger risk note calling the journals "on one machine under OneDrive" was factually wrong for the project's whole life; superseded in commit 268cdf9. Estate now backed up as naiad_estate_2026-07-27.zip (477.6 MB, sha256 f6cc33b404e3c684f927ac16bcd54417eaa969ff7c8e4c0129fff49c174b0960, 71/71 bidirectionally verified) on TWO independent Google Drive accounts. OneDrive outage root cause = QUOTA EXHAUSTION (registry ItemCantFitInQuotaLastShown 2026-07-27T01:05:08Z against a 21.78 GB / 22,731-file tree); fixed by archiving six completed phases (tc1/s3/s2/v3_anchor/s1/tc4) to research_outputs/_archive/*.zip — 21,055.60 MB to 977.70 MB, 13,502 members, 0 mismatches, 19.61 GB net reclaimed, tree now ~1.9 GB; the 25 git-tracked files in those phase dirs were PRESERVED in place (builder halted and asked rather than deleting them). Archives kept locally as the primary copy plus both Drive accounts. RULING R-A' RATIFIED: cloud Claude Code sessions must push in the same session or the work does not count — an unpushed commit in an ephemeral environment is never treated as done. Reviewer error class logged and now guarded: verification gates must be written from the POST-action state, simulated step-by-step before the paste ships (three instances on 2026-07-26/27).

### Entry 7

Multi-chat orchestration + STATUS-block standing instruction (adopted 2026-07-27; merges former #22/#23; lane names per #24; PATHS CORRECTED 2026-08-02). LANES: APOLLO (SSv12, census, Tier-C, Engine V2, SSv12 Pine) · ARGUS (analytics toolkit, daily brief, volume filter) · ATHENA (system resilience, backups, integrity, manifest ritual, workflow — inherits the old SYSTEM lane) · DIONYSUS (independent critique) · HERMES (coordination/verification, Cowork) · HEPHAESTUS (builder). PATH CORRECTION, verified 2026-08-02: claude/ contains EXACTLY ONE file, STATUS_SYSTEM.md — claude/STATUS_ENGINE.md and claude/STATUS_BRIEF.md HAVE NEVER EXISTED; the real lane status docs were untracked .txt files at repo root ("STATUS — ENGINE (…).txt" 2,917 B, "STATUS — BRIEF (…).txt" 3,008 B). This entry previously asserted all four .md paths and was wrong; the builder caught it while seeding the per-lane ledgers and used the real sources with a provenance note. SUPERSEDED BY exchange/status/LEDGER_<GOD>.md per the 2026-08-02 rulings — the per-lane append-only ledgers are now the status layer of record. Chats cannot read each other; the project box, the GitHub sync bus and operator-relayed STATUS blocks are the ONLY bus, and no chat assumes knowledge of another's sessions. STANDING INSTRUCTION: end any MATERIAL session (phase registered/completed, ruling ratified, ledger entry, contract drafted, commit/push, acceptance, halt, incident, go-paste, fixtures reported) with a STATUS block, under 25 lines, no prose padding, machine-relayable: "=== STATUS_<LANE> — <date> ===" then NOW (2-3 sentences) / LAST EVENT (date + one line) / FACTS (max 6 lines, each tagged verified|ledger|ratified|handoff|unconfirmed-live|pending|open) / PENDING (numbered, operator-owned) / NEXT (single action + owner) / "=== END STATUS ===", plus the two Q-8 integers (operator actions this session; files re-ingested to seed). In the ARGUS lane every feature fact also carries its firewall side (display-only vs evidence). Lane attribution is by CONTENT, not chat title — flag it when emitting so the coordinator files events correctly.

### Entry 8

Prime directive (operator, 2026-08-01, verbatim): "Project Naiad's main purpose is to develop consistently profitable trading systems." Secret Sauce is ONE of Naiad's systems, not the project itself.

### Entry 9

Olympian architecture + pause (operator, 2026-08-01, from hand-drawn diagrams + instruction doc): Naiad's AI infrastructure = six roles. APOLLO (this chat: SSv12, census, Tier-C, Engine V2, SSv12 Pine) · ARGUS (all-seeing analytics, daily brief, volume filter) · ATHENA (new; inherits SYSTEM lane; system resilience/sustainability, context compaction, backups, file-sharing simplification) · DIONYSUS (new; creative critique — questions shapes, division of labor, coordination; probably a Cowork task) · HERMES (the Cowork task formerly "orchestrator", renamed; coordination + verification node with visibility across the project; supports Athena on integrity; tracks per-Olympian changes conciliated in shared files) · HEPHAESTUS (the builder = local Claude Code). Apollo/Athena/Argus/Dionysus work in parallel; Hermes spans them. TRADING-SYSTEM DEVELOPMENT PAUSED as of 2026-08-01; highest priority = the ClaudeAI–Ludwig workflow redesign: friction-free, low-ferrying flow via a shared exchange directory in the local repo where all reviewer emissions and builder reports land by convention (proposed: exchange/{contracts,reports,status,drops}; Athena owns the scoping interview, Hermes stewards visibility; operator attention reserved for judgment and gates). Five primer docs drafted by Apollo 2026-08-01 to seed the sibling chats; Cowork surfaces (Hermes/Dionysus) can read/write the local repo directly — web project chats cannot, they share only the project box and project memory.

### Entry 10

Workflow architecture RULED (operator, 2026-08-02, via FUNNEL_DIONYSUS_W1 + FUNNEL_ATHENA_W2 "defaults"). CAPABILITY FACTS: (F2) the Claude project box can SYNC FROM GITHUB — link repo+branch, select folders, one operator "Sync now" click makes every pushed file readable by all web lanes; this is the visibility bus that replaces per-file ferrying. (F4, tested 2026-08-02) SCHEDULED Cowork runs CAN read/list/write the mounted local repo live (falsifies the "scheduled = remote-only" assumption) BUT have NO network egress (github.com/pypi/binance blocked → a scheduled run CANNOT git push) and no OAuth connectors; git availability untested; alarmingly, allow_cowork_file_delete granted deletion UNATTENDED with no human gate → written policy: scheduled lanes get no delete duties. W1 RULINGS: Q-1 C sync bus + DIGEST as an index of pointers (never a re-authored substitute) · Q-2 A auto-push scoped to exchange/** + lane ledgers ONLY, evidence never auto-publishes · Q-3 A per-lane append-only ledgers exchange/status/LEDGER_<GOD>.md, central LEDGER.md stays the evidence ledger · Q-4 A hybrid Hermes 2x/day scheduled + on-demand, and CRONOS = the schedule itself + staleness timestamps (gate W-3 resolved, no seventh agent) · Q-5 builder queue exchange/queue/ with drafting rights APOLLO/ATHENA/ARGUS, operator ratification stamp per item mandatory, Hermes validates+sequences, Hephaestus works the queue · Q-6 A Apollo stays a web chat, HELIOS tripwire on record (spawn only if Hermes misses two consecutive cycles or verifies his own executions) · Q-7 A CHALLENGE answer debt is non-blocking, written answer due by next phase boundary · Q-8 A every session-end STATUS carries two integers (operator actions this session; files re-ingested to seed), Hermes tallies weekly. W2 IMPLEMENTATION (Athena, all defaults): exchange/{status,queue,reports,drops} + DIGEST.md, a queue item IS a full contract carrying its RATIFIED stamp (no separate contracts/ folder); text-only 1 MB per-file cap, bigger artifacts referenced by path+sha pointer; _reviewer_box migrates but STAYS LIVE during transition with a routine sweep so in-flight Apollo/Argus pastes never break; manifest v1.1 (round-trip F-M1, config --get, onedrive_running, ledger_head, lane_last_commit, queue_open, BOX_DIR); publish step is path-scoped and staged-set-verified so evidence physically cannot ride an auto-push; Task Scheduler arms daily routine 07:00 and weekly backup Sunday; retention = keep newest 4 estate generations + 1 phase set, REPORT-only, never auto-prune. Operator's recurring floor by design: drops + one Sync-now click + verdicts.

### Entry 11

REVIEWER ERROR TAXONOMY — three classes, each with its cure (named 2026-08-02; the reviewer's mistakes are almost never analytical). CLASS A · MACHINERY-CONTENT: asserting what the code or data contains without reading it. Instances: census.json records RELATIVE paths and cannot pin a location; tf_align=15m specified when 15m is not in MTF_SET; "estate under OneDrive" false since Phase 0; claude/STATUS_ENGINE.md and STATUS_BRIEF.md specified when they have never existed. CURE: the custodian context-gap audit moves into CONTRACT DRAFTING, not after; any §Basis claim must cite a file actually read this session. CLASS B · POST-ACTION-STATE: writing a verification gate from the state before the paste's own actions run. Instances: a five-file deletion list against an empty-worktree gate when ten existed; a ledger entry asserting 18/18 while the same paste restored a file making it 19/19; a phase-directory deletion contradicting its own expected-EMPTY git status; .gitignore covers *.zip but not the .sha256 sidecar, so writing it legitimately failed F-K3. CURE: simulate the paste's own steps in order and write every gate from the POST-action state before shipping. CLASS C · PROVENANCE: promoting an operator or builder report to [verified]. Instance: a STATUS block tagged a Google Drive folder [verified] when the source was an interview answer and the folder did not exist. CURE: operator-reported is [operator], builder-reported is [handoff]; [verified] means computed or read this session. COUNTER-PATTERN, and it is the system working: the builder has refused a reviewer instruction on a ratified invariant at least six times — deleting git-tracked files, committing raw candles against charter §10, rewriting an allowlist into a Bash-breaking backslash form, appending false ledger history in a misrouted clone, treating a push authorization as transferable across environments, and throwing away real lane history to honour a wrong filename. A stack whose executor never pushes back is a stack whose reviewer errors all land.

### Entry 12

BACKUP ARCHITECTURE — three tiers (Athena 2026-08-02/03; corrected 2026-08-03 and again 2026-08-04). PRINCIPLE: sync is NOT backup — OneDrive, Google Drive and Dropbox all propagate deletions and corruption faithfully; version history is a retention window, not durability. The architecture is dated, non-overwriting, hash-pinned archives across independent providers. TIER 1 · DATA ESTATE (irreplaceable): C:\Users\luisf\AppData\Local\naiad\data_cache via engine/data.py cache_dir(), ~70 files / ~653 MB; backup_estate.py --estate writes dated generations to G:\My Drive\naiad-backups plus a browser-only second Drive account that is NOT machine-verifiable (date recorded by hand in exchange/status/SECOND_ACCOUNT.md; a missing date line is itself an alert). TIER 2 · REPO: GitHub is the durable copy of everything tracked; phase archives in research_outputs\_archive\*.zip are PERMANENT EVIDENCE and NEVER prunable on a retention rule — corrected 2026-08-03, the old keep-1-phase-set rule would have named 1,025 MB of unique study evidence as deletable and it lived in TWO scripts at once, so each restated the other and looked corroborated. TIER 3 · THE AI OPERATING SYSTEM: ⚠ CORRECTION 2026-08-03 — this entry previously asserted that project memory is NOT included in Anthropic data exports. Current support documentation states the opposite (all memory data IS included in exports) and documents a dedicated verbatim export path plus an experimental import. Resolution is EMPIRICAL: inspect the first real export (ticket T-6). Regardless of that outcome, docs/memory/ snapshots remain the copy under OUR verification standard — versioned, hash-pinned, --workflow-covered — and the restore path, because the import flow is extraction-based and lossy. User preferences, project instructions and custom styles are cloud-only and appear in NO export; hand-paste into docs/primers/OPERATOR_PREFERENCES.md. A global "Reset memory" permanently deletes project memories too, irreversibly. VERIFICATION STANDARD (all modes): bidirectional per member — bytes re-read OUT of the archive must hash equal to a fresh read of the source AND every embedded manifest pin must agree with both — plus set membership cross-checked archive/manifest/disk, plus F-K5 re-reading the archive FROM the destination after write, plus a no-clobber guard (--force-same-day permits same-day re-runs under a -NN suffix, never overwriting; earlier-day targets still refuse). RESTORE HAZARD, verified on tc5 and s3: archives embed MANIFEST.json at their root while some phase folders carry manifest.json — on case-insensitive Windows extraction SILENTLY OVERWRITES one with the other. Restore case-sensitively or rename one entry first. RETENTION: keep newest 4 estate + 4 workflow generations; phase archives never prunable; the scripts REPORT and never delete. TRIGGERS, verified 2026-08-04 by enumerating Task Scheduler: daily routine 07:00 ARMED · estate backup Sundays 08:00 ARMED · WORKFLOW backup Sundays 08:30 ARMED since 2026-08-02, state Ready, never yet fired (first fire 2026-08-09). ⚠ RETRACTION 2026-08-04: an earlier version of this entry asserted the workflow trigger was NOT ARMED. That was FALSE and is withdrawn. The finding came from querying only the estate task by name and inferring the other's absence instead of listing the tasks — a Class A error that then propagated into CADENCE.md, CONVENTIONS.md, a session report and two ledger entries before being caught by the builder. What WAS true and stays true: the newest workflow archive was dated 2026-08-02 and genuinely predated the 08-03/08-04 files, so those files had no off-machine copy until a hand run on 2026-08-04. The gap was real; the cause given for it was not. LESSON, now standing: to claim a thing does not exist, ENUMERATE the set — a negative result from a single lookup is not a measurement of absence.

### Entry 13

SURFACE-ACCESS MAP + THE BOX BUDGET — who can see what, and what it costs (Athena 2026-08-03, extended 2026-08-06). ⚠ THE FINDING THAT COST SEVEN TURNS: when a project is in SEARCH MODE, GitHub-synced repo files are INDEXED FOR RETRIEVAL, NOT MOUNTED. Listing the mounted project directory shows ONLY flat hand-uploads and will report a working sync as broken. THE CORRECT TEST IS THE PROJECT-KNOWLEDGE SEARCH TOOL, never a directory listing, and a file is confirmed visible only when it returns WITH a folder path — a bare filename may be a hand-upload and proves nothing. WEB CHATS (APOLLO, ARGUS, ATHENA): share the box and project memory; CANNOT read each other's conversations, CANNOT touch the local disk, CANNOT run on a schedule. The box is their ONLY access to repo content. COWORK (HERMES, DIONYSUS): read/list/write the mounted local repo directly and run code in a Linux sandbox; they do NOT need the box and do NOT see project memory. Scheduled Cowork runs see the live folder but have NO network egress (github/pypi/binance blocked, so no git push, no market data) and no OAuth; STANDING SCHEDULED-LANE NO-DELETE POLICY, because allow_cowork_file_delete granted deletion unattended with no human gate. HEPHAESTUS: local, sees everything, commit-no-push by default, auto-push scoped to exchange/** only. THREE ACCESSES THAT ARE NOT THE SAME: trusted Cowork folders reach Cowork lanes only; a file dragged into a chat lives in that ONE conversation and never enters the box; project knowledge / GitHub sync is the only path reaching every web lane. THE BOX BUDGET, measured 2026-08-05 two independent ways (LEDGER.md 255,011 B = 4%; s3_excursion_substrate.jsonl 6,007,227 B = 94%): total capacity ≈ 6.39 MB, and ticking in the sync picker is per-file with a live percentage beside each. THE DISCRIMINATOR: the box is for DISCOVERY, not access — tick a file only if a lane must find it WITHOUT KNOWING IT NEEDS IT. Anything a lane knows it wants, the operator drags into that one chat at zero permanent cost. THE LAW, confirmed three separate times: DOCUMENTS ARE CHEAP, DATA IS NOT. Every prose artifact ARGUS ever wrote totals ~770 KB (~12%); two capture JSONs from a single 2026-08-06 cycle totalled 4,024,198 B (~63%). Write more reports, never fewer; never let a capture, render, results JSON or substrate into exchange/ — reference it by path + sha256 pointer per CONVENTIONS §4.2 (text only, 1 MB per file). FLAGGING RULE (operator, 2026-08-06, binds EVERY lane): creating a file that will enter the box is a WORKFLOW DECISION, not a side effect. Every build document's file-disposition table carries a BOX COST column — bytes and % of the 6.39 MB budget — and any artifact over ~1% is flagged for the operator with its intended home stated. Standing tick set as of 2026-08-06: LEDGER.md + exchange/ ONLY; every other file is requested per lane, per need.

### Entry 14

DECISION-FUNNEL INTERVIEW — the ratified format for presenting data and context-informed decisions to the operator (operator, 2026-08-02, reconfirmed verbatim 2026-08-03; absorbs former #13; supersedes any compressed-register format). Ludwig has no technical background. Every gating decision, at every step, is presented as a decision-funnel interview with detailed context: (1) WHAT IS IT — remind him what this is in plain language, assuming zero context loaded; (2) WHY DOES IT MATTER — why we are doing it, what breaks or improves, and why it needs HIS attention specifically rather than the reviewer's; (3) OPTIONS — every actionable option, including the reviewer's suggested path; (4) IMPLICATIONS — clear consequences for and against each option. The reviewer's RECOMMENDATION must be APPROPRIATELY SUPPORTED by evidence actually in hand; where the reviewer is uncertain, it must say so explicitly, explain further, and REQUEST GUIDANCE rather than manufacture a confident default. Questions must be self-contained — never reference earlier scrollback. Big questions first, details cascading below (funnel shape, HTF→LTF). One-line answer affordances (defaults + exceptions by number) remain welcome, but context richness is never traded for brevity. COMPANION RULINGS (operator, 2026-07-29, merged from former #13): "KEEP EVERYTHING MOVING THAT CAN BE MOVING" — all independent workstreams run in parallel, always, and builder idle time during reviewer↔operator sessions is a cost to design against; and at every completed builder work cycle the builder DISPLAYS ON SCREEN the exact filenames the operator must carry plus each one's full repo path, with the filenames highlighted in BRIGHT COLOURS.

### Entry 15

Rulings 2026-08-03 (operator: "forensics / discriminant / write synthesis / scoping interview pre-Atlas-feedback"; SS work RESUMED in APOLLO): (1) RESEARCH INVERSION adopted — condition on outcome, discriminate at birth: exhaustive scrutiny of big winners vs losers; the loser book is TWO species with opposite cures (born-dead births → entry gates; died-of-neglect, the 73.67% of +1R-touchers that round-trip → harvest fixes). (2) DISCRIMINANT FRAME + TAIL-RETENTION GAUGE ratified as MANDATORY census deliverables: discriminants must be decision-curtain-clean, five-asset sign-replicated, held-in-time stable, BH-FDR q=0.10 within each discriminant family; TRG = every candidate filter/gate reports the fraction of the unfiltered book's top-decile R it retains, printed beside strip-best (a filter that lifts expectancy but keeps 40% of the tail is condemned). (3) W-F1 "Winner Forensics" commissioned [RATIFIED "forensics"]: Tier-A top-vs-bottom-decile profiling of the 7,094 resolved S-3 tranches (join (cell,tranche_id); r1=3,825 primary, v=78 excluded+counted, re_entry=3,214 secondary; stopout = exit_reason∈{stop,stop_gap} = 5,016) including the first funding_cum realized-funding read; queue item in exchange/queue/. (4) FOCUS: a novel interpretation of SEQUENTIAL patterns in the multiTF cascade; SEQ scoping interview issued BEFORE the operator's Atlas feedback, presenting both Atlas v2.1 and Cascade_Rewire findings. (5) Cascade_Rewire.html = DIONYSUS's first artifact (memo signed "Dionysus → APOLLO"). Its discovery: the LEAP is the dominant activation mode of the 4h+ tier in PURE COUNTS with no outcome data — 66% of 4h arrivals jump directly from the fast tier (≤30m) while 1h is a stair rung (75% adjacent arrivals) — so the operator's motif family is structurally motivated, not outcome-fished. Six-item DATA REQUEST logged (per-cascade event stream with MAE + curtain-cut flags; slow-step timing; per-lens AND per-asset sequence tables; curtain-cut outcomes; depth-complete rung-strings; MAE per terminus); consolidated extract = census 2a substrate candidate. SS Reassessment Synthesis 2026-08-03 filed to exchange/reports/ (reaches the box via GitHub sync — no hand-uploads, box at 92%).

### Entry 16

SESSION-START POINTER + THE SINGLE BUILD DOCUMENT (operator rulings G-4 a 2026-08-03, single-document 2026-08-04). (1) POINTER: at the start of every session in this project, read exchange/status/CONVENTIONS.md. It is AUTHORITATIVE for paste construction (routing line, environment assertion, exact paste-ready text, post-action gates, ROLLBACK line), reporting format, file handling, lane etiquette, and the manual-task instruction rule. It is also the ONLY instruction surface reaching all six actors: project memory does NOT reach Claude Cowork, so HERMES, DIONYSUS and HEPHAESTUS have never seen a memory entry. It carries the FULL TEXT of the behaviours relocated out of memory on 2026-08-03 with exceptions and reminders intact, a MOVED-FROM-MEMORY register with probe coverage, and a plain-language maintenance guide. Verbatim memory snapshots under docs/memory/ (opening 30-entry, closing delta + auto-generated layer, and the current-state capture). (2) SINGLE BUILD DOCUMENT — operator ruling 2026-08-04, supersedes the former two-artifact rule: every builder session emits exactly ONE document, not a separate Builder's Report and Session Summary. Document proliferation was the problem; the content is unchanged. It serves BOTH purposes at once — the forensic record of the work AND the review artifact for the Pantheon and the operator — so it carries: gate and probe values as printed, the full fixture transcript, full result tables not summaries, versions/hashes/commits, findings reported-not-fixed, what was decided and on whose authority, what was found that changes the plan, what remains open with its owner, the honest next options with implications, and the six-column file-disposition table (path · exists · tracked · committed · pushed · protected by). Written as a FILE, never screen-only, plain language for a non-technical operator, zero prior context assumed, no jargon undefined. A file never contains its own sha256. (3) REACHABILITY RULE: a repo file is confirmed visible to a web lane only when a project-knowledge search returns it WITH a folder path — a bare filename may be a hand-upload and proves nothing. The GitHub sync does NOT index the repo ROOT: 82 tracked files / 7.8 MB / 61 markdown documents live there, including LEDGER.md, every study RESULTS doc and builder contract, SS_Cascade_v11_0_2.pine and s3_excursion_substrate.jsonl — committed and pushed but invisible to APOLLO, ARGUS and ATHENA. Anything a web lane must read belongs under exchange/ or docs/. (4) If CONVENTIONS.md and a memory entry ever conflict, do not choose silently: say so, quote both, and ask the operator.

### Entry 17

PHASE-ARCHIVE PROTECTION — settled and verified 2026-08-04/05. STATE OF RECORD: all 9 phase archives (1,043,691,544 B) exist in TWO places — locally at research_outputs/_archive/ (git-ignored by .gitignore:38 *.zip) and on G:/My Drive/naiad-backups/phases/ — and every one was HASH-verified byte-identical between the two, zero mismatches. That was the first true integrity check of the estate; earlier passes compared file SIZE only, which catches truncation but not corruption. FINGERPRINTS: all 9 carry a .sha256 sidecar beside the archive on Drive AND a git-TRACKED local sidecar pushed to GitHub (.gitignore covers *.zip but NOT *.sha256 — that asymmetry is the whole design: the archive stays out of git, its fingerprint goes in, so losing the machine still leaves the Drive copies verifiable against GitHub-held hashes). The legacy "naiad (local folder) BACKUP" snapshot on G: has been DELETED; nothing was lost because its six archives were copied to phases/ and hash-verified first. RESIDUAL, unchanged: backup_estate.py --phase writes ONLY inside the repo (line 1030, dest hardcoded) and SILENTLY IGNORES --dest — the flag parses, exits 0 and prints success while writing nowhere near Drive, unlike --estate/--workflow which hard-fail without it. So every NEW phase archive is machine-only by default until queue item 002 (--mirror DIR + --phase --dest becomes an error) ships. ⚠ TWO RETRACTIONS ON RECORD, both the same error class: an ATHENA probe claimed ~995 MB machine-only (wrong by 57x) because it checked ONE hard-coded Drive folder and treated absence there as absence everywhere; and a closing ledger draft claimed all 9 had tracked sidecars when 7 did — the builder measured git ls-files and corrected it before it entered the append-only ledger. CURE, standing for any backup audit: WALK THE DESTINATION ROOT, compare on (name, size) across every location found, de-duplicate with os.path.samefile because case-variant Windows paths read as two places, and never write a [verified] fact you have not measured this session.

### Entry 18

RE-PRIMING 2026-08-05 + SEQ8/W-F1 STATE (APOLLO; sources: Dionysus note + SEQ8 builder report + WF1 tables + ARGUS pantheon report, all read this session). (a) SEQ8 EXTRACT BUILT: 288,711 raw cross events (7 TFs × BOTH alphabets {9,89,200} and {12,25}, per-event multiTF snapshot); 950,145 cascades as 8 derived views (4 windows × 2 chaining rules); outcome tables + cascade→trade bridge built and EMBARGOED; 8/8 fixtures, byte-identical rerun. Operator's multiTF-sequence meaning CANONICAL: winning patterns ACROSS TIME AND ACROSS TIMEFRAMES (F-SEQ8 proved minable from the raw stream). Tier grammar = DESCRIPTIVE labels only (FAST 5m/15m/30m · STAIR 1h · SLOW 4h/12h · TREND 1d) — no rule force. THE LEAP IS ANCHOR-RELATIVE — cite only with anchor stated ("as seen from a pullback low"); unanchored, 1H feeds 4H 5,736 vs 3,344 from the fast tier; P-SEQ-ii REPLICATES 7/7 in the atlas_star AND direction-consistent frames (5 solid assets + TAO n=2 / JTO n=7 thin; fails in window-chained and 3-rung-truncated frames — all four rows printed). Direction-consistent EPISODE = natural atom (window inert: 24h→1W moves counts by 95/~65,200). 286 trade births inside NO cascade — first-class question when stamps return. Atlas errata: window measured from confirmation bar (+5), monotone 83.01% not 83.6%. EMA-300/450 warmup: 13 asset/TF cells NEVER warm (D6 gate). 1D governor never joins trades. PARKED BY OPERATOR until the SS deep-dive (DIONYSUS hosts, regime question first): promotions i/iii/iv, D3/D4 analysis, four Q1c detectors (unbuilt), {12,25} pairing scope. (b) W-F1 COMPLETE (9/9 fixtures, seed 20260803): 84 Section-A tests → 15 FDR-surviving → 12 CANDIDATES (r1-only book: 20; v-excluded: 11). P-WF2 CONFIRMED (re_entry Δ −0.427 CI[−0.474,−0.381] q=0.0006 → losers); P-WF3 CONFIRMED Z1-ONLY (Δ +0.209; Z2 ≈ 0 — the DEEP zone discriminates, not zones generally); P-WF4 CONFIRMED at ratio 1541.8× (W median hold 128.5h vs L 300s = ONE 5m bar — the born-dead species photographed); P-WF5 CONFIRMED dimensionless (W |funding|/|pnl| 3.5%; position-mandate 10.2%; defect D4 mixed-units owned); P-WF1 PREMISE-FALSE — journals carry NO slow-stack lattice (s2 = 30m/1h det bits + 4h/12h/1d level pivots only; F1/F2/F4 slow-stack bits live in the CENSUS arming-anchor table → tide features reach trades only via the SEQ8 bridge). TRG DEMONSTRATION: NO naive single-feature filter survives (atr_exec_bps TRG 50.6%; fill_class filters ~39.6-39.7%; zone=Z3 65% with NEGATIVE ΔExp) → composite rules only, ever. Exit separation PERFECT: W = 63% opposite_cross + 37% failure_x, ZERO stops; L = 100% stop/stop_gap. MFE at exit: W 1,555 bps vs L 5 bps. (c) ARGUS analytics 1.5.0 CERTIFIED (sha ea5f02f2…; 286 pass/1 skip; oscillators+resample+rolling/anchored VWAP+σ chart-certified vs operator's TradingView; volume_profile family fixture-only BY DESIGN — approximation chip permanent). INTERFACE.md = the contract: causality classes; endpoint_only functions (volume_profile, percentile_rank) must be SLICED not indexed historically; instrument travels with the number (BINANCE:<SYM>USDT.P vs INDEX = separate level, 1.51× collapse tolerance); five-asset scorable panel. Confluence engine live (131-169 levels/asset, dual scoring with/without volume: 6 relocations + 14 refinements, median 0.044 ATR); de-peg layer + hinge (both operator-originated). 8 census candidates routed with deflation gauges; ARGUS priority advice: H-VBT first (aims at harvest problem #3) → CENSUS-1d Phase-0 free proxy (event_price/p0 on 149,802 rows) → H-VDP; σ3 episodes too thin for cross-scale replication. (d) LEDGER_APOLLO 9 days stale — append owed on next paste; P-SEQ-ii builder-report methodology verified this session, ledger-entry text conformance check rides next paste. Chat re-primed 2026-08-05 for the SS definition + screenshot session per operator Instructions (7 items).

### Entry 19

THE PATH + rulings 2026-08-06b (operator, "Replies & next steps"; committed on explicit instruction): Refining Secret Sauce = drafting SSv12/EngineV2's early structure. SEQUENCE: (1) define how SSv12 enters/exits for the next Tier-C → (2) design census-2 to capture exactly the data that Tier-C needs → (3) run census → (4) Tier-C with EngineV2. Understand winning trades to catch more winners and avoid losers (inversion reaffirmed). KISS-REJECT DEMOTED to confluence element (present in the May-26 case, NOT load-bearing; not every arming carries it) — measured, never assumed. TRAP = PO3 SYMMETRY: fake-out/spring ≡ PO3 (range builds → false break traps → liquidation fuel reverses); applies to ENTRIES and MIRRORED to EXITS (failed-continuation + reclaim of prior range = ratchet/TP/close signal). MIRROR PRINCIPLE (C7): exit logic = the inverse, symmetric version of the entry system. STOP RULING: structural-1h = point of control with a LOW BAR (only proves better than the v11 ATR line); the RATCHET is OPEN — operator hypothesis: long-EMA (200/300/450) test-and-reclaims on 15m/1H/4H with a tunable n×ATR cushion as ratchet/add points; any ratchet candidate must beat the ride-only control. SUBSYSTEM CANDIDATES (hypotheses to test, never commitments): S1 scalper 5m-1H · S2 swing 4H-1D · S3 long-cascade hunter 5m→1D (stall-hopping; 30m-1h chop = LTF range whose break = add) · S4 turtle-outlook 200/300/450 on 4H/1D/1W. DOWNWARD FRACTAL: 1m-5m 200/300/450 crosses&kisses as entry/exit unlock; TWAP-style split entries following an LTF long-EMA cascade (cost identity + survivability = census questions). 1m restricted EMA set: 89/200/300/450 only. WARMUP HARD LIMITS (structural, any convention, estate 2019+): EMA450 impossible on 1W (~360 weekly bars) and 1M (~90); EMA300 on 1W marginal-late (BTC only); on 1M only {12,25} can ever warm — the "all EMAs 1m-1M" ask must be pinned to the feasible matrix. MAY-26 PROGRAM (7a-d): define SIMILARITY on curtain-clean birth features FIRST, then find similar setups and read outcomes (never reverse); project full feasible EMA set + analytics pack onto the May-26 4H short; same inspection on the leap-family winners; mini-census+mini-TierC scoped via the MC-1..7 funnel interview (in the synthesis doc); program requires partial embargo lift → stamp decisions ride it. NOMENCLATURE PROGRAM opened (10b): names drawn from the KB cross-map (momentum deep-dive §1); understand the 4H cross's role before renaming "slow tier armed". Momentum doc scrutinized: 5-stage post-trigger framework adopted as the manual's spine; flags logged (early-tightening warning ≡ two-line falsification; BE-moves stay a measured question; oscillator ripple annex-only; partial-then-risk-free ≠ naive BE-trail). SS_SYSTEM_SYNTHESIS_2026-08-06 produced (trade manual + next-Apollo primer + pantheon update) → destined exchange/reports.

### Entry 20

MC RULINGS 2026-08-06c (operator: "proceed with defaults / include 25ema in 1m / produce paste"): All MC-1..7 defaults RATIFIED. Amendment: 1m EMA set = {25,89,200,300,450}. SCOPED EMBARGO LIFT GRANTED — the SEQ8 cascade→trade bridge and D3/D4 outcome tables open for the MC program's named populations ONLY. PROMOTIONS STAMPED & REGISTERED (pre-run, in queue item 2026-08-06_MC1_may26_program_APOLLO.md): P-i leap outcome edge [55%] · P-iii grind↔loser join [60%] · P-iv route-conditioned edge [45%]. CRITICAL EVIDENCE-CLASS RULING (reviewer, binding, stated in the paste): the May-26 2026 episode is LOCKBOX-ERA (post-2024-07-01) — the dossier on it is OPS-CLASS, display-only, built from freshly fetched klines stored OUTSIDE the estate, never scored as evidence, hypothesis-generation only; ALL scored tables (similarity family, outcomes, joins, ratchet substrate, promotions) run on exploration-classic data only, max-timestamp fixture-enforced. Similarity stamps (constants [VETO]): seal-agreement k=6 4H bars · band contact b=0.5×ATR(4h) · trap = ≥2 counter-direction FAST-tier crosses in prior 24h · first-attempt N=20 4H bars · kiss-v0 ε=0.25 δ=0.75 k=10. Ratchet substrate grid: long-EMA {200,300,450} × {15m,1h,4h} test-reclaims, cushion n∈{0.25,0.5,1.0}×ATR, ride-only control printed FIRST. Paste also carries: synthesis doc filing to exchange/reports, LEDGER_APOLLO append (was 9 days stale), P-SEQ-ii ledger-entry text check. Seed 20260806.

---

## End of capture — 20 entries.

**Note on layer 1.** Legacy memory also carries an auto-generated project summary, synthesised
from conversation history, which cannot be edited directly and regenerates on its own. It is
not reproduced here; its substance as of 2026-08-03 is recorded in
`docs/memory/claude_project_memory_2026-08-03_CLOSING.md`.

*This file must never contain its own sha256 — that row would be permanently stale.*
