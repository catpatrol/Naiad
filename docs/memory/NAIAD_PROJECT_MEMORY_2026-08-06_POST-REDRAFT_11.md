# Naiad project memory — CLOSING SNAPSHOT after the 2026-08-06 redraft · 11 entries

## Provenance
Written by ATHENA on 2026-08-06, immediately after executing the ratified redraft through the
memory editing interface. Each entry below is the exact text confirmed by the panel at write time.
`[verified — written and confirmed this session]`

**Lineage:** 30 entries (opening, 2026-08-03) → 23 (restructure) → 20 (2026-08-06 pre-redraft,
`NAIAD_PROJECT_MEMORY_2026-08-06_VERBATIM.md`) → **11 (this file)**. Injected size ~45 KB → ~13 KB.
Nothing lost: every stripped narrative survives verbatim in the pre-redraft snapshot; APOLLO's
lane state relocated to `exchange/reports/SS_SYSTEM_SYNTHESIS_2026-08-06.md` +
`exchange/status/LEDGER_APOLLO.md` + `exchange/queue/2026-08-06_MC1_may26_program_APOLLO.md`
(all three verified reachable on the bus before deletion).

A global "Reset memory" deletes project memories irreversibly; these snapshots are the restore
path (Anthropic's import is extraction-based and lossy — restore by pasting entries back).

---

### Entry 1
THE FIRST RULE (operator, 2026-08-06): be elitist, clever, concise, efficient, and elegant — highest standard of craft, signal-dense, no ceremony; the shortest version that loses nothing. PRIME DIRECTIVE (operator, 2026-08-01, verbatim): "Project Naiad's main purpose is to develop consistently profitable trading systems." Secret Sauce is ONE of Naiad's systems, not the project itself. OPERATOR: Ludwig, discretionary trader, no programming background — every operational instruction plain-language, step-by-step, with expected outputs; major deliverables are zero-context explainers with provenance per claim (verified/measured/expect/open) ending in an options menu with reasoning (ratified 2026-07-15).

### Entry 2
GUIDING ATTITUDE (operator, ratified 2026-07-15): "the alpha is buried under a pile of debris." (1) Stay with underperforming mandates — turn the problem until the mechanism is apparent, then fix it; never retire early. (2) No restructuring around fees/execution before the strategy shows demonstrable edge. (3) Priority: better trade-birth filters → effective stop ratchet → take-profit that keeps profit. History and verbatim quotes: docs/memory snapshot 2026-08-06, entry 2.

### Entry 3
FRACTAL LENS (operator, 2026-07-18, standing analytic frame): price structure may replicate across timeframes with scale-adapted variables — never treat cross-TF signals/outcomes as different animals by default; study the correlations explicitly and falsifiably. Working hypothesis: GROSS behavior is fractal-compatible, NET is not — costs are scale-invariant in bps while excursions scale with TF. All MTF tables print absolute AND governor-relative coordinates.

### Entry 4
STUDY SPINE (findings through 2026-07-20 + rulings 2026-08-03; full numbers in LEDGER.md; SS definition, THE PATH and the MC program live in exchange/reports/SS_SYSTEM_SYNTHESIS_2026-08-06.md). ARCHITECTURE OF RECORD: the STRUCTURAL STOP (anchor beyond a real 1h swing pivot) dominates — grid +565.58, strip-best +301.99; SWING positive-and-robust for the first time; struct-30m dropped as a lottery. FALSIFIED, never re-assert: the two-line exit architecture (S-2: the native ratchet fires first and the trail clips engaged winners). HARVEST PROBLEM (priority 3, unsolved): 73.67% of +1R-touchers round-trip to gross loss; the winning book rides to regime break (opposite_cross 67% / failure_x 24%), never manages individual winners. ADD GATE: P-CIRC — 97.59% of +1R winning moments had NO pullback-reclaim PRIME available (circular topology). ENTRY ADMISSION: the biggest gate vindicated (P-F1); 8 conditions negative on S-1 flip POSITIVE on the struct book, slow TFs strongest. FRACTAL PROGRAM: FH-3 CONFIRMED RELATIVE — all cross-TF rules in governor-relative steps; TC-5: mechanical fractal confirmed, economic claim falsified — entries floored at 5m PERMANENTLY; TC-2 (re-entry quality bar) inherits the intraday mandate. RESEARCH INVERSION (ratified "forensics"): condition on outcome, discriminate at birth; the loser book is TWO species with opposite cures — born-dead births → entry gates; died-of-neglect +1R round-trips → harvest fixes. DISCRIMINANT FRAME + TAIL-RETENTION GAUGE are mandatory census deliverables: decision-curtain-clean, five-asset sign-replicated, held-in-time stable, BH-FDR q=0.10; TRG printed beside strip-best. PRIOR CONCLUSIONS ARE POINTS OF CONTROL, never mandatory design constraints. SIZING deferred post-TC-1; count-based tiers, never fitted weights. MACHINERY: MTF_SET = {5m,1h,4h,12h} — the engine has NO 1D and NO 30m anywhere; completion commits reproduce the run from a clean checkout.

### Entry 5
PASTE RULES (full text CONVENTIONS §2–3; history in docs/memory snapshot 2026-08-06, entry 5): every paste = plain-language purpose statement (what it's for, what each step does, what you'll see on screen, what changes on disk) + routing line ABOVE the code block ("→ WHERE TO PASTE: your LOCAL Claude Code — the PowerShell window on your PC", or explicit cloud target) + ROLLBACK line + hard environment assertion halting before ANY action, read-only pastes included — a misrouted read silently returns plausible nonsense. Identity probe: `git rev-parse --short HEAD; pwd`; "local" = the session whose probe returns the working-clone path. Cloud sessions: catpatrol/Naiad only, must push in the SAME session or the work does not count (R-A'). SHELL HAZARDS, each having cost a failed run: backslashes die in transit — forward slashes and chr(92); publish_exchange.py is a LIBRARY with no __main__ — call publish() exactly per CONVENTIONS §3.4; publish() is path-scoped to exchange/** so a git add outside it strands the file; a status read through a pipe ending in a formatter always exits 0 — capture grep's status directly. Every build document ENDS with the disposition table: PATH · EXISTS · TRACKED · COMMITTED · PUSHED · PROTECTED BY · BOX COST. Committed ≠ pushed ≠ backed up; EXISTENCE IS NOT PROTECTION — a twin justifies removal only if TRACKED and on the REMOTE, verified.

### Entry 6
BACKUP + INTEGRITY (state verified 2026-08-04/05; standard also in CONVENTIONS §8; retraction narratives in docs/memory snapshot 2026-08-06, entries 12+17). SYNC IS NOT BACKUP — sync propagates deletions faithfully. TIER 1 estate: C:\Users\luisf\AppData\Local\naiad\data_cache via engine/data.py cache_dir() — NOT in the repo, never under OneDrive; dated hashed generations on G:\My Drive\naiad-backups plus the browser-only second Drive account (date hand-recorded in exchange/status/SECOND_ACCOUNT.md). TIER 2 repo: GitHub for everything tracked; phase archives research_outputs/_archive/*.zip are PERMANENT EVIDENCE, never prunable; all 9 hash-verified byte-identical laptop↔G:/My Drive/naiad-backups/phases/, each with a sidecar in BOTH places — the local .sha256 is git-TRACKED (.gitignore covers *.zip, not *.sha256; that asymmetry is the design: archive out of git, fingerprint in). TIER 3 AI OS: docs/memory/ snapshots are the restore path (Anthropic's import is lossy); a global "Reset memory" deletes project memories irreversibly; export inclusion disputed → ticket T-6. VERIFICATION STANDARD, binding: bidirectional per member — bytes re-read OUT of the archive hash equal to a fresh read of the source; membership cross-checked archive/manifest/disk; re-read FROM destination after write; no-clobber. RESTORE HAZARD: archives embed MANIFEST.json while some folders carry manifest.json — Windows extraction silently clobbers; restore case-sensitively. RESIDUAL: backup_estate.py --phase writes only inside the repo (dest hardcoded, --dest silently ignored) — every new phase archive is machine-only until queue 002 ships. TRIGGERS (by enumeration): daily 07:00 · estate Sun 08:00 · workflow Sun 08:30, all ARMED.

### Entry 7
LANES + ORCHESTRATION (rulings 2026-08-01/02; history in docs/memory snapshot 2026-08-06, entries 7+9+10). APOLLO — SSv12, census, Tier-C, Engine V2, SSv12 Pine · ARGUS — analytics, daily brief, volume filter (charter CONVENTIONS §5.1) · ATHENA — resilience, backups, integrity, workflow · DIONYSUS — independent critique (Cowork) · HERMES — coordination/verification (Cowork): DIGEST 2×/day, box budget, staleness stamps, inbox routing, queue validation; never re-authors, never self-verifies (HELIOS tripwire) · HEPHAESTUS — the builder (local Claude Code). Chats cannot read each other: the exchange/ bus + the project box + operator-relayed STATUS blocks are the ONLY channels. Queue drafting rights: APOLLO/ATHENA/ARGUS only; every item is a full contract carrying the operator's RATIFIED stamp. Status layer of record: exchange/status/LEDGER_<GOD>.md, append-only — a correction is a NEW entry. Every material session ends with a STATUS block (<25 lines: NOW / LAST EVENT / FACTS tagged verified|ledger|ratified|handoff|open / PENDING / NEXT / the two Q-8 integers); ARGUS facts also carry their firewall side (display-only vs evidence); lane attribution is by CONTENT, not chat title. APOLLO'S LIVE STATE lives in exchange/reports/SS_SYSTEM_SYNTHESIS_2026-08-06.md + exchange/status/LEDGER_APOLLO.md + exchange/queue/2026-08-06_MC1_may26_program_APOLLO.md — read at session start in that lane; lane state never returns to shared memory.

### Entry 8
REVIEWER ERROR TAXONOMY (three classes; instance catalogue in docs/memory snapshot 2026-08-06 entry 11 and CONVENTIONS §6.2 — the reviewer's mistakes are almost never analytical). CLASS A · machinery-content: asserting what code, data, or memory contains without reading it. Cure: the context-gap audit runs during CONTRACT DRAFTING; any §Basis claim cites a file actually read this session — the memory panel counts as a file, call the view; to claim a thing does not exist, ENUMERATE the set — a negative from a single lookup measures that lookup, not absence. CLASS B · post-action-state: writing a verification gate from the state before the paste's own steps run. Cure: simulate the steps in order, write every gate from the POST-action state; gates match distinctive fragments, case-insensitively, never full sentences authored in the same act. CLASS C · provenance: promoting a report to [verified]. Cure: [verified] = computed or read this session; operator-reported = [operator]; builder-reported = [handoff]. COUNTER-PATTERN, the system working: the builder has refused reviewer instructions on ratified invariants nine-plus times — a stack whose executor never pushes back is a stack whose reviewer errors all land.

### Entry 9
SURFACES + THE BOX (2026-08-03/06; incident history in docs/memory snapshot 2026-08-06, entry 13). WEB CHATS (APOLLO/ARGUS/ATHENA) share the box + memory, cannot read each other, cannot touch the disk — the box is their ONLY repo access, and it is INDEXED FOR RETRIEVAL, not mounted: test visibility with a project-knowledge search returning a FOLDER PATH, never a directory listing. COWORK (HERMES/DIONYSUS): read/write the mounted repo, no memory, no network egress on schedule, standing NO-DELETE POLICY. HEPHAESTUS: local, commit-no-push except exchange/** via the guard. A file dragged into a chat lives in that one conversation at zero box cost. BOX ≈ 6.39 MB, and it is for DISCOVERY, not access — tick only what a lane must find without knowing it needs it; standing tick set: LEDGER.md + exchange/ ONLY; everything else requested per lane, per need (DIGEST.md is the catalogue that makes requesting possible). THE LAW, confirmed three times: DOCUMENTS ARE CHEAP, DATA IS NOT — write more reports, never fewer; captures, renders, results JSONs and substrates NEVER enter exchange/ — path + sha256 pointer per CONVENTIONS §4.2 (text only, 1 MB/file). Creating a box-bound file is a WORKFLOW DECISION: the disposition table's BOX COST column prices it, and anything over ~1% is flagged at creation with its intended home stated.

### Entry 10
WORKING WITH THE OPERATOR (rulings 2026-08-02/03, reconfirmed verbatim; full text CONVENTIONS §1). Every gating decision = a decision-funnel interview: (1) WHAT IS IT — plain language, zero context assumed; (2) WHY IT MATTERS — and why it needs HIS attention rather than the reviewer's; (3) OPTIONS — every actionable one, including the reviewer's path; (4) IMPLICATIONS — for and against each. Recommendations only as far as evidence in hand supports; state uncertainty and request guidance rather than manufacture a confident default. Questions self-contained (never reference scrollback), big first, HTF→LTF; one-word answer affordances welcome; context richness never traded for brevity. KEEP EVERYTHING MOVING THAT CAN BE MOVING — independent workstreams run in parallel always; builder idle time during reviewer↔operator sessions is a cost to design against. The builder ends every work cycle displaying the exact filenames the operator must carry, with full repo paths, in BRIGHT COLOURS.

### Entry 11
SESSION-START POINTER: read exchange/status/CONVENTIONS.md FIRST — AUTHORITATIVE for paste construction, reporting, file handling, lane etiquette, the manual-task rule; it opens with THE FIRST RULE and a FIND IT FAST index. It is the ONLY instruction surface reaching all six actors: memory does NOT reach Cowork — HERMES, DIONYSUS and HEPHAESTUS have never seen a memory entry. SINGLE BUILD DOCUMENT (operator, 2026-08-04): every builder session emits exactly ONE document — forensic record AND review artifact — gate values as printed, full fixture transcript, full result tables, findings reported-not-fixed, decisions with authority, open items with owners, options with implications, the disposition table with BOX COST; a file never contains its own sha256. REACHABILITY: a repo file is visible to a web lane only when a project-knowledge search returns it WITH a folder path — a bare filename may be a hand-upload and proves nothing. Verbatim memory snapshots: docs/memory/ (newest 2026-08-06, the 20 pre-redraft entries). If CONVENTIONS and a memory entry conflict: say so, quote both, ask the operator.

---
End of capture — 11 entries. This file must never contain its own sha256.
