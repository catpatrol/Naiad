# Naiad project memory — verbatim snapshot, 2026-08-03

## Provenance
Read directly from the project memory panel by ATHENA (reviewer session, 2026-08-03),
via the memory editing interface — [verified this session]. Entry count at capture: 30
(panel cap: 30 entries; up to 100,000 characters per entry). Account experience at
capture: LEGACY (Settings > Capabilities > Memory), confirmed by operator screenshot
2026-08-03. Supersedes docs/memory/claude_project_memory_2026-07-26.md (21 entries),
which is retained, never deleted.

## Why this file exists
Project memory lives only in Anthropic's cloud. Whether it appears in account data
exports is DISPUTED (current support documentation says all memory data is included;
our entry #24 says it is not; resolution = inspect the first real export, ticket T-6).
Either way, this file is the copy under OUR verification standard: versioned in git,
covered by backup_estate.py --workflow, and restorable via Anthropic's experimental
memory-import flow (Settings > Capabilities > Memory > Start import — extraction-based
and lossy, which is why this capture is verbatim). A global "Reset memory" permanently
deletes all memories INCLUDING project memories; this file is the recovery path.
Convention per docs/memory/README.md. This file must never contain its own sha256.

## The 30 entries, verbatim as read

### Entry 1
SS PINE DISPLAY BACKLOG (merge of former #1/#2; APOLLO's lane, display-only, no study impact). B-1 "SWING MODE" (operator, 2026-07-10): make 12H governor/regime signals printable in the SS Pine indicator — v11's tfGovern input defaults to 240 and drives all regime tint/triangles, and the 12H cascade layer is suppressed when chart TF = 720; exact semantics (mandate presets vs un-suppressing the native-TF layer) to be pinned at SSv12 spec time. B-3 (operator, 2026-07-11): in v11.0.2 grade text prints only on R1 PRIMEs while all R2+ adds render as identical unlabeled tiny circles (A+ indistinguishable from B); chartered as SSv11.3 — grade-differentiated glyphs (VR-A Option 3, VR-B defaults ratified) plus playbook erratum corrections for E-1 (faint-tint/provisional Z2 entries are real in v11.0.2; code revert deferred to a v12 named-variant slot). Parity charts stay pinned to v11.0.2 until 3C closes. Both items are inherited requirements of the SSv12 Pine deliverable.

### Entry 2
Reporting methodology ratified 2026-07-15: Ludwig wants major deliverables written like "V3 Forensics Report 2 — The Deep Dive" (zero-context plain-language explainer: explains the whole project and every mechanism from scratch, defines all jargon, states provenance per claim as verified/measured/expect/open, ends with an options menu + reasoning). Reason he gave: it lets him verify the work conceptually despite no programming background and while running other work in parallel, without holding full project context in mind. Use this format for future reports.

### Entry 3
Guiding attitude ratified 2026-07-15 (Ludwig's step-3 reply, his words): "the alpha is buried under a pile of debris. We explore, examine and implement a continuous improvement mindset to achieve our goal." Concretely: (1) Do NOT retire underperforming mandates — stay with the problem, turn it around, look at it from different angles until the mechanism becomes apparent, then fix it. Rejected the reviewer's "retire intraday and position, go swing-only" proposal. (2) Do NOT reorder work around fees/execution before the strategy has demonstrable edge — "if we rework the system around fees we are left with a system whose complexity isn't translating into profit, making the whole project pointless." Limit-chase/fee work is agreed but comes AFTER. (3) Priority order: better trade-birth filters → effective stop ratchet → working take-profit that keeps profit. Apply this attitude when proposing next steps.

### Entry 4
TC-4 design rulings (2026-07-17): (1) G-8a equity floor = 25% of initial equity ($2,500 on $10k) — permanent cell halt on breach. (2) Intraday bps problem: guards this cycle PLUS pre-register a 1H/5m intraday re-spec (5m exec instead of 1m) as a named Tier-C candidate, run after S-1. (3) 3-strike rule RESCOPED: no count cap; instead a quality bar on re-entry fills (re-entry = fill with nothing open), design informed by S-1, implemented as TC-2. Context: RC recompute found mc=3 pyramid cohort is the study's first positive cohort (+4.88 expectancy, 72.6% win, positive at 2x costs); re-entries carry 83% of the clean-grid loss via the cost identity cost-in-R = cost_bps / stop_bps; add-signal drought (not the BE gate) is the pyramid's binding constraint; four intraday cells near-bankrupted (<1% equity).

### Entry 5
Fractal lens adopted as standing analytic frame (operator, 2026-07-18): price structure may replicate across timeframes with scale-adapted variables; never treat cross-TF signals/outcomes as "different animals" by default — study the correlations explicitly and falsifiably. Working hypothesis from existing evidence: GROSS behavior is fractal-compatible; NET behavior is provably not, because costs are scale-invariant in bps (toll 14/20/30) while excursions scale with TF (median MFE 10/28/57 bps intraday/swing/position) — "same animal, different tax bracket." TC-5 (1H/5m re-spec) = fractal repair. Falsifiable program FH-1..FH-5 (signal-shape invariance, R-space anatomy invariance, relative-vs-absolute MTF gradient discriminator, cross-mandate nesting census, rule transfer) rides RC-7 mostly free. FH-3 discriminator gates C13b's form (absolute 30m vs "two steps below governor"). Cross-scale replication added as an S-2 detector robustness bar. Reporting convention: all MTF tables print absolute AND governor-relative coordinates.

### Entry 6
STUDY FINDINGS THROUGH 2026-07-20 — conclusions and standing rulings (merge M-2, operator-approved 2026-08-02; absorbs former entries #7 RC-7 rulings, #8 RC-7r, #9 S-2, #11 S-2b, #12 TC-5, #13 S-3. FULL NUMERIC DETAIL LIVES IN LEDGER.md — memory keeps only what changes design). ARCHITECTURE OF RECORD: the STRUCTURAL STOP (anchor beyond a real 1h swing pivot, not a fixed distance) is the dominant architecture — grid +565.58, strip-best +301.99; 52% of 4,167 shakeouts convert to ≥1R; SWING flips positive-and-robust for the first time (+384.82/+250.62); struct-30m dropped as a lottery (strip-best sign-flips). FALSIFIED, and memory must not re-assert it: the TWO-LINE exit architecture was ratified 2026-07-19 and killed by S-2 (grid −121.71 vs band [500,650]) — the live native ratchet almost always fires first and the trail clips engaged winners; ranking F4-structural ≫ corrected fold-in ≫ baseline ≫ two-line. D-2 re-ruled to the TC-1 2x2 factorial {stop: bar-extreme vs structural-1h-static} × {exit: none vs gov-e200 trail}, implemented in the TRADING layer so signals.py stays byte-untouched. THE HARVEST PROBLEM (priority #3, still unsolved): PROTECTED paradox inverted in cause — 100% zero-stop-advance under struct because the stop protects by WIDTH not advance; 73.67% of trades reaching +1R still reverse to gross loss; median peak-capture −43.65% WORSE than baseline. The winning book wins by RIDING TO REGIME BREAK (opposite_cross 719@67%, failure_x 1352@24%), never by managing individual winners. ADD GATE: P-CIRC confirmed emphatically — 97.59% of 3,114 +1R winning moments had NO pullback-reclaim PRIME available; the gate is frozen exactly when adding is wanted (circular topology). ENTRY ADMISSION: P-F1 vindicated the biggest gate (229,963 no_zone rejects would run −1.205/unit vs −0.577 admitted); D3 confirmed — 8 conditions negative on S-1 flip POSITIVE on the struct book, slow TFs strongest (1d s2_aligned +0.229); P-LAT falsified both halves (no entry cohort positive under the old exits); C14 nesting, F8, and the detectors as entry signals all retired. FRACTAL PROGRAM: FH-1/FH-2 falsified; FH-3 CONFIRMED RELATIVE — the MTF gradient is governor-relative, not absolute-TF, so all cross-TF rules are expressed in governor-relative steps. TC-5 (1H/5m): mechanical fractal CONFIRMED (mfe/toll 0.65→1.79) but economic claim FALSIFIED (net −1,180.32) — intraday's disease is SIGNAL as well as scale; entries floored at 5m PERMANENTLY; survival-matched recount showed PRIME density 4.2× sparser and the apparent birth-count rise was purely a survival artifact; TC-2 (re-entry quality bar) inherits the mandate. SIZING: every lever is negative first-order on the current book — sizing is deferred post-TC-1 and, when it returns, uses pre-registered count-based tiers, never fitted weights. MACHINERY: MTF_SET = {5m,1h,4h,12h} — the engine has NO 1D and NO 30m anywhere; tf_align = one MTF_SET rung below governor, which makes the 1H/5m mandate a COMPRESSED confluence ladder. STANDING RULE from F-ENG: completion commits must contain every file required to reproduce the run from a clean checkout.

### Entry 7
Operational rule (ratified 2026-07-19): whenever the reviewer produces next steps that include commands, questions, or instructions for the builder (Claude Code) or the terminal, it must provide the EXACT paste-ready text in a code block — never a paraphrase or description of what to ask. Applies to git commands, builder one-liner questions, and contract go-pastes. Purpose: operator pastes and goes, no reconstruction needed.

### Entry 8
Standing reviewer practice (ratified 2026-07-25): periodically — roughly once or twice a day, and always at the start of a session or a new phase — audit whether the project has produced files the reviewer has NO access to in the context box, and judge whether any would improve reasoning or context. Method: grep the ledger, contracts and in-box scripts for referenced repo paths and import statements, diff that against the box contents, rank gaps by which upcoming artifact each unblocks. If gaps matter, either ask Ludwig to copy them into the project box or issue an exact paste-ready builder instruction (per the paste-go rule). Rationale/precedent: the 2026-07-25 audit added the engine package, configs and data-estate manifest, and reading engine/cells.py immediately produced two material corrections — the engine has no 1D or 30m timeframe anywhere (INTERVAL_MS/MTF_SET stop at 12h), so D5's graduation basis of "robust on 12h/1d" is half un-representable and narrows to 12H/position primary + 4H/swing secondary; and the census's "governor lens" is a measurement frame, NOT the engine's per-cell tf_gov. Reviewer spec errors have consistently been errors about what the machinery actually contains, so this check moves verification earlier, into contract drafting.

### Entry 9
Claude-optimization track opened 2026-07-26 (runs parallel to the study): (1) Two skills delivered as installable .skill files — naiad-custodian (session-start context-gap audit; integrity toolkit: Python-hashlib binary reads only, msys grep/sed silently strip CR and are unreliable for byte questions; one-paste-complete-manifest rule; explicit destination filenames; authorization boundaries) and naiad-daily-brief (Atlas-styled ops report; two-half architecture: builder computes from repo data estate, reviewer verifies + adds calendar via web search + writes bias synthesis). (2) Ratified ops builds, contracts drafted 2026-07-26: scripts/reviewer_manifest.py (one-paste integrity JSON: HEAD, status, per-file sha256 + HEAD-blob match, untracked-root inventory; fixtures F-M1..4) and scripts/daily_brief.py (layers: structure/VP+TPO/rolling AND anchored VWAP/MTF-RSI divergences/vol regime/funding/OI/sessions/BTC-beta/SS governor dashboard; count-based bias scorecard with printed votes and mandatory invalidations, never fitted; LIVE-DATA FIREWALL: ops artifact, never study evidence, no journal reads, no lockbox outcome stats; /brief Claude Code command + optional Task Scheduler; fixtures F-B1..6). Both commit-no-push. (3) Knowledge program: Trading_Knowledge_Foundation_v0 + Level_Selection_Deep_Dive (Wyckoff levels/points mapped to SS: spring test = TC-2 re-entry mechanism candidate, LPS-qualified split and tested-vs-untested stop anchors = free journal-arithmetic tests; AMT opposing-level harvest = candidate for priority-#3 take-profit; CCL = MTF cross-cluster KDE level hypothesis — location-confluence reframe since timing-confluence is D9-falsified; P-CCL-3 VP-equivalence deflation gauge stated in advance; CENSUS-2 headline candidate AFTER 1c, likely Tier-A free-proxy on existing census substrate) — both docs to repo docs/knowledge/. (4) Invocation convention for knowledge work: "Depth: <topic>" = one-theme deep pass or operator interview; "Breadth: <domains>" = wide search-enriched sweep with citations.

### Entry 10
Two builder environments exist and pastes must name their target (rule encoded in naiad-custodian skill §C.0): (1) PRIMARY = local Windows Claude Code holding the working catpatrol/Naiad clone (branch v12-v1-census, contains _reviewer_box/ and the full kline/funding/journal data estate) — ALL study and ops contracts execute here; (2) ephemeral cloud Claude Code sessions = fresh scoped clones, no data estate, unpushed commits evaporate at session end. 2026-07-26 incident (contained): a cloud session scoped to catpatrol/Prometheus (the deprecated first-agent repo) initially committed the knowledge docs there on branch claude/file-knowledge-docs-pcnqba (72b921f, unpushed); after operator challenge it cloned Naiad and mirrored the commit (19cd17e, unpushed) — independently confirming origin/v12-v1-census = 140520d. Prometheus receives no new project work. Naiad durable filing done via the one-zip-one-paste bundle (naiad_filing_2026-07-26.zip) committed locally.

### Entry 11
Paste-routing + environment-assertion + file-disposition conventions (2026-07-26/27, extended 2026-08-02): every paste-go carries an operator-facing routing line ABOVE the code block ("→ WHERE TO PASTE: your LOCAL Claude Code — the PowerShell window on your PC", or an explicit cloud target) plus an in-paste ENVIRONMENT header, and opens with a HARD environment assertion that halts before ANY write — READ-ONLY pastes included, because a misrouted read paste silently returns plausible nonsense. Identity probe when unsure: `git rev-parse --short HEAD; pwd` — "local" = any session whose probe returns the C:\ (or /c/) Users...OneDrive working-clone path (desktop-app Code tab AND PowerShell CLI both qualify; the Code tab's default cwd can be the parent "Midas-Claude Code Resources" folder, so prefer the PowerShell session already cd'd into naiad). History: FOUR routing failures on 2026-07-26; the three that carried the ENVIRONMENT label were all halted by it; zero damage. Rulings: (R-A) cloud Claude Code sessions are scoped to catpatrol/Naiad ONLY — Prometheus receives no new work; (R-B) cold archives go to a second cloud (Google Drive/Dropbox) via backup_estate.py, dated + hashed, generations accumulate; (R-C) ledger corrections fold into the NEXT entry rather than getting their own. FILE DISPOSITION TABLE (operator-requested standing rule, 2026-08-02): every builder report must END with a table covering every file the paste created, modified or moved, with six columns — full repo-relative PATH · EXISTS on disk · TRACKED / untracked / ignored (and which .gitignore line if ignored) · COMMITTED in which short SHA (or "not committed") · PUSHED yes/no plus the remote ref · PROTECTED BY which backup (estate zip / phase archive / --workflow archive / GitHub only / NOT PROTECTED). Purpose: the operator can see at a glance where every artifact lives and whether losing the machine would lose it. Committed ≠ pushed; pushed ≠ backed up; and "not visible on GitHub" is usually the browser showing the DEFAULT branch (main) rather than v12-v1-census, which carries all project work.

### Entry 12
Estate location + rulings (2026-07-27, all verified): THE DATA ESTATE IS NOT IN THE REPO AND NEVER WAS UNDER ONEDRIVE — it resolves via engine/data.py:39-48 cache_dir() to C:\Users\luisf\AppData\Local\naiad\data_cache (633.5 MB, 71 files; NAIAD_CACHE_DIR unset). census.json records RELATIVE paths only (klines/BTCUSDT_1m.parquet) so it does not pin a location by itself. The pre-2026-07-27 ledger risk note calling the journals "on one machine under OneDrive" was factually wrong for the project's whole life; superseded in commit 268cdf9. Estate now backed up as naiad_estate_2026-07-27.zip (477.6 MB, sha256 f6cc33b404e3c684f927ac16bcd54417eaa969ff7c8e4c0129fff49c174b0960, 71/71 bidirectionally verified) on TWO independent Google Drive accounts. OneDrive outage root cause = QUOTA EXHAUSTION (registry ItemCantFitInQuotaLastShown 2026-07-27T01:05:08Z against a 21.78 GB / 22,731-file tree); fixed by archiving six completed phases (tc1/s3/s2/v3_anchor/s1/tc4) to research_outputs/_archive/*.zip — 21,055.60 MB to 977.70 MB, 13,502 members, 0 mismatches, 19.61 GB net reclaimed, tree now ~1.9 GB; the 25 git-tracked files in those phase dirs were PRESERVED in place (builder halted and asked rather than deleting them). Archives kept locally as the primary copy plus both Drive accounts. RULING R-A' RATIFIED: cloud Claude Code sessions must push in the same session or the work does not count — an unpushed commit in an ephemeral environment is never treated as done. Reviewer error class logged and now guarded: verification gates must be written from the POST-action state, simulated step-by-step before the paste ships (three instances on 2026-07-26/27).

### Entry 13
Multi-chat orchestration + STATUS-block standing instruction (adopted 2026-07-27; merges former #22/#23; lane names per #24; PATHS CORRECTED 2026-08-02). LANES: APOLLO (SSv12, census, Tier-C, Engine V2, SSv12 Pine) · ARGUS (analytics toolkit, daily brief, volume filter) · ATHENA (system resilience, backups, integrity, manifest ritual, workflow — inherits the old SYSTEM lane) · DIONYSUS (independent critique) · HERMES (coordination/verification, Cowork) · HEPHAESTUS (builder). PATH CORRECTION, verified 2026-08-02: claude/ contains EXACTLY ONE file, STATUS_SYSTEM.md — claude/STATUS_ENGINE.md and claude/STATUS_BRIEF.md HAVE NEVER EXISTED; the real lane status docs were untracked .txt files at repo root ("STATUS — ENGINE (…).txt" 2,917 B, "STATUS — BRIEF (…).txt" 3,008 B). This entry previously asserted all four .md paths and was wrong; the builder caught it while seeding the per-lane ledgers and used the real sources with a provenance note. SUPERSEDED BY exchange/status/LEDGER_<GOD>.md per the 2026-08-02 rulings — the per-lane append-only ledgers are now the status layer of record. Chats cannot read each other; the project box, the GitHub sync bus and operator-relayed STATUS blocks are the ONLY bus, and no chat assumes knowledge of another's sessions. STANDING INSTRUCTION: end any MATERIAL session (phase registered/completed, ruling ratified, ledger entry, contract drafted, commit/push, acceptance, halt, incident, go-paste, fixtures reported) with a STATUS block, under 25 lines, no prose padding, machine-relayable: "=== STATUS_<LANE> — <date> ===" then NOW (2-3 sentences) / LAST EVENT (date + one line) / FACTS (max 6 lines, each tagged verified|ledger|ratified|handoff|unconfirmed-live|pending|open) / PENDING (numbered, operator-owned) / NEXT (single action + owner) / "=== END STATUS ===", plus the two Q-8 integers (operator actions this session; files re-ingested to seed). In the ARGUS lane every feature fact also carries its firewall side (display-only vs evidence). Lane attribution is by CONTENT, not chat title — flag it when emitting so the coordinator files events correctly.

### Entry 14
Workflow-design rule (operator, 2026-07-27): whenever a workflow requires reviewer↔builder coordination, the reviewer must design it to optimize operator attention and Claude time — decision-tree contracts that execute autonomously until an operator decision is genuinely required, halting only on failures or true forks. At fork points the reviewer runs a plain-language interview: each question states its purpose and the implications of each option for a non-programmer, carries a recommended default, and is answerable in one word. Minimize screenshot-bridging: contracts instruct the builder to hand back machine-readable reports/artifacts into _reviewer_box (plus STATUS blocks) so the operator drags files instead of screenshotting terminals.

### Entry 15
BRIEF lane — purpose, labor division and roles (operator-ratified 2026-07-27). PURPOSE: the operator's discretionary method has a MECHANICAL half (where levels are, which tools agree at each price, regime per timeframe, funding/OI posture) and a JUDGMENT half (what it means, whether to take it, what would prove him wrong). The brief automates the mechanical half only. Four objectives, in order: (1) SERVE THE TRADE — reproduce the grammar of his manual Notion review: every layer produces levels → levels earn authority through multi-tool confluence → output collapses to two lines-in-the-sand per asset plus if-then trade hypotheses; analysis is never an end in itself; (2) BUILD A COMPOUNDING RECORD — identical printed rules every day make captures comparable across months; each capture is self-describing (embeds the rule set + rules_version + sha that produced it) so an old number still explains itself after the rules move on; JSON is the record, HTML a disposable regenerable view; (3) FEED NAIAD THROUGH THE CORRECT DOOR ONLY — the archive is a hypothesis mine and the proving ground where shared measurement tools get stress-tested by daily use (a bug hides in a study batch job, but gets caught in a week when read every morning); it is NEVER evidence; (4) FORCE TACIT SKILL INTO EXPLICIT RULES — automating requires stating what counts as a level, how close is the same level, what makes one area stronger; this is the same mechanism by which Secret Sauce became a Pine script then a replay engine. GOVERNING TENSION: the brief consumes live/lockbox-era data, legitimate for ops and forbidden as evidence; the confluence engine is the sharpest edge because a high-scoring area FEELS like a discovery when it is only a statement that tools agree — whether agreement predicts anything is a study question (CCL/CENSUS-2) answerable only on exploration-classic data under G-7. LABOR DIVISION: operator = judgment, all merges, all gate decisions, the position file and journal notes, TradingView parity readings; reviewer (this lane) = specification, contracts, independent acceptance, falsification on record, plain-language interviews at true forks; builder (local Claude Code) = execution, fixtures, commit-no-push. Chats cannot read each other; the project box and operator-relayed STATUS blocks are the only bus.

### Entry 16
Standing workflow preference (operator, 2026-07-29, extends #24): design workflows to maximize the reviewer's AND the builder's productive time without requiring constant operator attention — autonomous decision-tree contracts, fat single round-trips, and parallel builder work whenever a contract is independent of open interviews; builder idle time during reviewer↔operator sessions is a cost to design against.

### Entry 17
Renames + rescope (operator, 2026-07-29): the ENGINE chat is now APOLLO — it owns Secret Sauce v12 (the v12 study IS SSv12), the census (an instrument of SSv12 development, part of v12), Tier-C runs, Engine V2, and a NEW deliverable: the SSv12 Pine indicator for the operator's discretionary TradingView use, produced after the next census + first Tier-C. The BRIEF chat is now ARGUS — it owns the analytics toolkit, the daily brief, and the toggleable multiTF volume filter (rolling VWAP at 7/30/90/365 calendar-day windows + volume profile over the same trailing windows, superimposed on 4H/1D/1W/1M charts, producing on-command HTML reports incl. RSI/StochRSI/MACD analysis; a daily tool whose volume-based value areas will be studied against SS signals). Prior conclusions — including the "entry combinatorics don't tilt raw price" macro-ruling — are points of control (best available conclusion from the data at the time), NEVER mandatory design constraints; v12 maintains critical awareness of inherited v10/v11 entry & add assumptions. Working SS definition (pending refinement): scrutinize the multiTF clustering of signals from the 9/89/200 EMA trio — a "signal" = a meaningful EMA/EMA interaction (cross OR test-and-rejection) or EMA/price interaction (test-and-bounce / SFP-like, or an S/R level break-and-flip) — to read range-vs-breakout, locate the meat of the move, and derive ideal entry/exit points; {9,89,200} and {12,25} are measured jointly AND separately, comparing sequential cross-cascade "fingerprints"; range detection is fractal across TFs; v12 entries are gated by "areas where I want to do business" (range extremes; RESPECTED vs DISRESPECTED range verdicts) to kill the stillbirth population.

### Entry 18
Decision-gate & workflow rules (operator, 2026-07-29): (1) every decision point is presented with full plain-language context — why the decision is required, its conceptual meaning, all available options including the reviewer's recommendation, and the implications of each — organized as funnel-type cascades of interview-based decisions (HTF→LTF), assuming no technical background; (2) "keep everything moving that can be moving" — all independent workstreams run in parallel, always; (3) at every completed builder work cycle, the builder displays on screen the exact filenames the operator must bring to the reviewer chat plus the exact repo directory path of each, with the filenames highlighted in bright colors; (4) sequence ruled 2026-07-29: scoping interview → Secret Sauce deep-dive interview (operator answers + Atlas feedback next) → census scope/measurement/workflow design (rich HTML decision reports) → accessory spec interviews later (RangeDetector; Analytics via the ARGUS contract; Ops) — accessory interviews are NOT presented early.

### Entry 19
SS interview rulings (operator, 2026-07-29), locked for census design: Q1c full signal taxonomy (crosses + EMA/EMA test-and-reject + price test-and-bounce + S/R flip + ribbon-state episodes as first-class events); Q2a sequential fingerprint mining = census headline ({9,89,200} and {12,25} alphabets in parallel); Q3a anti-fishing package ratified as written (pinned alphabet/length/support floors, net-of-toll scoring, five-asset replication bar, held-in-time split inside exploration-classic, quarantined annex, ladder-conformity anchor); Q4b+f lenses extend to 1W measured + 1M display-only, regime anchor measured at every lens with engine-representability printed; Q5c+b verdict shape measured with hysteresis as registered prior — operator: hysteresis is the bulk first filter, finer filters later; trap-rate table mandatory; Q6c stillbirth counterfactual first (rescore all historical fills by range-position before any location gate becomes law); Q7c borders = walls + rectangles counted, midrange measured as first-class cluster object; Q8b PRIME = mandatory control arm, fingerprint triggers measured beside it; Q9c all three add-families (HTF-band revisit / micro-range verdict / P-PYR-1-conditioned 4H rung) counted at winning moments; Q10a exit counterfactual leg in 2b + operator prediction (prov. H-RVX-2): opposing-wall harvest levels show significant confluence with 7D/30D rolling-VWAP sigma-bands — measured counted vs null; Q11a provisional (2a EMA-native + anchor timestamps only; volume overlay in 2b post-ARGUS adoption), dedicated volume-in-census scoping interview queued APOLLO-side. Operator analytical principles: standing prediction that sequential 9/89/200 cascade examination sharpens entries/exits; analysis always HTF→LTF (trend tier 1D/1W/1M, build tier 12H–5m); LTF structure read against its HTF enclosure (a 5m "range" may be one 4H wick) — census stamps every structure with HTF enclosure state; signals are developing relationships (price↔EMA, EMA↔EMA), episodes + events; MTF patterns also serve as watchlist/asset-screening layer (SSv12 Pine scanner DNA). Post-Atlas sequence: operator delivers Atlas feedback + intuitions → APOLLO analyzes against all collected data per established guidelines → tightening SS re-interview ("measure twice, cut once") → SSv12 design.

### Entry 20
Prime directive (operator, 2026-08-01, verbatim): "Project Naiad's main purpose is to develop consistently profitable trading systems." Secret Sauce is ONE of Naiad's systems, not the project itself.

### Entry 21
Olympian architecture + pause (operator, 2026-08-01, from hand-drawn diagrams + instruction doc): Naiad's AI infrastructure = six roles. APOLLO (this chat: SSv12, census, Tier-C, Engine V2, SSv12 Pine) · ARGUS (all-seeing analytics, daily brief, volume filter) · ATHENA (new; inherits SYSTEM lane; system resilience/sustainability, context compaction, backups, file-sharing simplification) · DIONYSUS (new; creative critique — questions shapes, division of labor, coordination; probably a Cowork task) · HERMES (the Cowork task formerly "orchestrator", renamed; coordination + verification node with visibility across the project; supports Athena on integrity; tracks per-Olympian changes conciliated in shared files) · HEPHAESTUS (the builder = local Claude Code). Apollo/Athena/Argus/Dionysus work in parallel; Hermes spans them. TRADING-SYSTEM DEVELOPMENT PAUSED as of 2026-08-01; highest priority = the ClaudeAI–Ludwig workflow redesign: friction-free, low-ferrying flow via a shared exchange directory in the local repo where all reviewer emissions and builder reports land by convention (proposed: exchange/{contracts,reports,status,drops}; Athena owns the scoping interview, Hermes stewards visibility; operator attention reserved for judgment and gates). Five primer docs drafted by Apollo 2026-08-01 to seed the sibling chats; Cowork surfaces (Hermes/Dionysus) can read/write the local repo directly — web project chats cannot, they share only the project box and project memory.

### Entry 22
Workflow architecture RULED (operator, 2026-08-02, via FUNNEL_DIONYSUS_W1 + FUNNEL_ATHENA_W2 "defaults"). CAPABILITY FACTS: (F2) the Claude project box can SYNC FROM GITHUB — link repo+branch, select folders, one operator "Sync now" click makes every pushed file readable by all web lanes; this is the visibility bus that replaces per-file ferrying. (F4, tested 2026-08-02) SCHEDULED Cowork runs CAN read/list/write the mounted local repo live (falsifies the "scheduled = remote-only" assumption) BUT have NO network egress (github.com/pypi/binance blocked → a scheduled run CANNOT git push) and no OAuth connectors; git availability untested; alarmingly, allow_cowork_file_delete granted deletion UNATTENDED with no human gate → written policy: scheduled lanes get no delete duties. W1 RULINGS: Q-1 C sync bus + DIGEST as an index of pointers (never a re-authored substitute) · Q-2 A auto-push scoped to exchange/** + lane ledgers ONLY, evidence never auto-publishes · Q-3 A per-lane append-only ledgers exchange/status/LEDGER_<GOD>.md, central LEDGER.md stays the evidence ledger · Q-4 A hybrid Hermes 2x/day scheduled + on-demand, and CRONOS = the schedule itself + staleness timestamps (gate W-3 resolved, no seventh agent) · Q-5 builder queue exchange/queue/ with drafting rights APOLLO/ATHENA/ARGUS, operator ratification stamp per item mandatory, Hermes validates+sequences, Hephaestus works the queue · Q-6 A Apollo stays a web chat, HELIOS tripwire on record (spawn only if Hermes misses two consecutive cycles or verifies his own executions) · Q-7 A CHALLENGE answer debt is non-blocking, written answer due by next phase boundary · Q-8 A every session-end STATUS carries two integers (operator actions this session; files re-ingested to seed), Hermes tallies weekly. W2 IMPLEMENTATION (Athena, all defaults): exchange/{status,queue,reports,drops} + DIGEST.md, a queue item IS a full contract carrying its RATIFIED stamp (no separate contracts/ folder); text-only 1 MB per-file cap, bigger artifacts referenced by path+sha pointer; _reviewer_box migrates but STAYS LIVE during transition with a routine sweep so in-flight Apollo/Argus pastes never break; manifest v1.1 (round-trip F-M1, config --get, onedrive_running, ledger_head, lane_last_commit, queue_open, BOX_DIR); publish step is path-scoped and staged-set-verified so evidence physically cannot ride an auto-push; Task Scheduler arms daily routine 07:00 and weekly backup Sunday; retention = keep newest 4 estate generations + 1 phase set, REPORT-only, never auto-prune. Operator's recurring floor by design: drops + one Sync-now click + verdicts.

### Entry 23
REVIEWER ERROR TAXONOMY — three classes, each with its cure (named 2026-08-02; the reviewer's mistakes are almost never analytical). CLASS A · MACHINERY-CONTENT: asserting what the code or data contains without reading it. Instances: census.json records RELATIVE paths and cannot pin a location; tf_align=15m specified when 15m is not in MTF_SET; "estate under OneDrive" false since Phase 0; claude/STATUS_ENGINE.md and STATUS_BRIEF.md specified when they have never existed. CURE: the custodian context-gap audit moves into CONTRACT DRAFTING, not after; any §Basis claim must cite a file actually read this session. CLASS B · POST-ACTION-STATE: writing a verification gate from the state before the paste's own actions run. Instances: a five-file deletion list against an empty-worktree gate when ten existed; a ledger entry asserting 18/18 while the same paste restored a file making it 19/19; a phase-directory deletion contradicting its own expected-EMPTY git status; .gitignore covers *.zip but not the .sha256 sidecar, so writing it legitimately failed F-K3. CURE: simulate the paste's own steps in order and write every gate from the POST-action state before shipping. CLASS C · PROVENANCE: promoting an operator or builder report to [verified]. Instance: a STATUS block tagged a Google Drive folder [verified] when the source was an interview answer and the folder did not exist. CURE: operator-reported is [operator], builder-reported is [handoff]; [verified] means computed or read this session. COUNTER-PATTERN, and it is the system working: the builder has refused a reviewer instruction on a ratified invariant at least six times — deleting git-tracked files, committing raw candles against charter §10, rewriting an allowlist into a Bash-breaking backslash form, appending false ledger history in a misrouted clone, treating a push authorization as transferable across environments, and throwing away real lane history to honour a wrong filename. A stack whose executor never pushes back is a stack whose reviewer errors all land.

### Entry 24
BACKUP ARCHITECTURE — three tiers (Athena, 2026-08-02/03). PRINCIPLE: sync is NOT backup — OneDrive, Google Drive and Dropbox all propagate deletions and corruption faithfully; version history is a retention window, not durability. The architecture is dated, non-overwriting, hash-pinned archives across independent providers. TIER 1 · DATA ESTATE (irreplaceable): C:\Users\luisf\AppData\Local\naiad\data_cache via engine/data.py cache_dir(), ~70 files / ~653 MB; backup_estate.py --estate writes dated generations to G:\My Drive\naiad-backups plus two browser-uploaded Drive accounts (the second account is NOT machine-verifiable). TIER 2 · REPO: GitHub is the durable copy of everything tracked; phase archives live in research_outputs\_archive\*.zip as the PRIMARY copy plus Drive — do not delete the local archives, that would leave evidence at a single provider. TIER 3 · THE AI OPERATING SYSTEM (the tier that was missing): conversations export ONLY manually — Settings > Privacy > Export data, emailed link, monthly habit — and CRITICALLY, PROJECT MEMORY IS NOT INCLUDED IN THAT EXPORT, so reviewer-written snapshots under docs/memory/ are the ONLY durable copy of these ratified entries; user preferences, project instructions and custom styles are cloud-only and must be hand-pasted into docs/primers/OPERATOR_PREFERENCES.md; backup_estate.py --workflow archives docs/memory, docs/knowledge, skills, prompts, claude, exchange, docs/primers, docs/history and drops/operator-exports. VERIFICATION STANDARD (all modes): bidirectional per member — bytes re-read OUT of the archive must hash equal to a fresh read of the source AND every embedded manifest pin must agree with both — plus set membership cross-checked archive/manifest/disk so no stray or omission can hide, plus F-K5 re-reading the archive FROM the destination after write (catches truncated or half-synced cloud writes), plus a no-clobber guard making generation loss structurally impossible. RESTORE HAZARD, verified on both tc5 and s3: archives embed MANIFEST.json at their root while some phase folders carry manifest.json — on case-insensitive Windows, extraction SILENTLY OVERWRITES one with the other. Restore case-sensitively or rename one entry first. RETENTION: keep newest 4 estate generations + 1 phase set + 4 workflow generations; the scripts REPORT what falls outside the rule and never delete — pruning is an operator action.

### Entry 25
INFRASTRUCTURE INVENTORY (Athena, current 2026-08-03 — refresh rather than re-derive each session). SCRIPTS, all under scripts/: reviewer_manifest.py v1.1 — one command emits the integrity manifest (HEAD, branch, ahead/behind, porcelain, per-file size+sha256+match_head for ~46 tracked sources, box mapping, untracked-root inventory, eol_config); v1.1 added a round-trip F-M1, BOX_DIR pointing at exchange/status/, onedrive_running, ledger_head, lane_last_commit (by commit prefix), queue_open; F-M4 now normalises out its own tracked output path or it would fail on every run. daily_brief.py — Atlas-styled market report, F-B1..8, ~337 s, LIVE-DATA FIREWALL (ops artifact, never evidence, no journal reads). daily_routine.py + routine_jobs.json — a config-driven JOB REGISTRY (new jobs are added by editing JSON, never code) that runs the manifest and brief, stages outputs, writes DAILY_<date>.md with a change-since-last-run diff, and sweeps _reviewer_box into exchange/. backup_estate.py — modes --estate / --phase / --verify / --workflow, fixtures F-K1..7 including tracked-file preservation; deletion is opt-in behind --delete-source and never touches the estate. publish_exchange.py — the shared path-scoped publish guard, dry-run 12/12 including rejecting an 'exchangeable/' lookalike, so evidence physically cannot ride an auto-push. TRIGGERS ARMED (Windows Task Scheduler): "Naiad daily routine" every day 07:00; "Naiad weekly backup" Sundays 08:00. ENVIRONMENT: the venv is at C:\venvs\naiad (Python 3.12.10), OUTSIDE OneDrive and NOT in the repo — a venv cannot be moved, only rebuilt, and scheduled tasks must call python by full path. LAYOUT: exchange/{status,queue,reports,drops} + DIGEST.md; per-lane append-only exchange/status/LEDGER_<GOD>.md; exchange/status/CADENCE.md is the trigger registry; _reviewer_box stays LIVE during transition so in-flight pastes never break. SKILLS INSTALLED: naiad-custodian, naiad-daily-brief. SCALE: repo tree ~1.41 GB after the tc5 release (was 21.78 GB before the 2026-07-27 reclamation); phase archives 977 MB local; C: ~35 GB free; G: ~11 GB free.

### Entry 26
SURFACE-ACCESS MAP — who can see and touch what (Athena, 2026-08-03). ⚠ THE FINDING THAT COST SEVEN TURNS: when a project is in SEARCH MODE (shown in project settings beside the capacity bar), GitHub-synced repo files are INDEXED FOR RETRIEVAL, NOT MOUNTED as browsable files. Listing the mounted project directory shows ONLY the flat hand-uploaded files and will report the sync as broken when it is working perfectly. THE CORRECT TEST IS THE PROJECT-KNOWLEDGE SEARCH TOOL, never a directory listing. Tell them apart by the path: synced files return real repo paths (exchange/status/CADENCE.md), hand-uploads return flattened names with fingerprints — a leading dot stripped (.gitignore → _gitignore) or a doubled underscore from a browser's (1) duplicate suffix. WEB CHATS (Apollo, Argus, Athena): share the project knowledge box and project memory; CANNOT read each other's conversations, CANNOT touch the local disk, CANNOT run on a schedule. Additions to the box DO appear mid-conversation. COWORK SURFACES (Hermes, Dionysus): read/list/write the mounted local repo and run code in a Linux sandbox; CAN write files into the project box. Scheduled Cowork runs DO see the live local folder (F4, 2026-08-02) but have NO network egress — github.com, pypi and api.binance.com all blocked, so a scheduled run CANNOT git push or fetch market data — and no OAuth connectors; git availability untested. Alarming F4 finding: allow_cowork_file_delete granted deletion UNATTENDED with no human gate, hence the standing SCHEDULED-LANE NO-DELETE POLICY. HEPHAESTUS (local Windows Claude Code): the only actor touching the data estate; commit-no-push by default; auto-push scoped to exchange/** only. THREE ACCESSES THAT ARE NOT THE SAME THING: (1) Trusted Cowork folders reach Cowork lanes only; (2) files dragged into a chat live in that ONE conversation and never enter the box; (3) project knowledge / GitHub sync is the ONLY path reaching every web lane. GITHUB SYNC: link repo + branch (v12-v1-census, NOT main — browsing github.com opens on the default branch and will look empty), select folders, click Sync now. Box capacity is FINITE (was 92%) — hand-uploaded copies crowd out the sync meant to replace them, so clean the box before syncing.

### Entry 27
DECISION-FUNNEL INTERVIEW — the ratified format for presenting data and context-informed decisions to the operator (operator, 2026-08-02, reconfirmed verbatim 2026-08-03; supersedes any compressed-register format). Ludwig has no technical background. Every gating decision, at every step, is presented as a decision-funnel interview with detailed context: (1) WHAT IS IT — remind him what this is in plain language, assuming zero context loaded; (2) WHY DOES IT MATTER — why we are doing it, what breaks or improves, and why it needs HIS attention specifically rather than the reviewer's; (3) OPTIONS — every actionable option, including the reviewer's suggested path; (4) IMPLICATIONS — clear consequences for and against each option. The reviewer's RECOMMENDATION must be APPROPRIATELY SUPPORTED by evidence actually in hand; where the reviewer is uncertain, it must say so explicitly, explain further, and REQUEST GUIDANCE rather than manufacture a confident default. Questions must be self-contained — never reference earlier scrollback. Big questions first, details cascading below (funnel shape, HTF→LTF). One-line answer affordances (defaults + exceptions by number) remain welcome, but context richness is never traded for brevity.

### Entry 28
Builder handback standing rules — TWO artifacts, mandatory in EVERY package sent to the builder by ANY Pantheon lane (operator, 2026-08-02 extended 2026-08-03; no exceptions). (1) BUILDER'S REPORT — the forensic record. Written as a FILE, never a screen-only block, to exchange/reports/BUILDERS_REPORT_<LANE>_<date>_<phase>.md, standalone and readable with zero context: gate/probe values, full fixture transcript, full result tables not summaries, versions/hashes/commits, findings reported-not-fixed, exact repo paths of every artifact, and the six-column file-disposition table. A file must never contain its own sha256 — the row is permanently stale. (2) SESSION SUMMARY — the DECISION artifact, a separate file beside it (SESSION_SUMMARY_<LANE>_<date>_<phase>.md), required at the end of any large block of work and at INTERIM stage boundaries when the block is very large. It carries everything the operator and reviewer need to discriminate the best path forward toward Naiad's objective of consistently profitable trading systems: what was built and what it is for, what was decided and on whose authority, what was found that changes the plan (especially findings reported-not-fixed and any operator ruling reinterpreted mid-build), what remains open with its owner, and the honest next options with their implications. Plain language for a non-technical operator, zero prior context assumed, no jargon left undefined. BOTH artifacts are copied into exchange/reports/ with sha256 verified before and after; the paste then runs scripts/publish_exchange.py to push exchange/** (the ratified W1 Q-2 exception to commit-no-push) and states plainly on screen whether the push SUCCEEDED so the operator knows whether to click Sync now. Never write "skip silently if absent" — if a directory is missing, create it and say so. On-screen output stays short and displays, in bright colors, the exact filenames and full repo paths the operator must carry. Purpose: eliminate screenshot-ferrying entirely; the files are the record.

### Entry 29
Manual-task instruction rule (operator, 2026-08-02): when asking Ludwig to perform any hands-on task (parity readings, chart captures, screenshots, data collection), the instruction must be fully self-contained and specify EXACTLY what to capture — which instrument/symbol, which timeframe, which period or candle, which indicator and its exact settings, and what must be visible in the frame if he screenshots (symbol, timeframe, hovered candle's timestamp, the values panel). Never say "take the readings" and assume he knows which ones. Never assume he has prior context loaded, has the referenced file open, or is doing only this task. Instructions must be chunked and resumable so he can stop after any block, and must state what each block certifies. Prefer designs that remove dependencies on artifacts he may not have (e.g. recompute reviewer-side against whatever candle he read, rather than requiring him to find a specific pre-named candle).

### Entry 30
Rulings 2026-08-03 (operator: "forensics / discriminant / write synthesis / scoping interview pre-Atlas-feedback"; SS work RESUMED in APOLLO): (1) RESEARCH INVERSION adopted — condition on outcome, discriminate at birth: exhaustive scrutiny of big winners vs losers; the loser book is TWO species with opposite cures (born-dead births → entry gates; died-of-neglect, the 73.67% of +1R-touchers that round-trip → harvest fixes). (2) DISCRIMINANT FRAME + TAIL-RETENTION GAUGE ratified as MANDATORY census deliverables: discriminants must be decision-curtain-clean, five-asset sign-replicated, held-in-time stable, BH-FDR q=0.10 within each discriminant family; TRG = every candidate filter/gate reports the fraction of the unfiltered book's top-decile R it retains, printed beside strip-best (a filter that lifts expectancy but keeps 40% of the tail is condemned). (3) W-F1 "Winner Forensics" commissioned [RATIFIED "forensics"]: Tier-A top-vs-bottom-decile profiling of the 7,094 resolved S-3 tranches (join (cell,tranche_id); r1=3,825 primary, v=78 excluded+counted, re_entry=3,214 secondary; stopout = exit_reason∈{stop,stop_gap} = 5,016) including the first funding_cum realized-funding read; queue item in exchange/queue/. (4) FOCUS: a novel interpretation of SEQUENTIAL patterns in the multiTF cascade; SEQ scoping interview issued BEFORE the operator's Atlas feedback, presenting both Atlas v2.1 and Cascade_Rewire findings. (5) Cascade_Rewire.html = DIONYSUS's first artifact (memo signed "Dionysus → APOLLO"). Its discovery: the LEAP is the dominant activation mode of the 4h+ tier in PURE COUNTS with no outcome data — 66% of 4h arrivals jump directly from the fast tier (≤30m) while 1h is a stair rung (75% adjacent arrivals) — so the operator's motif family is structurally motivated, not outcome-fished. Six-item DATA REQUEST logged (per-cascade event stream with MAE + curtain-cut flags; slow-step timing; per-lens AND per-asset sequence tables; curtain-cut outcomes; depth-complete rung-strings; MAE per terminus); consolidated extract = census 2a substrate candidate. SS Reassessment Synthesis 2026-08-03 filed to exchange/reports/ (reaches the box via GitHub sync — no hand-uploads, box at 92%).

## End of snapshot — 30 entries.
